# Named MO2 mod export manifest

Immutable `0.1.0` evidence for an explicit, refusal-safe export into one named
Mod Organizer 2 mod directory. Absolute local paths are machine evidence and
must be reviewed before publication.
