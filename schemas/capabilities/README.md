# Capability Registry Schemas

Capability registry schemas validate capability data and provider references
declared by a WastelandForge project.

R004 and R005 require capabilities and providers to be modeled explicitly.
Gate 13 adds `0.1.0/schema.json` as an immutable Draft 2020-12 public schema.
