# ADR-009 - Generator and Build Pipeline Architecture

Status: Accepted
Research classification: Documented
Source: `WasteLandForge/research/WastelandForge Generator and Build Pipeline Architecture-deep-research-report.md`

## Decision

WastelandForge outputs are generated through a deterministic, capability-aware build graph.

Generated artifacts are disposable and rebuildable. Every output must carry provenance, at minimum through a local build manifest once build work exists.

## Consequences

- Build inputs must be declared and traceable.
- Generated outputs belong under `generated/` or `dist/`.
- Clean operations must target generated outputs only unless explicitly authorized.
- Build manifests are mandatory for build/release gates.

## Gate Impact

Gate 1 creates disposable output boundaries. Gate 9 implements the first build
manifest and release dry-run evidence. Gate 61 implements the first
deterministic `forge generate` and `forge build` metadata-report outputs, with
generate outputs under `generated/` and build evidence under `dist/`. Gate 62
adds the first game-facing deterministic text output through the
`mcm-json` target, still carrying local manifest and checksum evidence.
Gate 63 validates that generated MCM output against a local output schema
before files are written.
Gate 64 adds deterministic translation INI outputs and records them in local
manifests/checksums.
Gate 65 adds deterministic checkbox and string-toggle option generation while
keeping the same generated/dist output boundaries and manifest evidence.
Gate 66 adds deterministic keybind option generation while keeping the same
generated/dist output boundaries and manifest evidence.
Gate 67 adds deterministic header option generation while keeping the same
generated/dist output boundaries and manifest evidence.
Gate 68 adds deterministic image option map generation while keeping the same
generated/dist output boundaries and manifest evidence.
Gate 69 validates MCM image asset references before generation/build writes
outputs, while keeping the same generated/dist output boundaries and manifest
evidence.
Gate 70 stages validated referenced MCM texture assets under generated/dist
loose-file trees and records those files in manifests, output digests, and
build checksums.
Gate 71 adds deterministic package-manifest metadata for the `mcm-json`
loose-file output tree and records that metadata in manifests, output digests,
and build checksums.
Gate 72 adds deterministic build-time ZIP archive creation for the `mcm-json`
payload tree and records the archive in package metadata, build manifests,
output digests, and build checksums.
Gate 73 exposes that deterministic MCM package tree through the canonical
`forge package` command, using package-specific provenance while keeping FOMOD
and release transport outside the gate.
Gate 74 validates the generated package manifest against an embedded schema
and checks package ZIP entries against the deterministic payload before local
manifest/checksum evidence is finalized.
Gate 75 adds deterministic install-preview report evidence for MCM package
outputs without mutating external game or MO2 state.
Gate 76 validates that generated install-preview report against an embedded
schema before local manifest/checksum evidence is finalized.
Gate 77 adds deterministic Markdown install-preview summary evidence beside
the generated JSON report while preserving generated/dist output boundaries.
Gate 78 adds deterministic package-verification report evidence beside the
package manifest and install-preview reports while preserving generated/dist
output boundaries.
Gate 79 validates that generated package-verification report against an
embedded schema before local manifest/checksum evidence is finalized.
Gate 80 adds deterministic package-verification Markdown summary evidence
beside the validated JSON report while preserving generated/dist output
boundaries.
Gate 81 adds deterministic package-verification evidence cross-checks before
local manifest/checksum evidence is finalized.
Gate 82 extracts those cross-checks into reusable generated-evidence validator
code while preserving generated/dist output boundaries and provenance
evidence.
Gate 83 adds a file-based verifier entrypoint that reads generated/dist
package evidence and reuses the validator while preserving generated/dist
output boundaries and provenance evidence.
Gate 84 extends that verifier with payload digest recomputation against
generated/dist package files while preserving generated/dist output boundaries
and provenance evidence.
Gate 85 extends that verifier with archive digest recomputation against
generated/dist package archives while preserving generated/dist output
boundaries and provenance evidence.
Gate 86 extends that verifier with generated package archive entry-name
revalidation while preserving generated/dist output boundaries and provenance
evidence.
Gate 87 records that future user-accessible existing-package evidence
verification should stay under `forge package` and continue reading generated
package evidence under `dist/`.
Gate 88 implements that existing-package evidence verification skeleton under
`forge package`, reading generated package evidence under `dist/` without
regenerating outputs.
Gate 89 keeps that generated/dist boundary and adds SARIF/GitHub diagnostic
projections for the same existing-package evidence verification result.
Gate 90 keeps that generated/dist boundary and adds Markdown summary output
for the same existing-package evidence verification result.
Gate 91 keeps that generated/dist boundary and adds checksum-file
revalidation for the same existing-package evidence verification result.
Gate 92 keeps that generated/dist boundary and adds build-manifest content
revalidation for the same existing-package evidence verification result.
Gate 93 keeps that generated/dist boundary and adds install-preview summary
content revalidation for the same existing-package evidence verification
result.
Gate 94 keeps that generated/dist boundary and adds
install-preview/package-manifest entry content cross-checking for the same
existing-package evidence verification result.
Gate 95 keeps that generated/dist boundary and adds package-verification
summary content revalidation for the same existing-package evidence
verification result.
Gate 96 keeps that generated/dist boundary and adds package-verification JSON
check content revalidation for the same existing-package evidence verification
result.
Gate 97 keeps that generated/dist boundary and adds package-verification JSON
metadata content revalidation for the same existing-package evidence
verification result.
Gate 98 keeps that generated/dist boundary and adds package-verification
archive detail content revalidation for the same existing-package evidence
verification result.
Gate 99 keeps that generated/dist boundary and adds install-preview archive
detail content revalidation for the same existing-package evidence
verification result.
Gate 100 keeps that generated/dist boundary and adds package-manifest archive
detail content revalidation for the same existing-package evidence
verification result.
Gate 101 keeps that generated/dist boundary and adds archive detail
cross-report consistency revalidation for the same existing-package evidence
verification result.
Gate 102 keeps that generated/dist boundary and adds package archive presence
revalidation for the same existing-package evidence verification result.
Gate 103 keeps that generated/dist boundary and adds checksum unexpected-entry
revalidation for the same existing-package evidence verification result.
Gate 104 keeps that generated/dist boundary and adds checksum duplicate-entry
revalidation for the same existing-package evidence verification result.
Gate 105 keeps that generated/dist boundary and adds checksum canonical-order
revalidation for the same existing-package evidence verification result.
Gate 106 keeps that generated/dist boundary and adds checksum digest
canonical-casing revalidation for the same existing-package evidence
verification result.
Gate 107 keeps that generated/dist boundary and adds checksum line-ending and
trailing-newline revalidation for the same existing-package evidence
verification result.
Gate 108 keeps that generated/dist boundary and adds checksum path separator
canonicalization revalidation for the same existing-package evidence
verification result.
Gate 109 keeps that generated/dist boundary and adds checksum blank-line
revalidation for the same existing-package evidence verification result.
Gate 110 keeps that generated/dist boundary and adds checksum entry spacing
canonicalization revalidation for the same existing-package evidence
verification result.
Gate 111 keeps that generated/dist boundary and adds checksum path casing
canonicalization revalidation for the same existing-package evidence
verification result.
Gate 112 keeps that generated/dist boundary and adds checksum case-insensitive
duplicate revalidation for the same existing-package evidence verification
result.
Gate 113 keeps that generated/dist boundary and adds checksum malformed-entry
format revalidation for the same existing-package evidence verification result.
Gate 114 keeps that generated/dist boundary and adds checksum path containment
revalidation for the same existing-package evidence verification result.
Gate 115 keeps that generated/dist boundary and adds checksum comment-line
rejection revalidation for the same existing-package evidence verification
result.
Gate 116 keeps that generated/dist boundary and adds schema-validated
`install-plan.json` plus `install-plan.md` as install-ready export planning
evidence for the `mcm-json` generate/build/package path.
Gate 117 keeps that generated/dist boundary and adds install-plan JSON and
Markdown content revalidation to the same existing-package evidence verifier.
Gate 118 keeps that generated/dist boundary and adds install-plan schema
revalidation to the same existing-package evidence verifier without
regenerating package outputs.
Gate 119 keeps that generated/dist boundary and adds package-manifest schema
revalidation to the same existing-package evidence verifier without
regenerating package outputs.
Gate 120 keeps that generated/dist boundary and adds install-preview schema
revalidation to the same existing-package evidence verifier without
regenerating package outputs.
Gate 121 keeps that generated/dist boundary and adds package-verification
schema revalidation to the same existing-package evidence verifier without
regenerating package outputs.
Gate 126 closes the current MCM Extender generation/package lane without
changing generated/dist boundaries or verifier behavior. The next
implementation lane should improve capability and environment reports before
adding more game-facing generators.
Gate 166 adds a deterministic redacted Doctor handoff ZIP sidecar outside the
generated/dist package graph. It is local Doctor evidence, not a release
package, FOMOD archive, or generated game payload.
Gate 168 extends that local Doctor evidence archive with per-requirement
explanation Markdown entries for unavailable project capability requirements.
It remains outside the generated/dist package graph and is not a release
package, FOMOD archive, or generated game payload.
Gate 169 adds matching per-requirement explanation JSON entries to that local
Doctor evidence archive. It remains outside the generated/dist package graph
and is not a release package, FOMOD archive, or generated game payload.
Gate 170 adds requirement explanation index JSON and Markdown entries to that
local Doctor evidence archive. It remains outside the generated/dist package
graph and is not a release package, FOMOD archive, or generated game payload.
Gate 171 adds a README entry to that local Doctor evidence archive. It
remains outside the generated/dist package graph and is not a release
package, FOMOD archive, or generated game payload.
Gate 172 adds diagnostic index entries to that local Doctor evidence archive.
They remain outside the generated/dist package graph and are not a release
package, FOMOD archive, generated game payload, or SARIF/GitHub diagnostic
projection mode.
Gate 173 adds action index entries to that local Doctor evidence archive.
They remain outside the generated/dist package graph and are not a release
package, FOMOD archive, generated game payload, or SARIF/GitHub diagnostic
projection mode.
Gate 174 adds requirement index entries to that local Doctor evidence archive.
They remain outside the generated/dist package graph and are not a release
package, FOMOD archive, generated game payload, or SARIF/GitHub diagnostic
projection mode.
Gate 175 adds provider index entries to that local Doctor evidence archive.
They remain outside the generated/dist package graph and are not a release
package, FOMOD archive, generated game payload, or SARIF/GitHub diagnostic
projection mode.
Gate 176 adds capability index entries to that local Doctor evidence archive.
They remain outside the generated/dist package graph and are not a release
package, FOMOD archive, generated game payload, or SARIF/GitHub diagnostic
projection mode.
Gate 177 adds Doctor area index entries to that local Doctor evidence
archive. They remain outside the generated/dist package graph and are not a
release package, FOMOD archive, generated game payload, or SARIF/GitHub
diagnostic projection mode.
Gate 178 adds catalogue-policy index entries to that local Doctor evidence
archive. They remain outside the generated/dist package graph and are not a
release package, FOMOD archive, generated game payload, or SARIF/GitHub
diagnostic projection mode.
Gate 179 adds summary index entries to that local Doctor evidence archive.
They remain outside the generated/dist package graph and are not a release
package, FOMOD archive, generated game payload, or SARIF/GitHub diagnostic
projection mode.
Gate 180 adds provider evidence index entries to that local Doctor evidence
archive. They remain outside the generated/dist package graph and are not a
release package, FOMOD archive, generated game payload, or SARIF/GitHub
diagnostic projection mode.
Gate 181 adds redaction index entries to that local Doctor evidence archive.
They remain outside the generated/dist package graph and are not a release
package, FOMOD archive, generated game payload, or SARIF/GitHub diagnostic
projection mode.
Gate 182 adds open-question index entries to that local Doctor evidence
archive. They remain outside the generated/dist package graph and are not a
release package, FOMOD archive, generated game payload, or SARIF/GitHub
diagnostic projection mode.
Gate 183 adds scan-input index entries to that local Doctor evidence archive.
They remain outside the generated/dist package graph and are not a release
package, FOMOD archive, generated game payload, or SARIF/GitHub diagnostic
projection mode.
Gate 184 adds bundle navigation index entries to that local Doctor evidence
archive. They remain outside the generated/dist package graph and are not a
release package, FOMOD archive, generated game payload, or SARIF/GitHub
diagnostic projection mode.
Gate 185 adds triage index entries to that local Doctor evidence archive.
They remain outside the generated/dist package graph and are not a release
package, FOMOD archive, generated game payload, or SARIF/GitHub diagnostic
projection mode.
Gate 186 adds primary Doctor export triage projection beside the existing
Doctor summary and index output. It remains outside the generated/dist package
graph and is not a release package, FOMOD archive, generated game payload, or
SARIF/GitHub diagnostic projection mode.
Gate 187 adds Doctor triage command hints beside the existing Doctor summary,
index, and triage output. It remains outside the generated/dist package graph
and is not a release package, FOMOD archive, generated game payload, or
SARIF/GitHub diagnostic projection mode.
Gate 188 adds Doctor triage worklist projection beside the existing Doctor
summary, index, triage, and command-hint output. It remains outside the
generated/dist package graph and is not a release package, FOMOD archive,
generated game payload, or SARIF/GitHub diagnostic projection mode.
Gate 189 adds Doctor worklist summary metadata beside the existing Doctor
summary, index, triage, command-hint, and worklist output. It remains outside
the generated/dist package graph and is not a release package, FOMOD archive,
generated game payload, or SARIF/GitHub diagnostic projection mode.
Gate 190 adds a Doctor remediation status header beside the existing Doctor
summary, index, triage, command-hint, worklist, and worklist-summary output.
It remains outside the generated/dist package graph and is not a release
package, FOMOD archive, generated game payload, or SARIF/GitHub diagnostic
projection mode.
Gate 191 adds human operator handoff checklist sections beside the existing
Doctor plain text, Markdown summary, and bundle triage Markdown output. It
remains outside the generated/dist package graph and is not a release package,
FOMOD archive, generated game payload, or SARIF/GitHub diagnostic projection
mode.
Gate 192 adds `handoff-summary.md` beside the existing Doctor bundle reports
and indexes. It remains outside the generated/dist package graph and is not a
release package, FOMOD archive, generated game payload, or SARIF/GitHub
diagnostic projection mode.
Gate 193 adds scan-side operator handoff text beside existing capability scan
plain and Markdown summary output. It remains outside the generated/dist
package graph and is not a release package, FOMOD archive, generated game
payload, or SARIF/GitHub diagnostic projection mode.
Gate 194 adds explain-side operator handoff text beside existing capability
explain plain and Markdown summary output. It remains outside the
generated/dist package graph and is not a release package, FOMOD archive,
generated game payload, or SARIF/GitHub diagnostic projection mode.
Gate 195 adds generated Doctor bundle documentation and archive test coverage
for requirement explanation Markdown handoff sections. It remains outside the
generated/dist package graph and is not a release package, FOMOD archive,
generated game payload, or SARIF/GitHub diagnostic projection mode.
Gate 196 adds provider-version declaration metadata to catalogue and
explanation views. It remains outside the generated/dist package graph and is
not a resolved provider version, build-graph key, release package, FOMOD
archive, generated game payload, or SARIF/GitHub diagnostic projection mode.
Gate 197 exposes the same declaration-only provider-version metadata in scan
and Doctor provider index views. It remains outside the generated/dist package
graph and is not a resolved provider version, build-graph key, release
package, FOMOD archive, generated game payload, or SARIF/GitHub diagnostic
projection mode.
Gate 202 records the JIP LN text-script generator evidence checkpoint. It
defines the future generator's safety boundary around explicit lifecycle
prefixes, size budgeting, provider requirements, FormID-resolution strategy,
generated/dist output roots, and provenance, without adding source contracts,
schemas, generator code, CLI target wiring, package staging, runtime probes,
GECK automation, MO2 VFS inspection, live Data mutation, external tool
execution, or generated game payloads.

Gate 203 adds the source contract skeleton for that future generator. It
does not add script text emission, generated/dist package staging, build graph
targets, CLI target wiring, runtime probes, GECK automation, MO2 VFS
inspection, live Data mutation, external tool execution, or generated game
payloads.

Gate 204 adds semantic checks over the JIP source contract. It still does not
add script text emission, generated/dist package staging, build graph targets,
CLI target wiring, runtime probes, GECK automation, MO2 VFS inspection, live
Data mutation, external tool execution, or generated game payloads.

Gate 205 adds opaque body/source-line records to the JIP source contract. It
still does not add script text emission, generated/dist package staging, build
graph targets, CLI target wiring, runtime probes, GECK automation, MO2 VFS
inspection, live Data mutation, external tool execution, or generated game
payloads.

Gate 206 adds source-line byte-budget validation before generation. It still
does not add script text emission, generated/dist package staging, build graph
targets, CLI target wiring, runtime probes, GECK automation, MO2 VFS
inspection, live Data mutation, external tool execution, or generated game
payloads.

Gate 207 adds duplicate output filename validation before generation. It still
does not add script text emission, generated/dist package staging, build graph
targets, CLI target wiring, runtime probes, GECK automation, MO2 VFS
inspection, live Data mutation, external tool execution, or generated game
payloads.

Gate 208 adds non-emitting generation planning before text rendering. The
planner records future generated paths, Data paths, install paths, byte
budgets, capability requirements, FormID-resolution strategy, and source
locations. It still does not add script text emission, generated/dist package
staging, build graph targets, CLI target wiring, runtime probes, GECK
automation, MO2 VFS inspection, live Data mutation, external tool execution,
or generated game payloads.

Gate 209 adds in-memory text rendering before generated-file emission. The
renderer joins validated opaque source lines with LF separators and records
UTF-8 byte counts while preserving plan metadata. It still does not add
generated/dist package staging, build graph targets, CLI target wiring,
runtime probes, GECK automation, MO2 VFS inspection, live Data mutation,
external tool execution, or generated game payloads.

Gate 210 adds generated-file emission under `generated/jip-scripts` only. It
writes rendered JIP text documents to Forge-owned generated output paths and
keeps `Data/nvse/plugins/scripts` as install metadata. It still does not add
generated/dist package staging, build graph targets, CLI target wiring,
runtime probes, GECK automation, MO2 VFS inspection, live Data mutation,
external tool execution, or release-grade generated evidence.

Gate 211 adds local emission evidence beside those generated JIP text files:
`jip-script-emission-manifest.json`, `checksums.sha256`, and output digest
records under `generated/jip-scripts`. The evidence remains generated and
non-authoritative, and the checksum file is deliberately excluded from the
returned output digest list to avoid self-referential provenance. It still
does not add generated/dist package staging, build graph targets, CLI target
wiring, runtime probes, GECK automation, MO2 VFS inspection, live Data
mutation, external tool execution, or release-grade generated evidence.

Gate 212 validates that generated emission manifest against
`jip-script-emission-manifest/0.1.0` before checksum evidence is written. It
still does not add generated/dist package staging, build graph targets, CLI
target wiring, runtime probes, GECK automation, MO2 VFS inspection, live Data
mutation, external tool execution, or release-grade generated evidence.

Gate 213 revalidates generated JIP emission checksum evidence against the
generated manifest and emitted files under `generated/jip-scripts`. It still
does not add generated/dist package staging, build graph targets, CLI target
wiring, runtime probes, GECK automation, MO2 VFS inspection, live Data
mutation, external tool execution, or release-grade generated evidence.

Gate 214 wires the existing JIP generated-root emitter into
`forge generate --target jip-scripts`. It produces generated script payloads,
emission manifest, checksum sidecar, CLI JSON/text output, and output digest
evidence under `generated/jip-scripts`; it still does not add build graph
targets, dist output, package staging, runtime probes, GECK automation, MO2
VFS inspection, live Data mutation, external tool execution, or release-grade
generated evidence.

Gate 215 wires rendered JIP text scripts into
`forge build --target jip-scripts`. It writes dist output under
`dist/jip-scripts`, records a local `build-manifest.json`, writes
`checksums.sha256`, supports dry-run planning, and validates custom output
containment under `dist/`. It still does not add package staging, runtime
probes, GECK automation, MO2 VFS inspection, live Data mutation, external tool
execution, FOMOD generation, or archive generation.

Gate 216 wires rendered JIP text scripts into
`forge package --target jip-scripts`. It writes a loose-file package staging
tree under `dist/jip-scripts/package/Data/nvse/plugins/scripts`, records
`package-manifest.json`, `install-plan.json`, `build-manifest.json`, and
`checksums.sha256`, supports dry-run planning, and validates custom output
containment under `dist/`. It still does not run runtime probes, automate
GECK, inspect MO2 VFS, mutate live Data, execute external tools, create FOMOD
installers, create archives, or add JIP package verify-existing behavior.

Gate 217 closes the current JIP command lane and preserves the generated,
build, and package boundaries already implemented. The next safe integration
lane is xEdit audit and inspection support, starting with evidence contracts
and synthetic fixtures rather than xEdit execution, plugin patch generation,
or plugin mutation.

Gate 218 starts that xEdit lane with an evidence-only source contract and a
non-emitting adapter planner. It validates `xedit-audit/0.1.0` registries,
loads typed audit definitions, and plans future generated Pascal script and
JSON report paths under `generated/xedit-audit` without writing files,
executing xEdit, parsing real reports, generating patches, or mutating
plugins.

Gate 219 renders the first generated xEdit audit script scaffolds under
`generated/xedit-audit/scripts`. The scaffold files are generated text
artifacts with output digests only; no report file, plugin patch, xEdit
process, MO2/GECK automation, runtime probe, or plugin mutation is produced.

Gate 220 adds local manifest and checksum evidence for those generated xEdit
audit scaffolds. `xedit-audit-script-manifest.json` records scaffold intent,
expected report paths, safety flags, source pointers, required capabilities,
and payload digests; `checksums.sha256` covers the scaffold files and manifest
under `generated/xedit-audit`. It still does not expose a CLI target, execute
xEdit, parse reports, generate patches, or mutate plugins.

Gate 221 exposes the same local scaffold evidence through canonical
`forge generate --target xedit-audit`. The command writes generated scaffold,
manifest, and checksum evidence under `generated/xedit-audit` and reports the
same output digests in CLI JSON/text output. It still does not execute xEdit,
parse reports, generate patches, mutate plugins, automate MO2/GECK, run
runtime probes, use real third-party plugin fixtures, or implement
`forge build --target xedit-audit`.

Gate 222 adds the first xEdit audit report parser contract. The parser reads
synthetic JSON report fixtures from `generated/xedit-audit/reports`, returns
typed safety, summary, record, and finding evidence, and emits `WF-GEN-009`
for missing or invalid synthetic report evidence. It still does not execute
xEdit, generate reports, parse real xEdit output, generate patches, mutate
plugins, automate MO2/GECK, run runtime probes, or apply findings to plugins.

Gate 223 projects that parsed report evidence into in-memory machine JSON and
LF human text handoff content. The projection carries parser status, summary
counts, report records, findings, diagnostics, and explicit safety flags while
remaining non-emitting. It still does not execute xEdit, generate reports,
write handoff files, generate patches, mutate plugins, automate MO2/GECK, run
runtime probes, or apply findings to plugins.

Gate 224 emits those report handoff payloads as generated JSON and text files
under `generated/xedit-audit`. The files are UTF-8 without BOM, use LF line
endings, and return output digest records. It still does not execute xEdit,
generate reports, write handoff manifest/checksum sidecars, generate patches,
mutate plugins, automate MO2/GECK, run runtime probes, or apply findings to
plugins.

Gate 225 adds local handoff provenance sidecars for those report handoff
payloads. The manifest records safety flags, handoff summary metadata,
generated file metadata, payload digests, and limitations; the dedicated
handoff checksum file covers the JSON handoff, text handoff, and manifest
without overwriting xEdit scaffold checksum evidence. It still does not
execute xEdit, generate reports, revalidate sidecars, generate patches, mutate
plugins, automate MO2/GECK, run runtime probes, or apply findings to plugins.

Gate 226 adds local revalidation for those report handoff sidecars. The
verifier compares the manifest outputs, checksum rows, and generated files and
returns `WF-GEN-010` for handoff evidence drift. It still does not execute
xEdit, generate reports, generate patches, mutate plugins, automate MO2/GECK,
run runtime probes, or apply findings to plugins.

Gate 227 exposes the same report handoff evidence through canonical
`forge generate --target xedit-audit-report-handoff`. The command parses
existing synthetic report fixtures, writes handoff JSON/text, manifest, and
checksum evidence under `generated/xedit-audit`, and reports the same output
digests in CLI JSON/text output. It still does not execute xEdit, generate
reports, generate patches, mutate plugins, automate MO2/GECK, run runtime
probes, use real third-party plugin fixtures, or implement
`forge build --target xedit-audit`.

Gate 228 closes the current xEdit audit command lane and moves the next build
graph value back to deterministic metadata. The next lane should start
`forge docs` reference indexes for schemas, registries, rule families,
capabilities, and command references before any static site generator,
watch-mode behavior, network publishing, graph/explain/clean expansion, or
higher-risk xEdit/plugin work.

Gate 229 implements that first `forge docs` slice. It writes deterministic
reference index JSON/Markdown plus docs manifest and checksum evidence under
`generated/docs`, keeps custom docs output contained under `generated/`, and
still does not add a static site generator, watch mode, network publishing,
graph/explain/clean behavior, package/release behavior, xEdit execution,
plugin mutation, MO2/GECK automation, runtime probes, real third-party plugin
fixtures, or AI behavior.

Gate 230 expands the same `forge docs` build graph lane with deterministic
per-schema reference page skeletons under
`generated/docs/schemas/<kind>/<version>/`. The docs manifest and checksum
sidecar now cover those pages, preserving disposable rebuildable output and
provenance while still avoiding static site generation, watch mode, network
publishing, graph/explain/clean behavior, package/release behavior, xEdit
execution, plugin mutation, MO2/GECK automation, runtime probes, real
third-party plugin fixtures, and AI behavior.

Gate 231 expands the same `forge docs` build graph lane with deterministic
project registry reference page skeletons under
`generated/docs/registries/<registry-path>/`. The docs manifest and checksum
sidecar now cover those pages, preserving disposable rebuildable output and
provenance while still avoiding static site generation, watch mode, network
publishing, graph/explain/clean behavior, package/release behavior, xEdit
execution, plugin mutation, MO2/GECK automation, runtime probes, real
third-party plugin fixtures, and AI behavior.

Gate 232 expands the same `forge docs` build graph lane with deterministic
validation rule reference page skeletons under
`generated/docs/rules/<rule-family>/`. The docs manifest and checksum sidecar
now cover those pages, preserving disposable rebuildable output and provenance
while still avoiding static site generation, watch mode, network publishing,
graph/explain/clean behavior, package/release behavior, xEdit execution,
plugin mutation, MO2/GECK automation, runtime probes, real third-party plugin
fixtures, and AI behavior.

Gate 233 expands the same `forge docs` build graph lane with deterministic
built-in capability reference page skeletons under
`generated/docs/capabilities/<capability-id>/`. The docs manifest and checksum
sidecar now cover those pages, preserving disposable rebuildable output and
provenance while still avoiding provider reference pages, capability
scan/explain behavior, provider detection changes, provider-version parsing
changes, static site generation, watch mode, network publishing,
graph/explain/clean behavior, package/release behavior, xEdit execution,
plugin mutation, MO2/GECK automation, runtime probes, real third-party plugin
fixtures, and AI behavior.

Gate 234 expands the same `forge docs` build graph lane with deterministic
built-in provider reference page skeletons under
`generated/docs/providers/<provider-id>/`. The docs manifest and checksum
sidecar now cover those pages, preserving disposable rebuildable output and
provenance while still avoiding capability scan/explain behavior, provider
detection changes, provider-version parsing changes, static site generation,
watch mode, network publishing, graph/explain/clean behavior, package/release
behavior, xEdit execution, plugin mutation, MO2/GECK automation, runtime
probes, real third-party plugin fixtures, and AI behavior.

Gate 235 expands the same `forge docs` build graph lane with deterministic
canonical command reference page skeletons under
`generated/docs/commands/<command-path>/`. The docs manifest and checksum
sidecar now cover those pages, preserving disposable rebuildable output and
provenance while still avoiding command behavior changes, command aliases,
static site generation, watch mode, network publishing, graph/explain/clean
behavior, package/release behavior, xEdit execution, plugin mutation,
MO2/GECK automation, runtime probes, real third-party plugin fixtures, and AI
behavior.

Gate 236 starts the `forge graph` command lane with deterministic project
source graph evidence under `generated/graph/`. The graph manifest and
checksum sidecar cover the project source graph JSON/Markdown outputs,
preserving disposable rebuildable output and provenance while still avoiding
graph visualization formats, build planning changes, package/release behavior,
xEdit execution, plugin mutation, MO2/GECK automation, runtime probes, real
third-party plugin fixtures, and AI behavior.

Gate 237 expands that graph evidence with declaration-only capability
requirement, built-in capability, and built-in provider nodes. The graph
manifest and checksum sidecar cover the expanded JSON/Markdown outputs while
still avoiding capability scans, provider status resolution, graph
visualization formats, build planning changes, package/release behavior, xEdit
execution, plugin mutation, MO2/GECK automation, runtime probes, real
third-party plugin fixtures, and AI behavior.

Gate 238 expands that graph evidence with declaration-only generator target
nodes and source/output boundary links. The graph manifest and checksum
sidecar cover the expanded JSON/Markdown outputs while still avoiding
generator execution, build planning changes, capability scans, provider status
resolution, graph visualization formats, package/release behavior, xEdit
execution, plugin mutation, MO2/GECK automation, runtime probes, real
third-party plugin fixtures, and AI behavior.

Gate 239 expands that graph evidence with declaration-only generated artifact
expectation nodes and generated/dist boundary links. The graph manifest and
checksum sidecar cover the expanded JSON/Markdown outputs while still avoiding
artifact existence checks, generator execution, build planning changes,
capability scans, provider status resolution, graph visualization formats,
package/release behavior, xEdit execution, plugin mutation, MO2/GECK
automation, runtime probes, real third-party plugin fixtures, and AI behavior.

Gate 240 expands that graph evidence with declaration-only manifest provenance
reference nodes and generated/dist boundary links. The graph manifest and
checksum sidecar cover the expanded JSON/Markdown outputs while still avoiding
generated manifest reads, artifact existence checks, generator execution,
build planning changes, capability scans, provider status resolution, graph
visualization formats, package/release behavior, xEdit execution, plugin
mutation, MO2/GECK automation, runtime probes, real third-party plugin
fixtures, and AI behavior.

Gate 241 closes the current graph metadata lane without changing generated
graph output or build graph behavior. The next lane moves to top-level
`forge explain` subject planning before any generated manifest reads, artifact
existence checks, generator execution, build planning changes, graph
visualization formats, package/release behavior, external tool execution, or
AI behavior.

Gate 242 starts that top-level `forge explain` lane as a subject contract and
reserved status skeleton only. It documents diagnostic, target, output,
capability, and provenance subjects without reading generated manifests,
checking artifact existence, reading provenance sidecars, changing the build
graph, executing generators, resolving providers, changing package/release
behavior, executing external tools, or using AI.

Gate 243 implements `forge explain diagnostic <rule-id>` as a family-level
diagnostic explanation skeleton. It does not read generated manifests, inspect
artifacts, read provenance sidecars, change build planning, execute
generators, resolve providers, change package/release behavior, execute
external tools, or use AI.

Gate 244 extends the diagnostic explanation skeleton with deterministic
documented rule metadata for concrete rule IDs. It still does not read
generated manifests, inspect artifacts, read provenance sidecars, change build
planning, execute generators, resolve providers, change package/release
behavior, execute external tools, or use AI.

Gate 245 implements `forge explain target <target-id>` as deterministic
target metadata only. It explains command surfaces, output roots, primary
outputs, capabilities, related rules, and boundaries without reading generated
manifests, inspecting artifacts, reading provenance sidecars, changing build
planning, executing generators, executing package/release behavior, resolving
providers, executing external tools, or using AI.

Gate 246 implements `forge explain output <generated-or-dist-path>` as
deterministic output path classification only. It explains documented output
patterns, output kinds, boundaries, targets, rebuild commands, provenance
expectations, and related rules without reading generated manifests,
inspecting artifacts, reading provenance sidecars, changing build planning,
executing generators, executing package/release behavior, resolving providers,
executing external tools, or using AI.

Gate 247 implements `forge explain capability <capability-id>` as deterministic
built-in capability catalogue metadata only. It explains capability metadata,
satisfying providers, detector kinds, provider version declaration metadata,
related `WF-CAP-*` rules, recovery commands, and boundaries without reading
generated manifests, inspecting artifacts, reading provenance sidecars,
changing build planning, executing generators, executing package/release
behavior, resolving providers, running capability scans, executing external
tools, running runtime probes, or using AI.

Gate 248 implements `forge explain provenance <manifest-or-output-path>` as
deterministic provenance boundary planning only. It explains evidence roles,
targets, rebuild commands, expected trace plans, related rules, and boundaries
without reading generated manifests, build manifests, checksums, provenance
sidecars, inspecting artifacts, changing build planning, executing generators,
executing package/release behavior, resolving providers, running capability
scans, executing external tools, running runtime probes, or using AI.

Gate 249 closes the top-level `forge explain` command slice and routes the
next implementation lane to `forge clean` planning only. It does not add delete
behavior, filesystem mutation, generated manifest reads, build manifest reads,
provenance sidecar reads, checksum reads, artifact existence checks, build
planning changes, generator execution, package/release behavior, provider
resolution, capability scan behavior changes, external tool execution, runtime
probes, or AI behavior.

Gate 250 implements `forge clean` scope planning only. It exposes documented
clean scopes for `generated/`, `dist/`, `.wastelandforge/cache/`, and `all`,
and emits reserved JSON with false execution flags. It does not delete files,
mutate the filesystem, read generated manifests, read build manifests, read
provenance sidecars, read checksums, inspect artifact existence, change build
planning, execute generators, execute package/release behavior, resolve
providers, change capability scan behavior, execute external tools, run runtime
probes, or use AI.

Gate 251 implements `forge clean` dry-run path planning only. It calculates
contained project roots for `generated`, `dist`, `cache`, and `all` scopes and
reports those roots without reading generated manifests, build manifests,
provenance sidecars, checksums, artifacts, or provider evidence. It does not
delete files, mutate the filesystem, change build planning, execute
generators, execute package/release behavior, resolve providers, change
capability scan behavior, execute external tools, run runtime probes, or use
AI.

Gate 252 implements `forge clean --all` confirmation/refusal planning only.
Unconfirmed all-scope plans return exit code 6 while still reporting the
generated, dist, and cache clean roots. Confirmed all-scope plans still remain
dry-run-only. It does not delete files, mutate the filesystem, read project
manifests, validate project IDs, read generated manifests, read build
manifests, read provenance sidecars, read checksums, inspect artifacts, change
build planning, execute generators, execute package/release behavior, resolve
providers, change capability scan behavior, execute external tools, run runtime
probes, or use AI.

Gate 253 implements explicit `forge clean --generated` execution. It deletes
only the contained project `generated/` output root and reports removed or
missing paths. Omitted-scope generated cleans, `--generated --dry-run`,
`--dist`, `--cache`, and confirmed `--all` remain non-mutating path plans. It
does not delete `dist` or cache outputs, perform all-scope deletion, read
project manifests, validate project IDs, read generated manifests, read build
manifests, read provenance sidecars, read checksums, inspect artifacts beyond
the selected target root, change build planning, execute generators, execute
package/release behavior, resolve providers, change capability scan behavior,
execute external tools, run runtime probes, or use AI.

Gate 254 implements explicit `forge clean --dist` execution. It deletes only
the contained project `dist/` output root and reports removed or missing paths.
Generated clean behavior remains unchanged, while `--dist --dry-run`,
omitted-scope generated cleans, `--cache`, and confirmed `--all` remain
non-mutating path plans. It does not delete cache outputs, perform all-scope
deletion, detect active builds or cache locks, read project manifests, validate
project IDs, read generated manifests, read build manifests, read provenance
sidecars, read checksums, inspect artifacts beyond the selected target root,
change build planning, execute generators, execute package/release behavior,
resolve providers, change capability scan behavior, execute external tools,
run runtime probes, or use AI.

Gate 255 implements explicit `forge clean --cache` execution. It deletes only
the contained project `.wastelandforge/cache/` output root and reports removed
or missing paths. Generated and dist clean behavior remain unchanged, while
`--cache --dry-run`, omitted-scope generated cleans, and confirmed `--all`
remain non-mutating path plans. It does not perform all-scope deletion, detect
active builds or cache locks, read project manifests, validate project IDs,
read generated manifests, read build manifests, read provenance sidecars, read
checksums, inspect artifacts beyond the selected target root, change build
planning, execute generators, execute package/release behavior, resolve
providers, change capability scan behavior, execute external tools, run
runtime probes, or use AI.

Gate 256 implements confirmed `forge clean --all` execution. It deletes only
the contained project `generated/`, `dist/`, and `.wastelandforge/cache/`
output roots when both `--yes` and `--confirm <project-id>` are supplied and
reports removed or missing paths per root. Selected-root clean behavior
remains unchanged, while explicit dry-runs, omitted-scope generated cleans,
and unconfirmed `--all` remain non-mutating path plans or refusals. It does
not validate project IDs, detect active builds or cache locks, read project
manifests, read generated manifests, read build manifests, read provenance
sidecars, read checksums, inspect artifacts beyond the target roots, change
build planning, execute generators, execute package/release behavior, resolve
providers, change capability scan behavior, execute external tools, run
runtime probes, or use AI.

Gate 257 implements all-scope project-ID confirmation validation before clean
mutation. Confirmed `forge clean --all` reads only the root project manifest
identity and requires `--confirm <project-id>` to match top-level manifest
`id` before deleting contained generated, dist, and cache roots. Missing,
multiple, unreadable, invalid, or mismatched manifest identity refuses without
deletion and reports machine-readable identity metadata. It does not perform
full manifest schema validation, load registries, detect active builds or
cache locks, read generated manifests, read build manifests, read provenance
sidecars, read checksums, inspect artifacts beyond the target roots, change
build planning, execute generators, execute package/release behavior, resolve
providers, change capability scan behavior, execute external tools, run
runtime probes, or use AI.

Gate 258 implements active build/cache lock safety before cache-affecting
clean mutation. `.wastelandforge/cache/build.lock` is the local lock marker.
Explicit `forge clean --cache` and manifest-confirmed `forge clean --all`
refuse without deletion when the marker exists and report machine-readable
`cacheLock` metadata. It does not inspect processes, expire stale locks, read
generated manifests, read build manifests, read provenance sidecars, read
checksums, inspect artifacts beyond the target roots and lock marker, change
build planning, execute generators, execute package/release behavior, resolve
providers, change capability scan behavior, execute external tools, run
runtime probes, or use AI.

Gate 259 closes the current `forge clean` command lane without adding runtime
behavior. The clean slice is parked after contained-root deletion, all-scope
project-ID confirmation, and active build/cache lock refusal. Deferred clean
backlog items include generated manifest reads, build manifest reads,
provenance sidecar reads, checksum reads, process inspection, stale lock
expiry, lock ownership metadata, artifact checks beyond target roots and the
lock marker, generated-output provenance validation before deletion, and
build/generate lock creation. The next lane is `forge release prepare`
planning only and stops before archive creation, release publishing, remote
repository calls, attestation/signing, external tool execution, plugin
mutation, MO2 automation, GECK automation, runtime probes, real third-party
plugin fixtures, or AI behavior.

Gate 260 starts the `forge release prepare` lane as planning metadata only.
The command reports default planned output root `dist/release-prepare`,
planned release-preparation evidence paths, output containment under project
`dist/`, a report contract, and false execution flags. It refuses planned
output roots outside `dist/` without writing files. It does not emit
release-plan files, release summaries, build manifests, checksums, release
archives, deterministic ZIP/FOMOD packages, remote release calls,
attestations, signatures, external tool execution, plugin mutation, MO2
automation, GECK automation, runtime probes, real third-party plugin fixtures,
or AI behavior.

Gate 261 emits the first release-prepare evidence file:
`dist/release-prepare/release-plan.json`. The plan records release-prepare
planned outputs, output root, execution flags, and boundaries. `--dry-run`
preserves the Gate 260 no-write path, and output roots outside project `dist/`
remain refused. Gate 261 does not emit release summaries, build manifests,
checksums, staging payloads, archives, deterministic ZIP/FOMOD packages,
remote release calls, attestations, signatures, external tool execution,
plugin mutation, MO2 automation, GECK automation, runtime probes, real
third-party plugin fixtures, or AI behavior.

Gate 262 emits the second release-prepare evidence file:
`dist/release-prepare/release-summary.json`. Normal `forge release prepare`
execution now writes both the release plan and release summary under
`dist/release-prepare`, while `--dry-run` remains no-write planning and output
roots outside project `dist/` remain refused. Gate 262 does not emit build
manifests, checksums, staging payloads, archives, deterministic ZIP/FOMOD
packages, remote release calls, attestations, signatures, external tool
execution, plugin mutation, MO2 automation, GECK automation, runtime probes,
real third-party plugin fixtures, or AI behavior.

Gate 263 emits local release-prepare build-manifest evidence:
`dist/release-prepare/build-manifest.json`. Normal `forge release prepare`
execution now writes the release plan, release summary, and build manifest
under `dist/release-prepare`; the build manifest records digests for the plan
and summary only. `--dry-run` remains no-write planning and output roots
outside project `dist/` remain refused. Gate 263 does not emit checksums,
staging payloads, archives, deterministic ZIP/FOMOD packages, remote release
calls, attestations, signatures, external tool execution, plugin mutation, MO2
automation, GECK automation, runtime probes, real third-party plugin fixtures,
or AI behavior.

Gate 264 emits local release-prepare checksum evidence:
`dist/release-prepare/checksums.sha256`. Normal `forge release prepare`
execution now writes the release plan, release summary, build manifest, and
checksum sidecar under `dist/release-prepare`; the checksum sidecar covers the
plan, summary, and build manifest, but not itself. `--dry-run` remains
no-write planning and output roots outside project `dist/` remain refused.
Gate 264 does not emit staging payloads, archives, deterministic ZIP/FOMOD
packages, remote release calls, attestations, signatures, external tool
execution, plugin mutation, MO2 automation, GECK automation, runtime probes,
real third-party plugin fixtures, or AI behavior.

Gate 265 emits a local release-prepare staging payload skeleton:
`dist/release-prepare/staging/release-payload.json`. Normal
`forge release prepare` execution now writes the staging payload skeleton,
release plan, release summary, build manifest, and checksum sidecar under
`dist/release-prepare`; the build manifest and checksum sidecar include the
staging payload evidence. `--dry-run` remains no-write planning and output
roots outside project `dist/` remain refused. Gate 265 does not emit release
archives, deterministic ZIP/FOMOD packages, installer metadata, real mod
payload staging, remote release calls, attestations, signatures, external tool
execution, plugin mutation, MO2 automation, GECK automation, runtime probes,
real third-party plugin fixtures, or AI behavior.

Gate 266 emits local release-prepare archive planning metadata:
`dist/release-prepare/release-archive-plan.json`. Normal
`forge release prepare` execution now writes the archive plan, staging payload
skeleton, release plan, release summary, build manifest, and checksum sidecar
under `dist/release-prepare`; the build manifest and checksum sidecar include
the archive plan evidence. The archive plan records
`dist/release-prepare/archives/release.zip` as planned-not-created metadata
only. `--dry-run` remains no-write planning and output roots outside project
`dist/` remain refused. Gate 266 does not create release archives,
deterministic ZIP/FOMOD packages, installer metadata, real mod payload
staging, remote release calls, attestations, signatures, external tool
execution, plugin mutation, MO2 automation, GECK automation, runtime probes,
real third-party plugin fixtures, or AI behavior.

Gate 267 emits a deterministic local release archive skeleton:
`dist/release-prepare/archives/release.zip`. Normal `forge release prepare`
execution now writes the archive, archive plan, staging payload skeleton,
release plan, release summary, build manifest, and checksum sidecar under
`dist/release-prepare`; the build manifest and checksum sidecar include the
archive digest evidence. The ZIP contains local release-prepare evidence only,
uses ordinal entry ordering, stored entries, and deterministic ZIP-compatible
timestamps. `--dry-run` remains no-write planning and output roots outside
project `dist/` remain refused. Gate 267 does not assemble FOMOD installers,
generate real mod payload staging, publish releases, call remote repositories,
sign or attest artifacts, execute external tools, mutate plugins, automate
MO2 or GECK, run runtime probes, use real third-party plugin fixtures, or use
AI.

Gate 268 emits a local release archive evidence sidecar:
`dist/release-prepare/release-archive-evidence.json`. Normal
`forge release prepare` execution now writes the archive evidence sidecar
beside the archive, archive plan, staging payload skeleton, release plan,
release summary, build manifest, and checksum sidecar under
`dist/release-prepare`; the build manifest and checksum sidecar include the
archive evidence digest. The sidecar revalidates archive SHA-256, length,
expected and actual entry names, ordinal entry ordering, stored compression,
and deterministic ZIP timestamp metadata. `--dry-run` remains no-write
planning and output roots outside project `dist/` remain refused. Gate 268
does not assemble FOMOD installers, generate real mod payload staging, publish
releases, call remote repositories, sign or attest artifacts, execute external
tools, mutate plugins, automate MO2 or GECK, run runtime probes, use real
third-party plugin fixtures, or use AI.

Gate 269 closes the current `forge release prepare` lane. Gates 260 through
268 are treated as the complete current release-prepare slice for planning,
release evidence, local build manifests, checksums, deterministic archive
metadata, and archive-evidence revalidation. Deferred release-prepare work
includes real payload staging, FOMOD installer assembly, pre-existing artifact
reads, provenance sidecar reads, checksum sidecar reads, release notes or
changelog synthesis, SemVer enforcement beyond skeleton metadata,
signing/attestation material, external tools, MO2/GECK automation, runtime
probes, real third-party plugin fixtures, and AI-assisted release drafting.
Gate 269 adds no runtime command behavior and routes the next release lane to
a no-publish `forge release publish` governance preflight skeleton.

Gate 270 implements the first `forge release publish` lane as a no-publish
governance preflight. The command reports required local evidence, governance
checks, explicit human approval requirements, and false execution flags for
publication, remote repository calls, release uploads, signing/attestation,
external tools, plugin mutation, MO2/GECK automation, runtime probes, and AI.
It does not read release artifacts yet, write publish outputs, or publish a
release.

Gate 271 extends the same no-publish `forge release publish` preflight with
local release-prepare evidence path discovery. It reports expected, present,
and missing artifact counts plus per-artifact `missing` or
`present-not-validated` status for release payload, archive plan, archive,
archive evidence, release plan, release summary, build manifest, and checksum
sidecar paths. It does not parse artifact contents, revalidate checksums,
reopen archives, write publish outputs, call remote repositories, upload
assets, sign or attest artifacts, execute external tools, run runtime probes,
or use AI.

Gate 272 extends the release-publish preflight with local evidence
content-shape classification. JSON evidence files are parsed only far enough
to confirm a JSON object root; `checksums.sha256` is parsed only far enough to
confirm line shape. The release archive remains binary-deferred as
`present-not-classified`. The gate reports well-formed, malformed, and
unclassified counts without semantic validation, checksum digest revalidation,
archive revalidation, publish outputs, remote repository calls, release
uploads, signing/attestation, external tools, runtime probes, or AI.

Gate 273 extends the release-publish preflight with checksum sidecar entry
classification. `checksums.sha256` entries are parsed for expected-path
coverage, unexpected paths, duplicate paths, and malformed entry shape only.
The build graph still does not recompute digests, open referenced files,
semantically accept release evidence, revalidate archives, publish outputs,
call remote repositories, upload assets, sign or attest artifacts, execute
external tools, run runtime probes, or use AI.

Gate 274 extends the release-publish preflight with build-manifest output
cross-reference. `build-manifest.json` output paths are parsed and compared to
expected release-prepare paths, local evidence paths, and checksum sidecar
paths only. The build graph still does not recompute digests, open referenced
outputs, semantically accept release evidence, revalidate archives, publish
outputs, call remote repositories, upload assets, sign or attest artifacts,
execute external tools, run runtime probes, or use AI.

Gate 275 extends the release-publish preflight with release-archive-evidence
metadata cross-reference. `release-archive-evidence.json` output paths are
parsed and compared to expected release-prepare paths, local evidence paths,
checksum sidecar paths, and build-manifest outputs only. The build graph still
does not recompute digests, open referenced outputs, reopen archives,
semantically accept release evidence, revalidate archives, publish outputs,
call remote repositories, upload assets, sign or attest artifacts, execute
external tools, run runtime probes, or use AI.

Gate 295 starts the reports package path as deterministic local package
planning evidence. `forge package --target reports` writes
`dist/reports-package/package-plan.json`,
`dist/reports-package/staging/package-layout.json`, a local
`build-manifest.json`, and `checksums.sha256`. It plans around the accepted
`dist/build` reports evidence set but does not copy inputs, read existing
build manifests, revalidate checksum digests, create archives, publish
outputs, call remote repositories, upload assets, sign or attest artifacts,
execute external tools, run runtime probes, automate MO2 or GECK, mutate
plugins, or use AI.

Gate 296 extends the reports package path with existence-only input
discovery. `forge package --target reports` classifies each expected
`dist/build` input as `present-not-copied` or `missing` and records
present/missing counts in the package plan, staging layout, local build
manifest, and CLI JSON. The build graph still does not read package input
contents, parse existing build manifests, copy inputs, revalidate checksum
digests, create archives, publish outputs, call remote repositories, upload
assets, sign or attest artifacts, execute external tools, run runtime probes,
automate MO2 or GECK, mutate plugins, or use AI.

Gate 297 extends the reports package path with staging-copy behavior for
present reports build evidence. `forge package --target reports` copies only
expected present `dist/build` inputs into
`dist/reports-package/staging/reports`, records `staged` and `stageStatus`
per entry, includes staged payloads in the package build manifest and
checksum sidecar, and skips missing expected inputs. The build graph still
does not parse existing build manifests, revalidate checksum digests, create
archives, publish outputs, call remote repositories, upload assets, sign or
attest artifacts, execute external tools, run runtime probes, automate MO2 or
GECK, mutate plugins, or use AI.

Gate 298 extends the reports package path with deterministic local archive
creation. `forge package --target reports` writes
`dist/reports-package/package.zip` with sorted stored ZIP entries and
deterministic timestamps. The archive contains package plan/layout evidence
and staged report payload entries under `reports/*`; the package build
manifest and checksum sidecar cover `package.zip`. The build graph still does
not emit archive evidence sidecars, revalidate archive digests or entries,
publish outputs, call remote repositories, upload assets, sign or attest
artifacts, execute external tools, run runtime probes, automate MO2 or GECK,
mutate plugins, or use AI.

Gate 299 extends the reports package path with local archive evidence
revalidation. `forge package --target reports` reopens
`dist/reports-package/package.zip` and writes
`dist/reports-package/package-archive-evidence.json` with the recomputed
archive digest, expected and actual ZIP entry names, entry ordering status,
stored compression status, and deterministic timestamp metadata. The package
build manifest and checksum sidecar cover the archive evidence sidecar. The
build graph still does not implement `reports` verify-existing behavior,
publish outputs, call remote repositories, upload assets, sign or attest
artifacts, execute external tools, run runtime probes, automate MO2 or GECK,
mutate plugins, or use AI.

Gate 300 closes the current reports package lane after package planning,
input discovery, staging copy, deterministic archive creation, archive
evidence revalidation, local build-manifest coverage, and checksum coverage
are in place. The next build-graph route moves to local `forge release verify`
self-report evidence under `dist/release-dry-run/release-verify.json` rather
than deeper reports package edge cases. Runtime build behavior is unchanged,
and the build graph still does not implement `reports` verify-existing
behavior, publish outputs, call remote repositories, upload assets, sign or
attest artifacts, execute external tools, run runtime probes, automate MO2 or
GECK, mutate plugins, or use AI.

Gate 301 adds that local `forge release verify` self-report to the release
dry-run graph. `release-verify.json` is emitted before build-manifest and
checksum sidecars, so `dist/release-dry-run/build-manifest.json` and
`dist/release-dry-run/checksums.sha256` cover the self-report. The graph still
does not run capability scans, run package verification, publish outputs, call
remote repositories, upload assets, sign or attest artifacts, execute external
tools, run runtime probes, automate MO2 or GECK, mutate plugins, or use AI.

Gate 302 adds `release-evidence-index.json` to the release dry-run graph. The
index records the local release-publish preflight evidence paths for schema
validation, capability/environment validation, package validation, and release
verification plus command hints for producing them. The file is emitted before
build-manifest and checksum sidecars, so it is covered as provenance evidence.
The graph still does not run command fan-out, capability scans, package
verification, publish outputs, call remote repositories, upload assets, sign
or attest artifacts, execute external tools, run runtime probes, automate MO2
or GECK, mutate plugins, or use AI.

Gate 303 adds `release-evidence-handoff.md` to the release dry-run graph. The
Markdown handoff renders the same evidence entries as the index for operator
use, including command hints, the release-publish dry-run hint, and disabled
execution boundaries. The file is emitted before build-manifest and checksum
sidecars, so it is covered as provenance evidence. The graph still does not
run command fan-out, capability scans, package verification, publish outputs,
call remote repositories, upload assets, sign or attest artifacts, execute
external tools, run runtime probes, automate MO2 or GECK, mutate plugins, or
use AI.

Gate 304 adds `release-evidence-status.json` to the release dry-run graph.
The status projection marks indexed release-publish preflight evidence paths
as present or missing from local file presence only, and the Markdown handoff
renders those statuses for operators. The file is emitted before
build-manifest and checksum sidecars, so it is covered as provenance
evidence. The graph still does not run command fan-out, capability scans,
package verification, evidence content validation, publish outputs, call
remote repositories, upload assets, sign or attest artifacts, execute
external tools, run runtime probes, automate MO2 or GECK, mutate plugins, or
use AI.

Gate 305 adds `release-evidence-actions.json` to the release dry-run graph.
The action checklist lists one manual action per missing indexed
release-publish preflight evidence path and links back to the evidence index,
status projection, and Markdown handoff. The file is emitted before
build-manifest and checksum sidecars, so it is covered as provenance
evidence. The graph still does not run command fan-out, capability scans,
package verification, evidence content validation, publish outputs, call
remote repositories, upload assets, sign or attest artifacts, execute
external tools, run runtime probes, automate MO2 or GECK, mutate plugins, or
use AI.

Gate 306 adds `release-evidence-collection-plan.json` to the release dry-run
graph. The collection plan orders the release-publish preflight evidence
steps, links missing manual steps to action IDs, and links back to the
evidence index, status projection, action checklist, and Markdown handoff.
The file is emitted before build-manifest and checksum sidecars, so it is
covered as provenance evidence. The graph still does not run command fan-out,
capability scans, package verification, evidence content validation, publish
outputs, call remote repositories, upload assets, sign or attest artifacts,
execute external tools, run runtime probes, automate MO2 or GECK, mutate
plugins, or use AI.

Gate 307 extends `forge release publish` to consume
`release-evidence-collection-plan.json` as local provenance evidence. Publish
preflight checks the plan identity, `dist/release-dry-run` output root,
index/status/action/handoff links, ordered evidence collection steps, summary
counters, manual action shape, and disabled execution flags. The build graph
still treats the plan as disposable local evidence only: publish does not run
collection steps, fan out commands, execute tools, upload assets, sign or
attest artifacts, mutate plugins, automate MO2 or GECK, run runtime probes,
or use AI.

Gate 308 extends `forge release publish` to consume the local release dry-run
evidence set as cross-linked provenance evidence. Publish preflight checks
`release-evidence-index.json`, `release-evidence-status.json`,
`release-evidence-actions.json`, `release-evidence-collection-plan.json`, and
`release-evidence-handoff.md` for identity, links, required evidence rows,
action references, collection-step references, summary counters, handoff
references, and disabled execution flags. The build graph still treats these
files as disposable local evidence only: publish does not run command fan-out,
execute tools, upload assets, sign or attest artifacts, mutate plugins,
automate MO2 or GECK, run runtime probes, or use AI.

Gate 309 extends `forge release publish` to summarize remediation for failed
release dry-run evidence provenance states. Publish preflight derives
`dryRunEvidenceRemediation` from local cross-link evidence, reports manual
actions for missing, malformed, or cross-link-mismatched dry-run evidence, and
keeps the graph local and declarative. It still does not run remediation
commands, command fan-out, tools, uploads, signing, plugin mutation, MO2/GECK
automation, runtime probes, or AI.

Gate 310 projects that remediation evidence into Doctor release-readiness and
handoff outputs while keeping the graph local and declarative. `forge doctor
export` consumes existing release dry-run preflight data and exposes manual
operator work, but still does not run remediation commands, command fan-out,
tools, uploads, signing, plugin mutation, MO2/GECK automation, runtime probes,
or AI.

Gate 311 closes the release dry-run remediation handoff lane and routes the
next implementation slice to `forge init` project scaffold planning. It adds
no build graph behavior, generated output behavior, release behavior,
external tool execution, provider installation, runtime probes, plugin
mutation, or AI.

Gate 312 adds no build graph execution. `forge init` now emits a
planning-only scaffold report for source files, registry files, generated and
distribution roots, local Forge config/cache paths, editor task metadata,
workflow metadata, and README, but it writes none of those files in this
gate. The report includes a future `forge validate <project-root> --format
json --no-input` command hint and explicit false execution flags. It does not
create generated outputs, dist outputs, build manifests, checksums, release
evidence, provider installs, external tool calls, runtime probes, plugin
mutation, MO2/GECK automation, remote calls, signing, attestation, or AI
behavior.

Gate 316 adds source scaffold file emission, not build graph execution.
`forge init` writes the root manifest and minimal dependency/capability
registry files under the selected project root when safety checks pass. It
does not create generated outputs, dist outputs, build manifests, checksums,
release evidence, editor/workflow files, README content, Forge config/cache
files, provider installs, external tool calls, runtime probes, plugin
mutation, MO2/GECK automation, remote calls, signing, attestation, or AI
behavior.

Gate 317 adds repo-local Forge config and README scaffold files, still outside
the build graph. `forge init` writes `.wastelandforge/config.jsonc` and
`README.md` as source-adjacent onboarding files while leaving generated
outputs, dist outputs, cache directories, editor task files, workflow files,
build manifests, checksums, release evidence, provider installs, external
tool calls, runtime probes, plugin mutation, MO2/GECK automation, remote
calls, signing, attestation, and AI behavior untouched.

Gate 318 adds VS Code task scaffold files, still outside the build graph.
`forge init` writes `.vscode/tasks.json` as source-adjacent editor onboarding
metadata while leaving generated outputs, dist outputs, cache directories,
workflow files, build manifests, checksums, release evidence, provider
installs, external tool calls, runtime probes, plugin mutation, MO2/GECK
automation, remote calls, signing, attestation, and AI behavior untouched.

Gate 319 adds GitHub Actions workflow scaffold files, still outside the build
graph. `forge init` writes `.github/workflows/wastelandforge.yml` as
source-adjacent CI onboarding metadata while leaving generated outputs, dist
outputs, cache directories, build manifests, checksums, release evidence,
provider installs, external tool calls, runtime probes, plugin mutation,
MO2/GECK automation, remote calls from `forge init`, signing, attestation, and
AI behavior untouched.

Gate 320 adds editor schema association scaffold files, still outside the
build graph. `forge init` writes `.vscode/settings.json` as source-adjacent
editor metadata that points JSON and YAML authoring globs at canonical
WastelandForge schema IDs while leaving generated outputs, dist outputs,
cache directories, build manifests, checksums, release evidence, provider
installs, external tool calls, runtime probes, plugin mutation, MO2/GECK
automation, remote calls from `forge init`, signing, attestation, VS Code
extension processes, language-server processes, and AI behavior untouched.

Gate 321 closes the `forge init` onboarding lane without changing the build
graph. The next routed value slice is Forge CLI runner/bootstrap planning, so
tasks and workflows can move toward a documented way to obtain a known
`forge` command before build graph or release steps execute. Gate 321 leaves
generated outputs, dist outputs, cache directories, build manifests, checksums,
release evidence, provider installs, external tool calls, runtime probes,
plugin mutation, MO2/GECK automation, remote calls, package publication,
signing, attestation, VS Code extension processes, language-server processes,
and AI behavior untouched.

Gate 322 defines the CLI runner/bootstrap model without changing the build
graph. The first concrete route is a source-built repo-local runner shim so
future generated tasks and workflows can move toward a documented `forge`
adapter before build graph or release steps execute. Gate 322 leaves generated
outputs, dist outputs, cache directories, build manifests, checksums, release
evidence, provider installs, external tool calls, runtime probes, plugin
mutation, MO2/GECK automation, remote calls, package publication, signing,
attestation, VS Code extension processes, language-server processes, and AI
behavior untouched.

Gate 323 adds that source-built repo-local runner shim without changing the
build graph. `eng/forge.ps1` invokes the checked-in CLI project through
`dotnet run` from the repository root, and `eng/forge.cmd` delegates to it for
Windows command shells. The shim is an adapter around the real CLI and does
not create generated outputs, dist outputs, build manifests, checksums,
release evidence, local-tool packages, tool manifests, workflow mutations,
provider installs, external tool calls, runtime probes, plugin mutation,
MO2/GECK automation, remote calls, package publication, signing, attestation,
VS Code extension processes, language-server processes, or AI behavior.

Gate 324 plans the local-tool package metadata lane without changing the
build graph. It records that package metadata should be scoped to
`src/WastelandForge.Cli/WastelandForge.Cli.csproj`, with local package output
and temporary local-tool install smoke before any checked-in tool manifest or
workflow integration. It does not create generated outputs, dist outputs,
build manifests, checksums, release evidence, local-tool packages, tool
manifests, workflow mutations, provider installs, external tool calls,
runtime probes, plugin mutation, MO2/GECK automation, remote calls, package
publication, signing, attestation, VS Code extension processes,
language-server processes, or AI behavior.

Gate 325 adds CLI local-tool package metadata without changing the build
graph. It makes only `WastelandForge.Cli` packable as a .NET tool, writes
local package output under ignored `artifacts/local-tool/nupkg`, and proves a
temporary local-tool install can run `forge --version` and `forge help`. It
does not create generated source outputs, build manifests, checksums, release
evidence, checked-in tool manifests, workflow mutations, provider installs,
external tool calls, runtime probes, plugin mutation, MO2/GECK automation,
remote calls, package publication, signing, attestation, VS Code extension
processes, language-server processes, or AI behavior.

Gate 326 plans the checked-in local tool manifest flow without changing the
build graph. It records that the repository-pinned manifest should not be
committed until the restore contract is explicit, because the package is still
local and unpublished. The future source-built/local-package restore shape is
`dotnet pack`, then `dotnet tool restore --add-source
artifacts/local-tool/nupkg`, then `dotnet tool run forge`. Gate 326 creates no
generated source outputs, build manifests, checksums, release evidence,
checked-in tool manifests, workflow mutations, provider installs, external
tool calls, runtime probes, plugin mutation, MO2/GECK automation, remote
calls, package publication, signing, attestation, VS Code extension processes,
language-server processes, or AI behavior.

Gate 327 adds the checked-in local tool manifest without changing the build
graph. `.config/dotnet-tools.json` pins `wastelandforge.cli` version `0.1.0`
and command `forge`, while restore remains explicit about local package output
until publication is gated. Validation packs the CLI, restores through a
temporary local-only NuGet config and isolated package cache, and runs
`forge --version` and `forge help` through the local tool. Gate 327 creates no
generated source outputs, build manifests, checksums, release evidence,
workflow mutations, provider installs, external tool calls, runtime probes,
plugin mutation, MO2/GECK automation, remote calls, package publication,
signing, attestation, VS Code extension processes, language-server processes,
or AI behavior.

Gate 328 plans generated workflow and task bootstrap integration without
changing the build graph. It records that the WastelandForge source repository
can pack the CLI, restore the checked-in tool manifest from local package
output, and invoke `dotnet tool run forge`, but consumer projects generated by
`forge init` cannot assume the WastelandForge source tree exists. Gate 328
creates no generated source outputs, build manifests, checksums, release
evidence, workflow mutations, task mutations, provider installs, external tool
calls, runtime probes, plugin mutation, MO2/GECK automation, remote calls,
package publication, signing, attestation, VS Code extension processes,
language-server processes, or AI behavior.

Gate 329 adds a repo-local local-tool restore helper without changing the
build graph. `eng/Restore-ForgeTool.ps1` packs the CLI project, restores the
checked-in tool manifest from local package output using an ignored local-only
NuGet config and isolated package cache, and verifies the restored `forge`
command. Gate 329 creates no generated source outputs, build manifests,
checksums, release evidence, workflow mutations, task mutations, provider
installs, external tool calls, runtime probes, plugin mutation, MO2/GECK
automation, remote calls, package publication, signing, attestation, VS Code
extension processes, language-server processes, or AI behavior.

Gate 330 integrates the repo-local restore helper into the repository-owned CI
workflow without changing the build graph. The Ubuntu validation lane and
release dry-run lane restore the local tool before invoking the restored
`forge` local tool for validation and release verification; the Windows lane
restores the local tool as bootstrap smoke. Gate 330 creates no generated
source outputs, build manifests, checksums, release evidence model changes,
generated workflow mutations, task mutations, provider installs, external
tool calls, runtime probes, plugin mutation, MO2/GECK automation, package
publication, signing, attestation, VS Code extension processes,
language-server processes, or AI behavior.

Gate 331 plans repository-owned developer task local-tool bootstrap without
changing the build graph. The planned source-repository `.vscode/tasks.json`
will restore the checked-in local tool before invoking read-only Forge tasks,
but Gate 331 creates no generated source outputs, build manifests, checksums,
release evidence model changes, repository task files, generated workflow
mutations, generated task mutations, provider installs, external tool calls,
runtime probes, plugin mutation, MO2/GECK automation, package publication,
signing, attestation, VS Code extension processes, language-server processes,
or AI behavior.

Gate 332 adds repository-owned developer task local-tool bootstrap without
changing the build graph. The source-repository `.vscode/tasks.json` restores
the checked-in local tool before invoking read-only Forge tasks through
`dotnet tool run forge --`. Gate 332 creates no generated source outputs,
build manifests, checksums, release evidence model changes, generated
workflow mutations, generated task mutations, provider installs, external
tool calls, runtime probes, plugin mutation, MO2/GECK automation, package
publication, signing, attestation, VS Code extension processes,
language-server processes, or AI behavior.

Gate 333 closes the repository-owned local-tool bootstrap lane without
changing the build graph. It records the accepted source-repository bootstrap
path and routes the next value slice to consumer-project package-source
policy planning. Gate 333 creates no generated source outputs, build
manifests, checksums, release evidence model changes, generated workflow
mutations, generated task mutations, provider installs, external tool calls,
runtime probes, plugin mutation, MO2/GECK automation, package publication,
signing, attestation, VS Code extension processes, language-server processes,
or AI behavior.

Gate 334 plans the consumer-project package-source policy without changing
the build graph. It keeps generated projects source-agnostic and using
`forge` on `PATH` until package publication, feed configuration, signing, or
standalone distribution policy is gated. Gate 334 creates no generated source
outputs, build manifests, checksums, release evidence model changes,
generated workflow mutations, generated task mutations, provider installs,
external tool calls, runtime probes, plugin mutation, MO2/GECK automation, package
publication, signing, attestation, VS Code extension processes,
language-server processes, or AI behavior.

Gate 335 changes generated consumer-project guidance without changing the build
graph. Generated README, VS Code task, and GitHub Actions workflow templates now
check or explain the prerequisite that `forge` already exists on `PATH`, while
the validate, capability scan, build reports, and release dry-run commands stay
thin wrappers around the same canonical CLI. Gate 335 creates no build
manifests, checksums, release evidence model changes, package restore,
publication, signing, attestation, provider installs, external tool calls,
runtime probes, plugin mutation, VS Code extension processes, language-server
processes, or AI behavior.

Gate 336 closes the generated command availability guidance lane without
changing the build graph. It records that consumer scaffolds now expect an
existing `forge` command honestly, while package/feed publication and standalone
distribution remain unresolved. Gate 336 creates no generated source outputs,
build manifests, checksums, release evidence model changes, executable package
outputs, installer outputs, package restore, publication, signing, attestation,
provider installs, external tool calls, runtime probes, plugin mutation,
VS Code extension processes, language-server processes, or AI behavior. The
next route is Gate 337 local standalone Forge executable distribution planning.

Gate 337 plans local standalone Forge executable distribution without changing
the build graph. The planned artifact shape is a local source-repository
distribution folder containing `forge.exe`, minimal instructions,
`build-manifest.json`, and `checksums.sha256`, separate from NuGet restore and
from `WastelandForge.exe` app-shell packaging. Gate 337 creates no generated
source outputs, build manifests, checksums, executable package outputs,
installer outputs, package restore, publication, signing, attestation, provider
installs, external tool calls, runtime probes, plugin mutation, VS Code
extension processes, language-server processes, or AI behavior. Gate 338 is
routed to the local standalone distribution scaffold.

Gate 338 adds the local standalone Forge executable distribution scaffold
without changing the Forge build graph or generated consumer projects.
`eng/Publish-StandaloneForge.ps1` publishes the CLI project into the ignored
`dist/local/forge/` distribution folder, writes local `build-manifest.json` and
`checksums.sha256` evidence, and includes a minimal `README.txt`. It does not
add release publication, archive creation, installer outputs, package restore,
NuGet publication, signing, attestation, provider installs, external tool
calls, runtime probes, plugin mutation, app-shell packaging, VS Code extension
processes, language-server processes, or AI behavior. Gate 339 is routed to
close the standalone distribution lane and select the next value slice.

Gate 386 adds deterministic combined MCM/JIP package assembly under
`dist/mod-package`. It rebuilds canonical source through existing component
generators, refuses normalized destination collisions, validates a dedicated
immutable package manifest, revalidates archive entries, and records local
source/output/archive provenance without installing or executing the payload.

Gate 390 specializes `forge init --template fnv-framework` into a 12-file
runtime-enabled MCM/JIP source scaffold. Init still writes no generated or
distribution output; the resulting canonical source can be validated and then
packaged separately through the Gate 386 deterministic build graph.
