# Dependency Registry Schemas

Reserved for dependency registry schemas.

R004 makes the dependency registry mandatory for the bootstrap contract slice, but Gate 3 only creates the manifest schema. Dependency schema content starts after the manifest package skeleton is accepted.
