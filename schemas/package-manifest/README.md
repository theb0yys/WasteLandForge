# Package manifest schemas

This directory contains WastelandForge package manifest schemas.

- `0.1.0/schema.json` defines the deterministic manifest emitted for the first MCM Extender loose-file package target.
