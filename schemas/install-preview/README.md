# Install preview schemas

This directory contains WastelandForge install-preview report schemas.

- `0.1.0/schema.json` defines the preview-only install intent report emitted for the first MCM Extender loose-file package target.
