# Dependency Registry Schemas

Dependency registry schemas validate capability requirements declared by a
WastelandForge project.

R004 makes the dependency registry mandatory for the bootstrap contract slice.
Gate 13 adds `0.1.0/schema.json` as an immutable Draft 2020-12 public schema.
