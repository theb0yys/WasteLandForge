# ADR-010 - Developer Experience and CLI Workflow Model

Status: Accepted
Research classification: Documented
Source: `WasteLandForge/research/R006 Developer Experience and CLI Workflow Model for WastelandForge-deep-research-report.md`

## Decision

WastelandForge uses a small, stable, offline-first, AI-optional CLI.

The canonical command surface is:

```text
forge init
forge validate
forge capabilities list
forge capabilities scan
forge capabilities explain
forge generate
forge build
forge package
forge release verify
forge release prepare
forge release publish
forge docs
forge graph
forge explain
forge clean
forge doctor export
forge help
forge --version
```

## Consequences

- Git hooks, editor tasks, and CI workflows must be thin wrappers around the Forge CLI.
- CLI behavior must be deterministic and composable.
- Help output should be examples-first.
- Machine-facing output contracts must be stable.
- Do not introduce undocumented convenience aliases.

## Gate Impact

Gate 6 implements the first CLI skeleton. Earlier gates must preserve the
command namespace. Gate 57 implements `forge capabilities list` against the
built-in FNV catalogue while keeping `forge capabilities scan` and
`forge capabilities explain` reserved for later detector and explanation
gates. Gate 58 implements `forge capabilities scan` for explicit local path
evidence. Gate 59 implements `forge capabilities explain` for built-in
capability and provider IDs against catalogue and scan evidence. Gate 60
extends `forge capabilities scan` with `--project` requirement resolution
without adding aliases or changing the canonical command surface. Gate 61
implements the canonical `forge generate` and `forge build` commands for the
low-risk `reports` target while keeping later game-facing generators reserved.
Gate 62 adds `--target mcm-json` to `forge generate` and `forge build`
without adding aliases or changing the canonical command surface.
Gate 63 keeps the same command surface and changes only the implemented output
from a placeholder skeleton to the validated MCM Extender runtime subset.
Gate 64 keeps the same command surface while adding runtime requirements
pass-through and translation-file output to the `mcm-json` target.
Gate 65 keeps the same command surface while expanding `mcm-json` to emit
checkbox and string-toggle settings.
Gate 66 keeps the same command surface while expanding `mcm-json` to emit
keybind settings.
Gate 67 keeps the same command surface while expanding `mcm-json` to emit
header settings.
Gate 68 keeps the same command surface while expanding `mcm-json` to emit
image settings.
Gate 69 keeps the same command surface while adding validation for MCM image
asset references.
Gate 70 keeps the same command surface while expanding `mcm-json` output to
include staged referenced texture assets under their game-relative target
paths.
Gate 71 keeps the same command surface while adding `package-manifest.json` to
the `mcm-json` generate/build output.
Gate 72 keeps the same command surface while adding build-time
`dist/mcm-json/package.zip` output for the `mcm-json` build target.
Gate 73 implements the canonical `forge package` command skeleton for
`--target mcm-json` without adding aliases or implementing FOMOD packaging.
Gate 74 keeps the same command surface while adding package manifest schema
validation and archive-entry validation to the `mcm-json` package/build path.
Gate 75 keeps the same command surface while adding `install-preview.json` to
the `mcm-json` generate/build/package outputs.
Gate 76 keeps the same command surface while validating that install-preview
report against an embedded schema.
Gate 77 keeps the same command surface while adding a human-readable
install-preview summary to the `mcm-json` generate/build/package outputs.
Gate 78 keeps the same command surface while adding `package-verification.json`
to the `mcm-json` generate/build/package outputs and human/JSON CLI summaries.
Gate 79 keeps the same command surface while validating
`package-verification.json` against an embedded schema.
Gate 80 keeps the same command surface while adding `package-verification.md`
as human-readable local package verification evidence for the `mcm-json`
generate/build/package path.
Gate 81 keeps the same command surface while adding local package-verification
evidence cross-checks and manifest cross-check status for the `mcm-json`
generate/build/package path.
Gate 82 keeps the same command surface while moving those checks into reusable
validator code for the `mcm-json` generate/build/package path.
Gate 83 keeps the same command surface while adding an internal file-based
package-verification verifier for generated `mcm-json` package evidence.
Gate 84 keeps the same command surface while adding internal package payload
digest verification for generated `mcm-json` package evidence.
Gate 85 keeps the same command surface while adding internal package archive
digest verification for generated `mcm-json` package evidence.
Gate 86 keeps the same command surface while adding internal package archive
entry-name revalidation for generated `mcm-json` package evidence.
Gate 87 keeps the same command surface and records
`forge package --target mcm-json --verify-existing` as the future command
shape for existing package evidence verification, without adding aliases.
Gate 88 keeps the same command surface and implements
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 89 keeps the same command surface and adds SARIF/GitHub diagnostic output
to `forge package --target mcm-json --verify-existing` without adding aliases.
Gate 90 keeps the same command surface and adds `--summary <path>` to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 91 keeps the same command surface and adds checksum-file revalidation to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 92 keeps the same command surface and adds build-manifest content
revalidation to `forge package --target mcm-json --verify-existing` without
adding aliases.
Gate 93 keeps the same command surface and adds install-preview summary
content revalidation to `forge package --target mcm-json --verify-existing`
without adding aliases.
Gate 94 keeps the same command surface and adds install-preview/package-manifest
entry content cross-checking to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 95 keeps the same command surface and adds package-verification summary
content revalidation to `forge package --target mcm-json --verify-existing`
without adding aliases.
Gate 96 keeps the same command surface and adds package-verification JSON
check content revalidation to `forge package --target mcm-json --verify-existing`
without adding aliases.
Gate 97 keeps the same command surface and adds package-verification JSON
metadata content revalidation to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 98 keeps the same command surface and adds package-verification archive
detail content revalidation to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 99 keeps the same command surface and adds install-preview archive detail
content revalidation to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 100 keeps the same command surface and adds package-manifest archive
detail content revalidation to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 101 keeps the same command surface and adds archive detail cross-report
consistency revalidation to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 102 keeps the same command surface and adds package archive presence
revalidation to `forge package --target mcm-json --verify-existing` without
adding aliases.
Gate 103 keeps the same command surface and adds checksum unexpected-entry
revalidation to `forge package --target mcm-json --verify-existing` without
adding aliases.
Gate 104 keeps the same command surface and adds checksum duplicate-entry
revalidation to `forge package --target mcm-json --verify-existing` without
adding aliases.
Gate 105 keeps the same command surface and adds checksum canonical-order
revalidation to `forge package --target mcm-json --verify-existing` without
adding aliases.
Gate 106 keeps the same command surface and adds checksum digest
canonical-casing revalidation to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 107 keeps the same command surface and adds checksum line-ending and
trailing-newline revalidation to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 108 keeps the same command surface and adds checksum path separator
canonicalization revalidation to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 109 keeps the same command surface and adds checksum blank-line
revalidation to `forge package --target mcm-json --verify-existing` without
adding aliases.
Gate 110 keeps the same command surface and adds checksum entry spacing
canonicalization revalidation to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 111 keeps the same command surface and adds checksum path casing
canonicalization revalidation to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 112 keeps the same command surface and adds checksum case-insensitive
duplicate revalidation to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 113 keeps the same command surface and adds checksum malformed-entry
format revalidation to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 114 keeps the same command surface and adds checksum path containment
revalidation to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 115 keeps the same command surface and adds checksum comment-line
rejection revalidation to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 116 keeps the same command surface and adds `install-plan.json` and
`install-plan.md` evidence to `forge generate|build|package --target mcm-json`
without adding aliases or installer subcommands.
Gate 117 keeps the same command surface and adds install-plan JSON and
Markdown content revalidation to
`forge package --target mcm-json --verify-existing` without adding aliases.
Gate 118 keeps the same command surface and adds install-plan schema
revalidation to `forge package --target mcm-json --verify-existing` without
adding aliases.
Gate 119 keeps the same command surface and adds package-manifest schema
revalidation to `forge package --target mcm-json --verify-existing` without
adding aliases.
Gate 120 keeps the same command surface and adds install-preview schema
revalidation to `forge package --target mcm-json --verify-existing` without
adding aliases.
Gate 121 keeps the same command surface and adds package-verification schema
revalidation to `forge package --target mcm-json --verify-existing` without
adding aliases.
Gate 122 keeps the same command surface and adds diagnostic projection
coverage for schema-gated verify-existing failures without adding aliases.
Gate 123 keeps the same command surface and adds explicit missing evidence
diagnostics to `forge package --target mcm-json --verify-existing` without
adding aliases.
Gate 124 keeps the same command surface and adds explicit malformed evidence
diagnostics to `forge package --target mcm-json --verify-existing` without
adding aliases.
Gate 125 keeps the same command surface and adds diagnostic projection
coverage for malformed JSON evidence failures without adding aliases.
Gate 126 keeps the same command surface and closes the current MCM Extender
lane. Existing `mcm-json` commands remain canonical, but the next implementation
lane moves back to `forge capabilities scan` and `forge capabilities explain`
instead of adding new MCM verifier aliases or micro-gates.
Gate 127 keeps the same command surface and adds Doctor-style readiness data to
`forge capabilities scan` plus target actions to `forge capabilities explain`.
It does not add `/forge scan`, `forge doctor`, or other convenience aliases.
Gate 128 keeps the same command surface and implements the canonical
`forge doctor export` command as a redacted local handoff bundle. It does not
add aliases or implement runtime probes, MO2 VFS launch, provider version
checks, or AI explanation.
Gate 129 keeps the same command surface and adds diagnostic projection formats
to `forge capabilities scan`: SARIF and GitHub annotations are now supported
for projected `WF-CAP-*` findings. It does not add aliases or change
`forge doctor export` into a SARIF/GitHub command.
Gate 130 keeps the same command surface and adds provider evidence detail to
the existing `forge capabilities scan` JSON, SARIF, GitHub, and text outputs.
It does not add aliases or change `forge doctor export` into a SARIF/GitHub
command.
Gate 131 keeps the same command surface and adds grouped provider evidence to
`forge capabilities explain` JSON and text output. It does not add aliases or
change `forge doctor export` into a SARIF/GitHub command.
Gate 132 keeps the same command surface and adds wrong-scope status and
`WF-CAP-004` projection to `forge capabilities scan`. It does not add aliases
or change `forge doctor export` into a SARIF/GitHub command.
Gate 133 keeps the same command surface and adds the existing `--project`
option shape to `forge capabilities explain`. It does not add aliases or
change `capabilities explain` into a SARIF/GitHub diagnostic projection
command.
Gate 134 keeps the same command surface and adds diagnostic handoff metadata
to `forge capabilities explain --project`. It does not add aliases or change
`capabilities explain` into a SARIF/GitHub diagnostic projection command.
Gate 135 keeps the same command surface and adds top-level summary and index
sections to `forge doctor export`. It does not add aliases or change
`forge doctor export` into a SARIF/GitHub diagnostic projection command.
Gate 136 keeps the same command surface and adds compact diagnostics entries
under `forge doctor export` `index.diagnostics`. It does not add aliases or
change `forge doctor export` into a SARIF/GitHub diagnostic projection
command.
Gate 137 keeps the same command surface and adds compact unavailable
requirement entries under `forge doctor export` `index.requirements`. It does
not add aliases or change `forge doctor export` into a SARIF/GitHub diagnostic
projection command.
Gate 138 keeps the same command surface and adds compact action groups under
`forge doctor export` `index.actions`. It does not add aliases or change
`forge doctor export` into a SARIF/GitHub diagnostic projection command.
Gate 139 keeps the same command surface and adds structured open-question
details under `forge doctor export` `index.openQuestionDetails`. It does not
add aliases or change `forge doctor export` into a SARIF/GitHub diagnostic
projection command.
Gate 140 keeps the same command surface and adds compact provider-status
groups under `forge doctor export` `index.providerStatuses`. It does not add
aliases or change `forge doctor export` into a SARIF/GitHub diagnostic
projection command.
Gate 141 keeps the same command surface and adds compact capability-status
groups under `forge doctor export` `index.capabilityStatuses`. It does not add
aliases or change `forge doctor export` into a SARIF/GitHub diagnostic
projection command.
Gate 142 keeps the same command surface and adds compact Doctor area-status
groups under `forge doctor export` `index.doctorAreaStatuses`. It does not add
aliases or change `forge doctor export` into a SARIF/GitHub diagnostic
projection command.
Gate 143 keeps the same command surface and adds compact catalogue-policy
groups under `forge doctor export` `index.cataloguePolicy`. It does not add
aliases or change `forge doctor export` into a SARIF/GitHub diagnostic
projection command.
Gate 144 keeps the same command surface and adds compact readiness groups
under `forge capabilities scan` `doctor.index.areaStatuses`. It does not add
aliases or change SARIF/GitHub scan output.
Gate 145 keeps the same command surface and adds compact scan status groups
under `forge capabilities scan` `index.providerStatuses` and
`index.capabilityStatuses`. It does not add aliases or change SARIF/GitHub
scan output.
Gate 146 keeps the same command surface and adds compact action groups under
`forge capabilities scan` `index.actions`. It does not add aliases or change
SARIF/GitHub scan output.
Gate 147 keeps the same command surface and adds compact unavailable
requirement entries under `forge capabilities scan` `index.requirements`. It
does not add aliases or change SARIF/GitHub scan output.
Gate 148 keeps the same command surface and adds compact already-projected
diagnostic entries under `forge capabilities scan` `index.diagnostics`. It
does not add aliases or change SARIF/GitHub scan output.
Gate 149 keeps the same command surface and adds compact catalogue-policy
groups under `forge capabilities scan` `index.cataloguePolicy`. It does not
add aliases or change SARIF/GitHub scan output.
Gate 150 keeps the same command surface and adds compact open-question detail
entries under `forge capabilities scan` `index.openQuestionDetails`. It does
not add aliases or change SARIF/GitHub scan output.
Gate 151 keeps the same command surface and adds catalogue-policy
open-question detail entries under `forge capabilities explain`
`cataloguePolicy.openQuestionDetails`. It does not add aliases or change
SARIF/GitHub explain output.
Gate 152 keeps the same command surface and adds catalogue-policy diagnostic
handoff metadata under `forge capabilities explain`
`cataloguePolicy.diagnosticHandoff`. It does not add aliases, new diagnostic
rules, or SARIF/GitHub explain output.
Gate 153 keeps the same command surface and adds catalogue-policy diagnostic
handoff metadata under `forge doctor export`
`index.cataloguePolicyDiagnosticHandoff`. It does not add aliases, new
diagnostic rules, or SARIF/GitHub Doctor export output.
Gate 154 keeps the same command surface and adds catalogue-policy diagnostic
handoff metadata under `forge capabilities scan`
`index.cataloguePolicyDiagnosticHandoff`. It does not add aliases, new
diagnostic rules, or SARIF/GitHub scan output.
Gate 155 keeps the same command surface and output contracts while sharing
catalogue-policy diagnostic handoff rendering across `forge capabilities
explain`, `forge capabilities scan`, and `forge doctor export`. It does not
add aliases, new diagnostic rules, or SARIF/GitHub output.
Gate 156 keeps the same command surface and output contracts while sharing
catalogue-policy open-question detail and source-type index rendering across
`forge capabilities explain`, `forge capabilities scan`, and
`forge doctor export`. It does not add aliases, new diagnostic rules, or
SARIF/GitHub output.
Gate 157 keeps the same command surface and output contracts while sharing a
catalogue-policy view model across `forge capabilities explain`,
`forge capabilities scan`, and `forge doctor export`. It does not add aliases,
new diagnostic rules, or SARIF/GitHub output.
Gate 158 keeps the same command surface while adding `index.actionSummary` to
`forge capabilities scan` and `forge doctor export`. It does not add aliases,
new diagnostic rules, or SARIF/GitHub output.
Gate 159 keeps the same command surface while adding `index.evidenceSummary`
to `forge capabilities scan` and `forge doctor export`. It does not add
aliases, new diagnostic rules, or SARIF/GitHub output.
Gate 160 keeps the same command surface while adding
`index.requirementSummary` to `forge capabilities scan` and
`forge doctor export`. It does not add aliases, new diagnostic rules,
resolver behavior, or SARIF/GitHub output.
Gate 161 keeps the same command surface while adding
`index.diagnosticSummary` to `forge capabilities scan` and
`forge doctor export`. It does not add aliases, new diagnostic rules,
diagnostic projection behavior, or SARIF/GitHub output.
Gate 162 keeps the same command surface while adding
`index.providerInventorySummary` to `forge capabilities scan` and
`forge doctor export`. It does not add aliases, new diagnostic rules,
provider detection behavior, or SARIF/GitHub output.
Gate 163 keeps the same command surface while adding
`index.doctorAreaCapabilitySummary` to `forge capabilities scan` and
`forge doctor export`. It does not add aliases, new diagnostic rules, Doctor
planning behavior, or SARIF/GitHub output.
Gate 164 keeps the same canonical command surface while adding
`forge doctor export --summary <path>` as a redacted Markdown sidecar summary.
It does not add aliases, `--format markdown`, new diagnostic rules, Doctor
planning behavior, or Doctor export SARIF/GitHub output.
Gate 165 keeps the same canonical command surface while adding
`forge capabilities scan --summary <path>` as a path-minimized Markdown
sidecar summary. It does not add aliases, `--format markdown`, new diagnostic
rules, Doctor planning behavior, scan SARIF/GitHub changes, GitHub
step-summary output, or provider detection behavior.
Gate 166 keeps the same canonical command surface while adding
`forge doctor export --bundle <path>` as a deterministic redacted ZIP sidecar
archive. It does not add aliases, `--format zip`, `--format markdown`, new
diagnostic rules, Doctor planning behavior, Doctor export SARIF/GitHub output,
GitHub step-summary output, release publishing, or provider detection
behavior.
Gate 167 keeps the same canonical command surface while adding
`forge capabilities explain --summary <path>` as a path-minimized Markdown
sidecar summary. It does not add aliases, `--format markdown`, new diagnostic
rules, Doctor planning behavior, explain SARIF/GitHub output, GitHub
step-summary output, or provider detection behavior.
Gate 168 keeps the same canonical command surface while extending
`forge doctor export --bundle <path>` with per-requirement explanation
Markdown entries for unavailable project capability requirements. It does not
add aliases, `--format zip`, `--format markdown`, new diagnostic rules,
Doctor planning behavior, Doctor export SARIF/GitHub output, GitHub
step-summary output, release publishing, or provider detection behavior.
Gate 169 keeps the same canonical command surface while adding matching
per-requirement explanation JSON entries to `forge doctor export --bundle
<path>`. It does not add aliases, `--format zip`, `--format markdown`, new
diagnostic rules, Doctor planning behavior, Doctor export SARIF/GitHub
output, GitHub step-summary output, release publishing, or provider detection
behavior.
Gate 170 keeps the same canonical command surface while adding requirement
explanation index JSON and Markdown entries to `forge doctor export --bundle
<path>`. It does not add aliases, `--format zip`, `--format markdown`, new
diagnostic rules, Doctor planning behavior, Doctor export SARIF/GitHub
output, GitHub step-summary output, release publishing, or provider detection
behavior.
Gate 171 keeps the same canonical command surface while adding `README.md` to
`forge doctor export --bundle <path>` archives. It does not add aliases,
`--format zip`, `--format markdown`, new diagnostic rules, Doctor planning
behavior, Doctor export SARIF/GitHub output, GitHub step-summary output,
release publishing, or provider detection behavior.
Gate 172 keeps the same canonical command surface while adding
`diagnostics/index.json` and `diagnostics/index.md` to
`forge doctor export --bundle <path>` archives. It does not add aliases,
`--format zip`, `--format markdown`, new diagnostic rules, Doctor planning
behavior, Doctor export SARIF/GitHub output, GitHub step-summary output,
release publishing, or provider detection behavior.
Gate 173 keeps the same canonical command surface while adding
`actions/index.json` and `actions/index.md` to
`forge doctor export --bundle <path>` archives. It does not add aliases,
`--format zip`, `--format markdown`, new diagnostic rules, Doctor planning
behavior, Doctor export SARIF/GitHub output, GitHub step-summary output,
release publishing, or provider detection behavior.
Gate 174 keeps the same canonical command surface while adding
`requirements/index.json` and `requirements/index.md` to
`forge doctor export --bundle <path>` archives. It does not add aliases,
`--format zip`, `--format markdown`, new diagnostic rules, Doctor planning
behavior, Doctor export SARIF/GitHub output, GitHub step-summary output,
release publishing, or provider detection behavior.
Gate 175 keeps the same canonical command surface while adding
`providers/index.json` and `providers/index.md` to
`forge doctor export --bundle <path>` archives. It does not add aliases,
`--format zip`, `--format markdown`, new diagnostic rules, Doctor planning
behavior, Doctor export SARIF/GitHub output, GitHub step-summary output,
release publishing, provider detection behavior, or provider-version
behavior.
Gate 176 keeps the same canonical command surface while adding
`capabilities/index.json` and `capabilities/index.md` to
`forge doctor export --bundle <path>` archives. It does not add aliases,
`--format zip`, `--format markdown`, new diagnostic rules, Doctor planning
behavior, Doctor export SARIF/GitHub output, GitHub step-summary output,
release publishing, provider detection behavior, resolver behavior, or
provider-version behavior.
Gate 177 keeps the same canonical command surface while adding
`doctor-areas/index.json` and `doctor-areas/index.md` to
`forge doctor export --bundle <path>` archives. It does not add aliases,
`--format zip`, `--format markdown`, new diagnostic rules, Doctor planning
behavior, Doctor export SARIF/GitHub output, GitHub step-summary output,
release publishing, provider detection behavior, resolver behavior, or
provider-version behavior.
Gate 178 keeps the same canonical command surface while adding
`catalogue-policy/index.json` and `catalogue-policy/index.md` to
`forge doctor export --bundle <path>` archives. It does not add aliases,
`--format zip`, `--format markdown`, new diagnostic rules, Doctor planning
behavior, Doctor export SARIF/GitHub output, GitHub step-summary output,
release publishing, provider detection behavior, resolver behavior, or
provider-version behavior.
Gate 179 keeps the same canonical command surface while adding
`summary/index.json` and `summary/index.md` to
`forge doctor export --bundle <path>` archives. It does not add aliases,
`--format zip`, `--format markdown`, new diagnostic rules, Doctor planning
behavior, Doctor export SARIF/GitHub output, GitHub step-summary output,
release publishing, provider detection behavior, resolver behavior, or
provider-version behavior.
Gate 180 keeps the same canonical command surface while adding
`evidence/index.json` and `evidence/index.md` to
`forge doctor export --bundle <path>` archives. It does not add aliases,
`--format zip`, `--format markdown`, new diagnostic rules, Doctor planning
behavior, Doctor export SARIF/GitHub output, GitHub step-summary output,
release publishing, provider detection behavior, resolver behavior, or
provider-version behavior.
Gate 181 keeps the same canonical command surface while adding
`redaction/index.json` and `redaction/index.md` to
`forge doctor export --bundle <path>` archives. It does not add aliases,
`--format zip`, `--format markdown`, new diagnostic rules, Doctor planning
behavior, Doctor export SARIF/GitHub output, GitHub step-summary output,
release publishing, provider detection behavior, resolver behavior, or
provider-version behavior.
Gate 182 keeps the same canonical command surface while adding
`open-questions/index.json` and `open-questions/index.md` to
`forge doctor export --bundle <path>` archives. It does not add aliases,
`--format zip`, `--format markdown`, new diagnostic rules, Doctor planning
behavior, Doctor export SARIF/GitHub output, GitHub step-summary output,
release publishing, provider detection behavior, resolver behavior, or
provider-version behavior.
Gate 183 keeps the same canonical command surface while adding
`scan-inputs/index.json` and `scan-inputs/index.md` to
`forge doctor export --bundle <path>` archives. It does not add aliases,
`--format zip`, `--format markdown`, new diagnostic rules, Doctor planning
behavior, Doctor export SARIF/GitHub output, GitHub step-summary output,
release publishing, provider detection behavior, resolver behavior, or
provider-version behavior.
Gate 184 keeps the same canonical command surface while adding
`bundle/index.json` and `bundle/index.md` to
`forge doctor export --bundle <path>` archives. It does not add aliases,
`--format zip`, `--format markdown`, new diagnostic rules, Doctor planning
behavior, Doctor export SARIF/GitHub output, GitHub step-summary output,
release publishing, provider detection behavior, resolver behavior, or
provider-version behavior.
Gate 185 keeps the same canonical command surface while adding
`triage/index.json` and `triage/index.md` to
`forge doctor export --bundle <path>` archives. It does not add aliases,
`--format zip`, `--format markdown`, new diagnostic rules, Doctor planning
behavior, Doctor export SARIF/GitHub output, GitHub step-summary output,
release publishing, provider detection behavior, resolver behavior, or
provider-version behavior.
Gate 186 keeps the same canonical command surface while adding top-level
`triage` to primary `forge doctor export --format json`, a `Triage:` section
to plain output, and `## Triage` to the Markdown summary sidecar. It does not
add aliases, `--format zip`, `--format markdown`, new diagnostic rules, Doctor
planning behavior, Doctor export SARIF/GitHub output, GitHub step-summary
output, release publishing, provider detection behavior, resolver behavior, or
provider-version behavior.
Gate 187 keeps the same canonical command surface while adding deterministic
Doctor triage command hints to primary JSON, plain text, Markdown summaries,
and ZIP archive triage entries. The hints use canonical commands with
placeholders and do not add aliases, `--format zip`, `--format markdown`, new
diagnostic rules, Doctor planning behavior, Doctor export SARIF/GitHub output,
GitHub step-summary output, release publishing, provider detection behavior,
resolver behavior, or provider-version behavior.
Gate 188 keeps the same canonical command surface while adding an ordered
Doctor triage worklist to primary JSON, plain text, Markdown summaries, and
ZIP archive triage entries. The worklist links to existing command-hint IDs
and does not add aliases, `--format zip`, `--format markdown`, new diagnostic
rules, Doctor planning behavior, Doctor export SARIF/GitHub output, GitHub
step-summary output, release publishing, provider detection behavior,
resolver behavior, or provider-version behavior.
Gate 189 keeps the same canonical command surface while adding Doctor worklist
summary metadata to primary JSON, plain text, Markdown summaries, and ZIP
archive triage entries. The summaries group existing worklist items by
priority and report section or archive path, and do not add aliases,
`--format zip`, `--format markdown`, new diagnostic rules, Doctor planning
behavior, Doctor export SARIF/GitHub output, GitHub step-summary output,
release publishing, provider detection behavior, resolver behavior, or
provider-version behavior.
Gate 190 keeps the same canonical command surface while adding a compact
Doctor remediation status header to primary JSON, plain text, Markdown
summaries, and ZIP archive triage entries. The header derives from existing
worklist and command-hint data and does not add aliases, `--format zip`,
`--format markdown`, new diagnostic rules, Doctor planning behavior, Doctor
export SARIF/GitHub output, GitHub step-summary output, release publishing,
provider detection behavior, resolver behavior, or provider-version behavior.
Gate 191 keeps the same canonical command surface while adding human operator
handoff sections to plain text, Markdown summaries, and ZIP archive triage
Markdown. The handoff derives from existing remediation, worklist-summary, and
command-hint data and does not add aliases, `--format zip`,
`--format markdown`, new diagnostic rules, Doctor planning behavior, Doctor
export SARIF/GitHub output, GitHub step-summary output, release publishing,
provider detection behavior, resolver behavior, provider-version behavior, or
command execution.
Gate 192 keeps the same canonical command surface while adding
`handoff-summary.md` to ZIP archive output from `forge doctor export --bundle
<path>`. The sidecar derives from existing redacted triage metadata and does
not add aliases, `--format zip`, `--format markdown`, new JSON contracts,
diagnostic rules, Doctor planning behavior, Doctor export SARIF/GitHub output,
GitHub step-summary output, release publishing, provider detection behavior,
resolver behavior, provider-version behavior, or command execution.
Gate 193 keeps the same canonical command surface while adding an operator
handoff checklist to `forge capabilities scan` plain output and Markdown
summary sidecars. It does not add aliases, `--format markdown`, new JSON
contracts, diagnostic rules, Doctor planning behavior, SARIF/GitHub scan
output changes, GitHub step-summary output, release publishing, provider
detection behavior, resolver behavior, provider-version behavior, or command
execution.
Gate 194 keeps the same canonical command surface while adding an operator
handoff checklist to `forge capabilities explain` plain output and Markdown
summary sidecars. It does not add aliases, `--format markdown`, new JSON
contracts, diagnostic rules, Doctor planning behavior, SARIF/GitHub explain
output changes, GitHub step-summary output, release publishing, provider
detection behavior, resolver behavior, provider-version behavior, or command
execution.
Gate 195 keeps the same canonical command surface while documenting and
testing that `forge doctor export --bundle <path>` requirement explanation
Markdown includes the explain-side operator handoff checklist. It does not add
aliases, `--format markdown`, new JSON contracts, diagnostic rules, Doctor
planning behavior, SARIF/GitHub output changes, GitHub step-summary output,
release publishing, provider detection behavior, resolver behavior,
provider-version behavior, or command execution.
Gate 196 keeps the same canonical command surface while adding
declaration-only provider-version metadata to `forge capabilities list` and
`forge capabilities explain` output. It does not add aliases, new commands,
new formats, provider detection behavior, resolver behavior,
unsupported-version diagnostics, runtime probes, MO2/GECK automation, or
command execution.
Gate 197 keeps the same canonical command surface while surfacing that
declaration-only provider-version metadata in `forge capabilities scan`
provider output and Doctor provider indexes. It does not add aliases, new
commands, new formats, provider detection behavior, resolver behavior,
unsupported-version diagnostics, runtime probes, MO2/GECK automation, or
command execution.
Gate 198 keeps the same canonical command surface while recording a
provider-version parser research checkpoint. It does not add aliases, new
commands, new formats, parser code, provider detection behavior, resolver
behavior, unsupported-version diagnostics, runtime probes, MO2/GECK
automation, or command execution.
Gate 199 keeps the same canonical command surface while adding the parser
contract under Registry. It does not add aliases, new commands, new formats,
CLI output fields, provider detection behavior, resolver behavior,
unsupported-version diagnostics, runtime probes, MO2/GECK automation, or
command execution.
Gate 200 keeps the same canonical command surface while documenting the
parser contract and future projection notes. It does not add aliases, new
commands, new formats, CLI output fields, provider detection behavior,
resolver behavior, unsupported-version diagnostics, runtime probes, MO2/GECK
automation, or command execution.
Gate 201 keeps the same canonical command surface while adding a standalone
parsed-evidence model for future scan data. It does not add aliases, new
commands, new formats, CLI output fields, provider detection behavior,
resolver behavior, unsupported-version diagnostics, runtime probes, MO2/GECK
automation, or command execution.
Gate 202 keeps the same canonical command surface while recording the JIP LN
text-script generator evidence checkpoint. It does not add aliases, new
commands, new formats, CLI output fields, `forge generate` target wiring,
`forge build` target wiring, provider detection behavior, resolver behavior,
runtime probes, MO2/GECK automation, or command execution.

Gate 203 keeps the same canonical command surface while adding the JIP LN
text-script source contract skeleton. It does not add aliases, new commands,
new CLI output fields, `forge generate` target wiring, `forge build` target
wiring, provider detection behavior, resolver behavior, runtime probes,
MO2/GECK automation, or command execution.

Gate 204 keeps the same canonical command surface while adding JIP source
semantic validation. It does not add aliases, new commands, new CLI output
fields, `forge generate` target wiring, `forge build` target wiring, provider
detection behavior, resolver behavior, runtime probes, MO2/GECK automation,
or command execution.

Gate 205 keeps the same canonical command surface while adding opaque JIP
body/source-line source records. It does not add aliases, new commands, new
CLI output fields, `forge generate` target wiring, `forge build` target
wiring, provider detection behavior, resolver behavior, runtime probes,
MO2/GECK automation, or command execution.

Gate 206 keeps the same canonical command surface while adding JIP source-line
byte-budget validation. It does not add aliases, new commands, new CLI output
fields, `forge generate` target wiring, `forge build` target wiring, provider
detection behavior, resolver behavior, runtime probes, MO2/GECK automation,
or command execution.

Gate 207 keeps the same canonical command surface while adding duplicate JIP
output filename validation. It does not add aliases, new commands, new CLI
output fields, `forge generate` target wiring, `forge build` target wiring,
provider detection behavior, resolver behavior, runtime probes, MO2/GECK
automation, or command execution.

Gate 208 keeps the same canonical command surface while adding an internal
non-emitting JIP generation planner. It does not add aliases, new commands,
new CLI output fields, `forge generate` target wiring, `forge build` target
wiring, provider detection behavior, resolver behavior, runtime probes,
MO2/GECK automation, or command execution.

Gate 209 keeps the same canonical command surface while adding an internal
in-memory JIP text renderer. It does not add aliases, new commands, new CLI
output fields, `forge generate` target wiring, `forge build` target wiring,
provider detection behavior, resolver behavior, runtime probes, MO2/GECK
automation, or command execution.

Gate 210 keeps the same canonical command surface while adding internal JIP
generated-file emission under `generated/jip-scripts`. It does not add
aliases, new commands, new CLI output fields, `forge generate` target wiring,
`forge build` target wiring, provider detection behavior, resolver behavior,
runtime probes, MO2/GECK automation, or command execution.

Gate 211 keeps the same canonical command surface while adding internal JIP
emission manifest, checksum, and digest evidence under `generated/jip-scripts`.
It does not add aliases, new commands, new CLI output fields,
`forge generate` target wiring, `forge build` target wiring, provider
detection behavior, resolver behavior, runtime probes, MO2/GECK automation,
or command execution.

Gate 212 keeps the same canonical command surface while adding internal JIP
emission manifest schema validation. It does not add aliases, new commands,
new CLI output fields, `forge generate` target wiring, `forge build` target
wiring, provider detection behavior, resolver behavior, runtime probes,
MO2/GECK automation, or command execution.

Gate 213 keeps the same canonical command surface while adding internal JIP
emission checksum sidecar revalidation. It does not add aliases, new commands,
new CLI output fields, `forge generate` target wiring, `forge build` target
wiring, provider detection behavior, resolver behavior, runtime probes,
MO2/GECK automation, or command execution.

Gate 214 keeps the same canonical command surface while adding the canonical
`forge generate --target jip-scripts` target. It adds JIP generate JSON/text
output for generated script, manifest, checksum, diagnostic, and digest
evidence without adding aliases, new commands, `forge build --target
jip-scripts`, provider detection behavior, resolver behavior, runtime probes,
MO2/GECK automation, package staging, live Data mutation, or external command
execution.

Gate 215 keeps the same canonical command surface while adding the canonical
`forge build --target jip-scripts` target. It adds JIP build JSON/text output
for dist script, build-manifest, checksum, source digest, output digest, and
diagnostic evidence without adding aliases, new commands, package staging,
runtime probes, MO2/GECK automation, live Data mutation, external command
execution, FOMOD generation, or archive generation.

Gate 216 keeps the same canonical command surface while adding the canonical
`forge package --target jip-scripts` target. It adds JIP package JSON/text
output for staged script, package-manifest, install-plan, build-manifest,
checksum, source digest, output digest, and diagnostic evidence without
adding aliases, new commands, JIP verify-existing mode, runtime probes,
MO2/GECK automation, live Data mutation, external command execution, FOMOD
generation, or archive generation.

Gate 217 keeps the same canonical command surface while closing the current
JIP command lane. It does not add aliases, new commands, output formats,
verify-existing behavior, runtime probes, MO2/GECK automation, xEdit
execution, plugin patch generation, plugin mutation, or external command
execution.

Gate 218 keeps the same canonical command surface. It adds xEdit audit source
contract validation and a non-emitting adapter planner only; it does not add
`forge generate --target xedit-audit`, aliases, output formats, xEdit process
execution, plugin patch generation, plugin mutation, MO2/GECK automation, or
external command execution.

Gate 219 still keeps the same canonical command surface. It adds internal
generation support for xEdit audit script scaffold files, but does not expose
`forge generate --target xedit-audit`, add aliases, launch xEdit, parse
reports, patch plugins, mutate plugins, automate MO2/GECK, or execute external
commands.

Gate 220 still keeps the same canonical command surface. It adds internal
manifest and checksum evidence for generated xEdit audit scaffolds, but does
not expose `forge generate --target xedit-audit`, add aliases, launch xEdit,
parse reports, patch plugins, mutate plugins, automate MO2/GECK, or execute
external commands.

Gate 221 uses the existing canonical `forge generate` command surface to add
the `xedit-audit` target. It does not add aliases, new commands, new output
formats, `forge build --target xedit-audit`, xEdit process execution, report
parsing, plugin patch generation, plugin mutation, MO2/GECK automation, or
external command execution.

Gate 222 keeps the same canonical command surface. It adds internal synthetic
xEdit audit report parser contracts only; it does not add commands, aliases,
new formats, parser CLI wiring, `forge build --target xedit-audit`, xEdit
process execution, real report parsing, plugin patch generation, plugin
mutation, MO2/GECK automation, or external command execution.

Gate 223 keeps the same canonical command surface. It adds internal machine
and human handoff projection for parsed synthetic xEdit audit reports only; it
does not add commands, aliases, new CLI formats, parser CLI wiring, handoff
file emission, `forge build --target xedit-audit`, xEdit process execution,
real report parsing, plugin patch generation, plugin mutation, MO2/GECK
automation, or external command execution.

Gate 224 keeps the same canonical command surface. It adds internal generated
file emission for xEdit audit report handoff JSON/text only; it does not add
commands, aliases, new CLI formats, parser CLI wiring, handoff
manifest/checksum sidecars, `forge build --target xedit-audit`, xEdit process
execution, real report parsing, plugin patch generation, plugin mutation,
MO2/GECK automation, or external command execution.

Gate 225 keeps the same canonical command surface. It adds internal manifest
and checksum sidecars for generated xEdit audit report handoff files only; it
does not add commands, aliases, new CLI formats, parser CLI wiring, sidecar
revalidation, `forge build --target xedit-audit`, xEdit process execution,
real report parsing, plugin patch generation, plugin mutation, MO2/GECK
automation, or external command execution.

Gate 226 keeps the same canonical command surface. It adds internal
revalidation for generated xEdit audit report handoff sidecars only; it does
not add commands, aliases, new CLI formats, parser CLI wiring, `forge build
--target xedit-audit`, xEdit process execution, real report parsing, plugin
patch generation, plugin mutation, MO2/GECK automation, or external command
execution.

Gate 227 uses the existing canonical `forge generate` command surface to add
the `xedit-audit-report-handoff` target. It does not add aliases, new
commands, new output formats, `forge build --target xedit-audit`, xEdit
process execution, report generation, real report parsing, plugin patch
generation, plugin mutation, MO2/GECK automation, or external command
execution.

Gate 228 keeps the same canonical command surface and closes the current xEdit
audit command lane. Existing xEdit generate targets remain available, but the
next implementation lane moves to the already-canonical `forge docs` command
instead of adding xEdit aliases, xEdit build/package targets, external command
execution, or AI behavior.

Gate 229 implements the already-canonical top-level `forge docs` command. It
adds docs JSON/text CLI output, `--project`, generated-output `--output`,
`--dry-run`, and local reference index evidence without adding aliases, new
formats, static site generation, watch mode, network publishing,
graph/explain/clean behavior, package/release behavior, external command
execution, or AI behavior.

Gate 230 keeps the same command surface and extends `forge docs` output with
per-schema reference page skeletons. It adds no aliases, new command names,
new output formats, static site generation, watch mode, network publishing,
graph/explain/clean behavior, package/release behavior, external command
execution, or AI behavior.

Gate 231 keeps the same command surface and extends `forge docs` output with
project registry reference page skeletons. It adds no aliases, new command
names, new output formats, static site generation, watch mode, network
publishing, graph/explain/clean behavior, package/release behavior, external
command execution, or AI behavior.

Gate 232 keeps the same command surface and extends `forge docs` output with
validation rule reference page skeletons. It adds no aliases, new command
names, new output formats, static site generation, watch mode, network
publishing, graph/explain/clean behavior, package/release behavior, external
command execution, or AI behavior.

Gate 233 keeps the same command surface and extends `forge docs` output with
built-in capability reference page skeletons. It adds no aliases, new command
names, new output formats, capability scan/explain behavior, provider detection
changes, provider-version parsing changes, static site generation, watch mode,
network publishing, graph/explain/clean behavior, package/release behavior,
external command execution, provider reference pages, or AI behavior.

Gate 234 keeps the same command surface and extends `forge docs` output with
built-in provider reference page skeletons. It adds no aliases, new command
names, new output formats, capability scan/explain behavior, provider detection
changes, provider-version parsing changes, static site generation, watch mode,
network publishing, graph/explain/clean behavior, package/release behavior,
external command execution, or AI behavior.

Gate 235 keeps the same command surface and extends `forge docs` output with
canonical command reference page skeletons for the existing ADR-010 command
surface. It adds no aliases, new command names, command behavior changes, new
output formats outside docs evidence, static site generation, watch mode,
network publishing, graph/explain/clean behavior, package/release behavior,
external command execution, or AI behavior.

Gate 236 keeps the same command surface and implements the canonical
`forge graph` command for project source graph evidence. It adds no aliases,
new command names, graph visualization formats, `--subject`, build planning
changes, package/release behavior, external command execution, or AI behavior.

Gate 237 keeps the same command surface and extends `forge graph` output with
declaration-only capability requirement graph nodes linked to the built-in
capability/provider catalogue. It adds no aliases, new command names, graph
visualization formats, `--subject`, capability scan behavior changes, provider
status resolution, build planning changes, package/release behavior, external
command execution, or AI behavior.

Gate 238 keeps the same command surface and extends `forge graph` output with
declaration-only generator target graph nodes linked to source registries and
generated/dist output boundaries. It adds no aliases, new command names, graph
visualization formats, `--subject`, generator execution, build planning
changes, capability scan behavior changes, provider status resolution,
package/release behavior, external command execution, or AI behavior.

Gate 239 keeps the same command surface and extends `forge graph` output with
declaration-only generated artifact expectation nodes linked to generator
targets and generated/dist output boundaries. It adds no aliases, new command
names, graph visualization formats, `--subject`, artifact existence checks,
generator execution, build planning changes, capability scan behavior changes,
provider status resolution, package/release behavior, external command
execution, or AI behavior.

Gate 240 keeps the same command surface and extends `forge graph` output with
declaration-only manifest provenance reference nodes linked to generator
targets, artifact expectations, and generated/dist output boundaries. It adds
no aliases, new command names, graph visualization formats, `--subject`,
generated manifest reads, artifact existence checks, generator execution,
build planning changes, capability scan behavior changes, provider status
resolution, package/release behavior, external command execution, or AI
behavior.

Gate 241 keeps the same command surface and closes the current `forge graph`
metadata lane. Existing `forge graph` behavior remains available, but ordinary
next-step work should move to top-level `forge explain` subject planning
instead of new graph aliases, graph visualization formats, generated manifest
reads, artifact existence checks, generator execution, build planning changes,
capability scan behavior changes, provider status resolution, package/release
behavior, external command execution, or AI behavior.

Gate 242 keeps the same command surface and adds top-level `forge explain`
subject-contract help plus reserved JSON metadata for diagnostic, target,
output, capability, and provenance subjects. At Gate 242, `forge explain` had
no executable subject parser and no aliases are introduced; the next command slice should start
`forge explain diagnostic <rule-id>` without changing graph, build, provider,
package/release, external command, or AI behavior.

Gate 243 keeps the same command surface and implements the diagnostic subject:
`forge explain diagnostic <rule-id>`. The subject emits family-level plain or
JSON explanation for reserved WastelandForge rule IDs. At Gate 243, other
explain subjects were still reserved; no aliases, graph
behavior, build behavior, provider behavior, package/release behavior,
external command behavior, or AI behavior are introduced.

Gate 244 keeps the same command surface and adds documented concrete rule
metadata to `forge explain diagnostic <rule-id>` while preserving
reserved-family fallback for valid IDs without embedded metadata. At Gate 244,
non-diagnostic explain subjects were still reserved; no aliases,
graph behavior, build behavior, provider behavior, package/release behavior,
external command behavior, or AI behavior are introduced.

Gate 245 keeps the same command surface and implements
`forge explain target <target-id>` for documented command targets. At Gate
245, output/capability/provenance explain subjects were still placeholders; no
aliases, graph
behavior, build behavior, provider behavior, package/release execution,
external command behavior, or AI behavior are introduced.

Gate 246 keeps the same command surface and implements
`forge explain output <generated-or-dist-path>` for documented generated/dist
output paths. At Gate 246, capability/provenance explain subjects were still
reserved; no aliases,
graph behavior, build behavior, provider behavior, package/release execution,
external command behavior, or AI behavior are introduced.

Gate 247 keeps the same command surface and implements
`forge explain capability <capability-id>` for built-in capability catalogue
metadata. Provenance remains reserved; no aliases, graph behavior, build
behavior, provider resolution, capability scan behavior changes,
package/release execution, external command behavior, runtime probes, or AI
behavior are introduced.

Gate 248 keeps the same command surface and implements
`forge explain provenance <manifest-or-output-path>` for documented
generated/dist paths. No aliases, graph behavior, build behavior, provider
resolution, capability scan behavior changes, package/release execution,
external command behavior, runtime probes, manifest reads, artifact reads, or
AI behavior are introduced.

Gate 249 keeps the same command surface and closes the top-level
`forge explain` subject lane. The next command lane is `forge clean` planning,
but no delete behavior, filesystem mutation, generated manifest reads, build
manifest reads, provenance sidecar reads, checksum reads, artifact existence
checks, graph behavior, build behavior, provider resolution, capability scan
behavior changes, package/release execution, external command behavior,
runtime probes, aliases, or AI behavior are introduced.

Gate 250 keeps the same command surface and implements a reserved
`forge clean` planning skeleton. Help documents `--generated`, `--dist`,
`--cache`, and `--all`; JSON output returns planned scopes, optional selected
scope metadata, report expectations, and false execution flags. No aliases,
delete behavior, filesystem mutation, generated manifest reads, build manifest
reads, provenance sidecar reads, checksum reads, artifact existence checks,
graph behavior, build behavior, provider resolution, capability scan behavior
changes, package/release execution, external command behavior, runtime probes,
or AI behavior are introduced.

Gate 251 keeps the same command surface and implements `forge clean` dry-run
path planning. Supported scopes emit text or JSON plans and the omitted scope
defaults to `generated`. `--all` reports severe-scope confirmation metadata but
does not delete anything. No aliases, filesystem mutation, generated manifest
reads, build manifest reads, provenance sidecar reads, checksum reads, artifact
existence checks, graph behavior, build behavior, provider resolution,
capability scan behavior changes, package/release execution, external command
behavior, runtime probes, or AI behavior are introduced.

Gate 252 keeps the same command surface and enforces all-scope confirmation
planning for `forge clean --all`. Missing `--yes` or `--confirm <project-id>`
returns exit code 6 and `status: refused`; providing both returns a dry-run
plan with exit code 0. No aliases, delete behavior, filesystem mutation,
project manifest reads, project-id validation, generated manifest reads, build
manifest reads, provenance sidecar reads, checksum reads, artifact existence
checks, graph behavior, build behavior, provider resolution, capability scan
behavior changes, package/release execution, external command behavior,
runtime probes, or AI behavior are introduced.
