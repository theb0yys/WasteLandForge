# ADR-011 - Validation and Release Governance Model

Status: Accepted
Research classification: Documented
Source: `WasteLandForge/research/WastelandForge Validation Testing CI Release and Governance-deep-research-report.md`

## Decision

WastelandForge uses layered validation, deterministic fixture-backed testing, GitHub Actions CI centered on Windows and .NET, immutable published schemas, SemVer-governed version streams, mandatory local build manifests, optional SLSA-style release provenance, least-privilege repository governance, and contribution rules that do not require AI.

## Consequences

- Validation is staged from load/source checks through release validation.
- Canonical issue data is JSON.
- SARIF is the diagnostics exchange format.
- TRX is the native .NET test output.
- Public fixtures must be synthetic and redistributable.
- GitHub Actions must include a mandatory Windows lane once CI exists.
- Release publish flows require governance checks and explicit approval.

## Gate Impact

Gate 7 creates fixture/test structure. Gate 8 creates CI and governance
baseline. Gate 9 creates release dry-run and build manifest evidence. Gate 61
extends local build-manifest evidence to `forge build --target reports` and
keeps release publishing gated separately. Gate 62 extends deterministic
fixture-backed tests and local manifest/checksum evidence to the `mcm-json`
generate/build target while keeping release publishing gated separately.
Gate 63 adds local generated-output schema validation for MCM Extender JSON
and extends fixture-backed tests around that validation path.
Gate 64 extends fixture-backed tests for generated translation files,
runtime requirement pass-through, and duplicate translation output refusal.
Gate 65 extends fixture-backed tests for generated checkbox and string-toggle
MCM Extender options.
Gate 66 extends fixture-backed tests for generated keybind MCM Extender
options.
Gate 67 extends fixture-backed tests for generated header MCM Extender
options.
Gate 68 extends fixture-backed tests for generated image MCM Extender options.
Gate 69 extends fixture-backed tests for MCM image asset-reference
diagnostics.
Gate 70 extends fixture-backed tests for staged MCM texture asset outputs,
manifest evidence, output digests, and build checksums.
Gate 71 extends fixture-backed tests for package-manifest entries, payload
digests, and checksum evidence.
Gate 72 extends fixture-backed tests for ZIP package creation, sorted archive
entries, normalized timestamps, package-manifest archive evidence, and
checksum evidence.
Gate 73 extends fixture-backed tests for canonical `forge package` execution,
package-specific provenance, archive evidence, and checksum coverage.
Gate 74 extends schema, back-compat, unit, and golden CLI tests for
`package-manifest/0.1.0`, package validation manifest evidence, and archive
entry checks.
Gate 75 extends unit and golden CLI tests for install-preview report evidence,
output digests, and build/package checksum coverage.
Gate 76 extends schema, back-compat, unit, and golden CLI tests for
`install-preview/0.1.0` and install-preview schema evidence.
Gate 77 extends unit and golden CLI tests for install-preview Markdown summary
evidence, output digests, and build/package checksum coverage.
Gate 78 extends unit and golden CLI tests for package-verification report
evidence, output digests, human/JSON CLI output, and build/package checksum
coverage.
Gate 79 extends schema, back-compat, unit, and golden CLI tests for
`package-verification/0.1.0` and package-verification schema evidence.
Gate 80 extends unit and golden CLI tests for package-verification Markdown
summary evidence, manifest summary paths, output digests, CLI output, and
build/package checksum coverage.
Gate 81 extends unit and golden CLI tests for package-verification
cross-check evidence in local manifests and Markdown summaries.
Gate 82 extends unit tests for reusable package-verification evidence
validator mismatch coverage while preserving the existing golden CLI command
surface.
Gate 83 extends unit tests for the file-based package-verification verifier
over generated MCM JSON evidence files while preserving the existing golden
CLI command surface.
Gate 84 extends unit tests for payload digest recomputation over generated MCM
JSON package payload files while preserving the existing golden CLI command
surface.
Gate 85 extends unit tests for archive digest recomputation over generated MCM
JSON package archives while preserving the existing golden CLI command
surface.
Gate 86 extends unit tests for archive entry-name revalidation over generated
MCM JSON package archives while preserving the existing golden CLI command
surface.
Gate 87 records the package-verification command-surface decision without
changing product code or the existing golden CLI command surface.
Gate 88 extends golden CLI tests for successful existing package evidence
verification and edited payload failure through
`forge package --target mcm-json --verify-existing`.
Gate 89 extends golden CLI tests for SARIF and GitHub diagnostic projections
from `forge package --target mcm-json --verify-existing` while preserving
offline-first package evidence verification.
Gate 90 extends golden CLI tests for Markdown summary output from
`forge package --target mcm-json --verify-existing` while preserving
offline-first package evidence verification.
Gate 91 extends unit and golden CLI tests for checksum-file revalidation from
`forge package --target mcm-json --verify-existing` while preserving
offline-first package evidence verification.
Gate 92 extends unit and golden CLI tests for build-manifest content
revalidation from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 93 extends unit and golden CLI tests for install-preview summary content
revalidation from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 94 extends unit and golden CLI tests for install-preview/package-manifest
entry content cross-checking from
`forge package --target mcm-json --verify-existing` while preserving
offline-first package evidence verification.
Gate 95 extends unit and golden CLI tests for package-verification summary
content revalidation from `forge package --target mcm-json --verify-existing`
while preserving offline-first package evidence verification.
Gate 96 extends unit and golden CLI tests for package-verification JSON check
content revalidation from
`forge package --target mcm-json --verify-existing` while preserving
offline-first package evidence verification.
Gate 97 extends unit and golden CLI tests for package-verification JSON
metadata content revalidation from
`forge package --target mcm-json --verify-existing` while preserving
offline-first package evidence verification.
Gate 98 extends unit and golden CLI tests for package-verification archive
detail content revalidation from
`forge package --target mcm-json --verify-existing` while preserving
offline-first package evidence verification.
Gate 99 extends unit and golden CLI tests for install-preview archive detail
content revalidation from
`forge package --target mcm-json --verify-existing` while preserving
offline-first package evidence verification.
Gate 100 extends unit and golden CLI tests for package-manifest archive detail
content revalidation from
`forge package --target mcm-json --verify-existing` while preserving
offline-first package evidence verification.
Gate 101 extends unit and golden CLI tests for archive detail cross-report
consistency revalidation from
`forge package --target mcm-json --verify-existing` while preserving
offline-first package evidence verification.
Gate 102 extends unit and golden CLI tests for package archive presence
revalidation from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 103 extends unit and golden CLI tests for checksum unexpected-entry
revalidation from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 104 extends unit and golden CLI tests for checksum duplicate-entry
revalidation from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 105 extends unit and golden CLI tests for checksum canonical-order
revalidation from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 106 extends unit and golden CLI tests for checksum digest canonical-casing
revalidation from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 107 extends unit and golden CLI tests for checksum line-ending and
trailing-newline revalidation from
`forge package --target mcm-json --verify-existing` while preserving
offline-first package evidence verification.
Gate 108 extends unit and golden CLI tests for checksum path separator
canonicalization revalidation from
`forge package --target mcm-json --verify-existing` while preserving
offline-first package evidence verification.
Gate 109 extends unit and golden CLI tests for checksum blank-line
revalidation from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 110 extends unit and golden CLI tests for checksum entry spacing
canonicalization revalidation from
`forge package --target mcm-json --verify-existing` while preserving
offline-first package evidence verification.
Gate 111 extends unit and golden CLI tests for checksum path casing
canonicalization revalidation from
`forge package --target mcm-json --verify-existing` while preserving
offline-first package evidence verification.
Gate 112 extends unit and golden CLI tests for checksum case-insensitive
duplicate revalidation from
`forge package --target mcm-json --verify-existing` while preserving
offline-first package evidence verification.
Gate 113 extends unit and golden CLI tests for checksum malformed-entry format
revalidation from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 114 extends unit and golden CLI tests for checksum path containment
revalidation from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 115 extends unit and golden CLI tests for checksum comment-line rejection
revalidation from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 116 extends schema, back-compat, unit, and golden CLI tests for
`install-plan/0.1.0`, generated install-plan evidence, manifest/checksum
coverage, and offline-first MCM package export planning.
Gate 117 extends unit and golden CLI tests for install-plan JSON and Markdown
content revalidation from `forge package --target mcm-json --verify-existing`
while preserving offline-first package evidence verification.
Gate 118 extends unit and golden CLI tests for install-plan schema
revalidation from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 119 extends unit and golden CLI tests for package-manifest schema
revalidation from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 120 extends unit and golden CLI tests for install-preview schema
revalidation from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 121 extends unit and golden CLI tests for package-verification schema
revalidation from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 122 extends golden CLI tests for SARIF, GitHub annotation, and Markdown
summary projections of schema-gated package verify-existing diagnostics while
preserving offline-first package evidence verification.
Gate 123 extends unit and golden CLI tests for missing package evidence-file
diagnostics from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 124 extends unit and golden CLI tests for malformed package evidence-file
diagnostics from `forge package --target mcm-json --verify-existing` while
preserving offline-first package evidence verification.
Gate 125 extends golden CLI tests for SARIF, GitHub annotation, and Markdown
summary projections of malformed package evidence diagnostics while preserving
offline-first package evidence verification.
Gate 126 adds no new verifier behavior or test category. It records the MCM
Extender lane as complete for now and directs the next fixture-backed coverage
toward capability scanner and Doctor-style environment reporting.
Gate 127 extends golden CLI coverage for the separate capability scan and
explanation reports. It adds no new diagnostic rule ID and keeps tests
synthetic, deterministic, and offline-first.
Gate 128 extends golden CLI coverage for `forge doctor export` JSON and
output-file behavior. It keeps provider evidence synthetic and temp-only,
redacts local paths, and adds no AI requirement or new diagnostic rule ID.
Gate 129 extends golden CLI coverage for `WF-CAP-*` JSON, SARIF, and GitHub
diagnostic projection from `forge capabilities scan --project`. It keeps
provider evidence synthetic and temp-only and adds no runtime, MO2, GECK,
network, or AI requirement.
Gate 130 extends unit and golden CLI coverage for provider evidence detail in
canonical diagnostic JSON, SARIF result properties, GitHub annotations,
capability scan project requirement JSON, and Doctor export redaction. It
keeps provider evidence synthetic and temp-only and adds no runtime, MO2,
GECK, network, or AI requirement.
Gate 131 extends golden CLI coverage for grouped provider evidence in
`forge capabilities explain` JSON and text output. It keeps provider evidence
synthetic and temp-only and adds no runtime, MO2, GECK, network, or AI
requirement.
Gate 132 extends golden CLI coverage for wrong-scope provider evidence in
`forge capabilities scan` JSON, SARIF, and GitHub output. It keeps provider
evidence synthetic and temp-only and adds no runtime, MO2, GECK, network, or
AI requirement.
Gate 133 extends golden CLI coverage for `forge capabilities explain
--project` JSON, plain, provider-target filtering, and project read failure
paths. It keeps provider evidence synthetic and temp-only and adds no runtime,
MO2, GECK, network, or AI requirement.
Gate 134 extends golden CLI coverage for `forge capabilities explain
--project` diagnostic handoff JSON and plain output. It reuses existing
synthetic project fixtures and temp-only provider evidence and adds no
runtime, MO2, GECK, network, or AI requirement.
Gate 135 extends golden CLI coverage for `forge doctor export` top-level
summary and index JSON plus plain output. It reuses existing synthetic project
fixtures and temp-only provider evidence and adds no runtime, MO2, GECK,
network, or AI requirement.
Gate 136 extends golden CLI coverage for `forge doctor export`
`index.diagnostics` JSON plus plain output. It reuses existing synthetic
project fixtures and temp-only provider evidence and adds no runtime, MO2,
GECK, network, or AI requirement.
Gate 137 extends golden CLI coverage for `forge doctor export`
`index.requirements` JSON plus plain output. It reuses existing synthetic
project fixtures and temp-only provider evidence and adds no runtime, MO2,
GECK, network, or AI requirement.
Gate 138 extends golden CLI coverage for `forge doctor export`
`index.actions` JSON plus plain output. It reuses existing synthetic project
fixtures and temp-only provider evidence and adds no runtime, MO2, GECK,
network, or AI requirement.
Gate 139 extends golden CLI coverage for `forge doctor export`
`index.openQuestionDetails` JSON plus plain output while preserving
`index.openQuestions`. It reuses existing synthetic project fixtures and
temp-only provider evidence and adds no runtime, MO2, GECK, network, or AI
requirement.
Gate 140 extends golden CLI coverage for `forge doctor export`
`index.providerStatuses` JSON plus plain output. It reuses existing synthetic
project fixtures and temp-only provider evidence and adds no runtime, MO2,
GECK, network, or AI requirement.
Gate 141 extends golden CLI coverage for `forge doctor export`
`index.capabilityStatuses` JSON plus plain output. It reuses existing
synthetic project fixtures and temp-only provider evidence and adds no
runtime, MO2, GECK, network, or AI requirement.
Gate 142 extends golden CLI coverage for `forge doctor export`
`index.doctorAreaStatuses` JSON plus plain output. It reuses existing
synthetic project fixtures and temp-only provider evidence and adds no
runtime, MO2, GECK, network, or AI requirement.
Gate 143 extends golden CLI coverage for `forge doctor export`
`index.cataloguePolicy` JSON plus plain output. It reuses existing synthetic
project fixtures, temp-only provider evidence, and built-in catalogue
open-question text and adds no runtime, MO2, GECK, network, or AI requirement.
Gate 144 extends golden CLI coverage for `forge capabilities scan`
`doctor.index.areaStatuses` JSON plus plain output. It reuses existing
synthetic project fixtures and temp-only provider evidence and adds no
runtime, MO2, GECK, network, or AI requirement.
Gate 145 extends golden CLI coverage for `forge capabilities scan`
`index.providerStatuses` and `index.capabilityStatuses` JSON plus plain
output. It reuses existing synthetic project fixtures and temp-only provider
evidence and adds no runtime, MO2, GECK, network, or AI requirement.
Gate 146 extends golden CLI coverage for `forge capabilities scan`
`index.actions` JSON plus plain output. It reuses existing synthetic project
fixtures and built-in Doctor actions and adds no runtime, MO2, GECK, network,
or AI requirement.
Gate 147 extends golden CLI coverage for `forge capabilities scan`
`index.requirements` JSON plus plain output. It reuses existing synthetic
project fixtures and existing requirement resolution output and adds no
runtime, MO2, GECK, network, or AI requirement.
Gate 148 extends golden CLI coverage for `forge capabilities scan`
`index.diagnostics` JSON plus plain output. It reuses existing synthetic
project fixtures and existing diagnostic projection output and adds no
runtime, MO2, GECK, network, or AI requirement.
Gate 149 extends golden CLI coverage for `forge capabilities scan`
`index.cataloguePolicy` JSON plus plain output. It reuses existing synthetic
fixtures and existing Doctor open-question output and adds no runtime, MO2,
GECK, network, or AI requirement.
Gate 150 extends golden CLI coverage for `forge capabilities scan`
`index.openQuestionDetails` JSON plus plain output. It reuses existing
synthetic fixtures and existing Doctor open-question output and adds no
runtime, MO2, GECK, network, or AI requirement.
Gate 151 extends golden CLI coverage for `forge capabilities explain`
`cataloguePolicy.openQuestionDetails` JSON plus plain output. It reuses
existing synthetic fixtures and existing Doctor open-question output and adds
no runtime, MO2, GECK, network, or AI requirement.
Gate 152 extends golden CLI coverage for `forge capabilities explain`
`cataloguePolicy.diagnosticHandoff` JSON plus plain output. It reuses existing
synthetic fixtures and existing Doctor open-question output and adds no
runtime, MO2, GECK, network, or AI requirement.
Gate 153 extends golden CLI coverage for `forge doctor export`
`index.cataloguePolicyDiagnosticHandoff` JSON plus plain output. It reuses
existing synthetic fixtures and existing Doctor open-question output and adds
no runtime, MO2, GECK, network, or AI requirement.
Gate 154 extends golden CLI coverage for `forge capabilities scan`
`index.cataloguePolicyDiagnosticHandoff` JSON plus plain output. It reuses
existing synthetic fixtures and existing Doctor open-question output and adds
no runtime, MO2, GECK, network, or AI requirement.
Gate 155 adds focused helper coverage for shared catalogue-policy diagnostic
handoff JSON shape and text indentation, and reruns the existing focused
golden CLI coverage for scan, explain, and Doctor export handoff output. It
adds no runtime, MO2, GECK, network, or AI requirement.
Gate 156 adds focused helper coverage for shared catalogue-policy
open-question detail JSON, source-type index JSON, and text indentation, and
reruns the existing focused golden CLI coverage for scan, explain, and Doctor
export output. It adds no runtime, MO2, GECK, network, or AI requirement.
Gate 157 adds focused helper coverage for shared catalogue-policy view
derivation, copied open-question inputs, and empty input behavior, and reruns
the existing focused golden CLI coverage for scan, explain, and Doctor export
output. It adds no runtime, MO2, GECK, network, or AI requirement.
Gate 158 adds focused helper coverage for Doctor action summary grouping, JSON
shape, text indentation, and empty output behavior, and extends golden CLI
coverage for scan and Doctor export action summaries. It adds no runtime,
MO2, GECK, network, or AI requirement.
Gate 159 adds focused helper coverage for provider evidence summary grouping,
JSON shape, text indentation, and empty output behavior, and extends golden
CLI coverage for scan and Doctor export evidence summaries. It adds no
runtime, MO2, GECK, network, or AI requirement.
Gate 160 adds focused helper coverage for requirement summary grouping, JSON
shape, text indentation, and empty output behavior, and extends golden CLI
coverage for scan and Doctor export requirement summaries. It adds no
runtime, MO2, GECK, network, or AI requirement.
Gate 161 adds focused helper coverage for diagnostic summary grouping, JSON
shape, text indentation, and empty output behavior, and extends golden CLI
coverage for scan and Doctor export diagnostic summaries. It adds no runtime,
MO2, GECK, network, or AI requirement.
Gate 162 adds focused helper coverage for provider inventory summary grouping,
JSON shape, text indentation, and empty output behavior, and extends golden
CLI coverage for scan and Doctor export provider inventory summaries. It adds
no runtime, MO2, GECK, network, or AI requirement.
Gate 163 adds focused helper coverage for Doctor area capability summary
grouping, JSON shape, text indentation, and empty output behavior, and extends
golden CLI coverage for scan and Doctor export Doctor area capability
summaries. It adds no runtime, MO2, GECK, network, or AI requirement.
Gate 164 extends golden CLI coverage for `forge doctor export --summary
<path>` Markdown sidecar output, summary redaction, and missing summary-path
usage errors. It adds no runtime, MO2, GECK, network, or AI requirement.
Gate 165 extends golden CLI coverage for `forge capabilities scan --summary
<path>` Markdown sidecar output, omission of local paths in that summary, and
missing summary-path usage errors. It adds no runtime, MO2, GECK, network, or
AI requirement.
Gate 166 extends golden CLI coverage for `forge doctor export --bundle <path>`
deterministic ZIP sidecar output, archive manifest and checksum entries,
stable ZIP timestamps, redaction of local paths inside archive payloads, and
missing bundle-path usage errors. It adds no runtime, MO2, GECK, network, or
AI requirement.
Gate 167 extends golden CLI coverage for `forge capabilities explain
--summary <path>` Markdown sidecar output, omission of local paths in that
summary, and missing summary-path usage errors. It adds no runtime, MO2, GECK,
network, or AI requirement.
Gate 168 extends golden CLI coverage for `forge doctor export --bundle <path>`
archives containing per-requirement explanation Markdown entries for
unavailable project requirements, manifest/checksum coverage for those
entries, stable ZIP timestamps, and omission of local paths inside the
supplemental Markdown. It adds no runtime, MO2, GECK, network, or AI
requirement.
Gate 169 extends golden CLI coverage for matching per-requirement explanation
JSON entries in `forge doctor export --bundle <path>` archives, including
manifest/checksum coverage, redacted project roots and evidence paths, stable
ZIP timestamps, and omission of local paths. It adds no runtime, MO2, GECK,
network, or AI requirement.
Gate 170 extends golden CLI coverage for requirement explanation index JSON
and Markdown entries in `forge doctor export --bundle <path>` archives,
including manifest/checksum coverage, entry-path references, summary counts,
stable ZIP timestamps, and omission of local paths. It adds no runtime, MO2,
GECK, network, or AI requirement.
Gate 171 extends golden CLI coverage for `README.md` entries in
`forge doctor export --bundle <path>` archives, including manifest/checksum
coverage, report and requirement index links, stable ZIP timestamps, and
omission of local paths. It adds no runtime, MO2, GECK, network, or AI
requirement.
Gate 172 extends golden CLI coverage for diagnostic index JSON and Markdown
entries in `forge doctor export --bundle <path>` archives, including
manifest/checksum coverage, README links, summary counts, stable ZIP
timestamps, and omission of local paths. It adds no runtime, MO2, GECK,
network, or AI requirement.
Gate 173 extends golden CLI coverage for action index JSON and Markdown
entries in `forge doctor export --bundle <path>` archives, including
manifest/checksum coverage, README links, summary counts, stable ZIP
timestamps, and omission of local paths. It adds no runtime, MO2, GECK,
network, or AI requirement.
Gate 174 extends golden CLI coverage for requirement index JSON and Markdown
entries in `forge doctor export --bundle <path>` archives, including
manifest/checksum coverage, README links, summary counts, stable ZIP
timestamps, and omission of local paths. It adds no runtime, MO2, GECK,
network, or AI requirement.
Gate 175 extends golden CLI coverage for provider index JSON and Markdown
entries in `forge doctor export --bundle <path>` archives, including
manifest/checksum coverage, README links, summary counts, provider evidence
summaries, stable ZIP timestamps, and omission of local paths. It adds no
runtime, MO2, GECK, network, or AI requirement.
Gate 176 extends golden CLI coverage for capability index JSON and Markdown
entries in `forge doctor export --bundle <path>` archives, including
manifest/checksum coverage, README links, summary counts, capability status
groups, stable ZIP timestamps, and omission of local paths. It adds no
runtime, MO2, GECK, network, or AI requirement.
Gate 177 extends golden CLI coverage for Doctor area index JSON and Markdown
entries in `forge doctor export --bundle <path>` archives, including
manifest/checksum coverage, README links, summary counts, area status groups,
stable ZIP timestamps, and omission of local paths. It adds no runtime, MO2,
GECK, network, or AI requirement.
Gate 178 extends golden CLI coverage for catalogue-policy index JSON and
Markdown entries in `forge doctor export --bundle <path>` archives, including
manifest/checksum coverage, README links, open-question counts, diagnostic
handoff counts, stable ZIP timestamps, and omission of local paths. It adds no
runtime, MO2, GECK, network, or AI requirement.
Gate 179 extends golden CLI coverage for summary index JSON and Markdown
entries in `forge doctor export --bundle <path>` archives, including
manifest/checksum coverage, README links, report summary counts, derived
summary counts, stable ZIP timestamps, and omission of local paths. It adds no
runtime, MO2, GECK, network, or AI requirement.
Gate 180 extends golden CLI coverage for evidence index JSON and Markdown
entries in `forge doctor export --bundle <path>` archives, including
manifest/checksum coverage, README links, evidence summary counts, compact
provider evidence entries, stable ZIP timestamps, and omission of local paths.
It adds no runtime, MO2, GECK, network, or AI requirement.
Gate 181 extends golden CLI coverage for redaction index JSON and Markdown
entries in `forge doctor export --bundle <path>` archives, including
manifest/checksum coverage, README links, redaction token and note counts,
stable ZIP timestamps, and omission of local paths. It adds no runtime, MO2,
GECK, network, or AI requirement.
Gate 182 extends golden CLI coverage for open-question index JSON and
Markdown entries in `forge doctor export --bundle <path>` archives, including
manifest/checksum coverage, README links, open-question counts, diagnostic
handoff counts, stable ZIP timestamps, and omission of local paths. It adds no
runtime, MO2, GECK, network, or AI requirement.
Gate 183 extends golden CLI coverage for scan-input index JSON and Markdown
entries in `forge doctor export --bundle <path>` archives, including
manifest/checksum coverage, README links, redacted input placeholders,
detector-family counts, runtime/MO2 flags, stable ZIP timestamps, and omission
of local paths. It adds no runtime, MO2, GECK, network, or AI requirement.
Gate 184 extends golden CLI coverage for bundle navigation index JSON and
Markdown entries in `forge doctor export --bundle <path>` archives, including
manifest/checksum coverage, README links, archive entry path coverage,
requirement-explanation path coverage, stable ZIP timestamps, and omission of
local paths. It adds no runtime, MO2, GECK, network, or AI requirement.
Gate 185 extends golden CLI coverage for triage index JSON and Markdown
entries in `forge doctor export --bundle <path>` archives, including
manifest/checksum coverage, README links, bundle-index coverage,
blocking/review/action counts, requirement-explanation review paths, stable
ZIP timestamps, and omission of local paths. It adds no runtime, MO2, GECK,
network, or AI requirement.
Gate 186 extends golden CLI coverage for primary Doctor export triage JSON,
plain text, and Markdown summary output, including blocking/review/action
counts, report-section references, bundle-path separation, and omission of
local paths. It adds no runtime, MO2, GECK, network, or AI requirement.
Gate 187 extends golden CLI coverage for Doctor triage command hints in
primary JSON, plain text, Markdown summaries, and ZIP archive triage entries,
including canonical command strings, placeholder usage, section/path
separation, and omission of local paths. It adds no runtime, MO2, GECK,
network, or AI requirement.
Gate 188 extends golden CLI coverage for Doctor triage worklist entries in
primary JSON, plain text, Markdown summaries, and ZIP archive triage entries,
including order, priority, command-hint links, section/path separation, and
omission of local paths. It adds no runtime, MO2, GECK, network, or AI
requirement.
Gate 189 extends golden CLI coverage for Doctor worklist summary metadata in
primary JSON, plain text, Markdown summaries, and ZIP archive triage entries,
including priority groups, section/path source groups, and omission of local
paths. It adds no runtime, MO2, GECK, network, or AI requirement.
Gate 190 extends golden CLI coverage for Doctor remediation status headers in
primary JSON, plain text, Markdown summaries, and ZIP archive triage entries,
including status, headline, first command, section/path separation, and
omission of local paths. It adds no runtime, MO2, GECK, network, or AI
requirement.
Gate 191 extends golden CLI coverage for human operator handoff sections in
plain text, Markdown summaries, and ZIP archive triage Markdown, including
copyable checklist commands, priority/source summaries, section/path
separation, and omission of local paths. It adds no runtime, MO2, GECK,
network, or AI requirement.
Gate 192 extends golden CLI coverage for `handoff-summary.md` in Doctor bundle
archives, including README, bundle-index, manifest, checksum, command-hint,
key-path, and local-path redaction coverage. It adds no runtime, MO2, GECK,
network, or AI requirement.
Gate 193 extends golden CLI coverage for scan-side operator handoff sections
in `forge capabilities scan` plain output and Markdown summaries, including
status, priority/source summaries, command hints, work items, and Markdown
local-path omission. It adds no runtime, MO2, GECK, network, or AI
requirement.
Gate 194 extends golden CLI coverage for explain-side operator handoff
sections in `forge capabilities explain` plain output and Markdown summaries,
including status, priority/source summaries, command hints, work items, and
Markdown local-path omission. It adds no runtime, MO2, GECK, network, or AI
requirement.
Gate 195 extends golden CLI coverage for Doctor bundle requirement
explanation Markdown handoff sections, generated archive notes, paired JSON
non-contract behavior, and Markdown local-path omission. It adds no runtime,
MO2, GECK, network, or AI requirement.
Gate 196 extends golden CLI coverage for provider-version declaration
metadata in catalogue list and capability explanation output. It adds no
runtime, MO2, GECK, network, external provider-version parser, resolver
change, release, or AI requirement.
Gate 197 extends golden CLI coverage for provider-version declaration
metadata in capability scan provider output, embedded Doctor scan JSON, and
Doctor bundle provider indexes. It adds no runtime, MO2, GECK, network,
external provider-version parser, resolver change, release, or AI requirement.
Gate 198 is documentation-only and adds no fixture, runtime, MO2, GECK,
network, external provider-version parser, resolver change, release, or AI
requirement. It records that later parser tests must use synthetic raw values
unless a private extended fixture lane is explicitly introduced.
Gate 199 adds synthetic parser unit coverage for provider-version `semver`,
`integer`, and `scaled-integer` values. It adds no real provider fixture,
runtime, MO2, GECK, network, external provider-version parser, resolver
change, release, or AI requirement.
Gate 200 adds documentation only for the parser contract and future
projection notes. It adds no test fixture, runtime, MO2, GECK, network,
external provider-version parser, resolver change, release, or AI
requirement.
Gate 201 adds synthetic unit coverage for provider-version parsed evidence.
It adds no real provider fixture, runtime, MO2, GECK, network, external
provider-version parser, resolver change, release, or AI requirement.
Gate 202 adds documentation only for the JIP LN text-script generator evidence
checkpoint. It adds no test fixture, generator test, schema test, runtime,
MO2, GECK, network, external tool execution, release, or AI requirement.
Future public JIP LN fixtures must remain synthetic and redistributable unless
explicit permission exists.

Gate 203 adds synthetic schema, back-compat, and semantic fixture coverage for
the JIP LN text-script source contract skeleton. It adds no runtime, MO2,
GECK, network, external tool execution, release, or AI requirement.

Gate 204 adds synthetic semantic fixture coverage for JIP source lifecycle
prefix and capability requirement validation. It adds no runtime, MO2, GECK,
network, external tool execution, release, or AI requirement.

Gate 205 adds synthetic schema and read-model fixture coverage for opaque JIP
body/source-line records. It adds no runtime, MO2, GECK, network, external
tool execution, release, or AI requirement.

Gate 206 adds synthetic semantic fixture coverage for source-line byte-budget
validation. It adds no runtime, MO2, GECK, network, external tool execution,
release, or AI requirement.

Gate 207 adds synthetic semantic fixture coverage for duplicate JIP output
filename validation. It adds no runtime, MO2, GECK, network, external tool
execution, release, or AI requirement.

Gate 208 adds focused unit coverage for non-emitting JIP generation planning
using existing synthetic fixtures. It adds no runtime, MO2, GECK, network,
external tool execution, release, or AI requirement.

Gate 209 adds focused unit coverage for in-memory JIP text rendering using
existing synthetic fixtures. It adds no runtime, MO2, GECK, network, external
tool execution, release, or AI requirement.

Gate 210 adds focused unit coverage for generated JIP file emission using
temp-copied synthetic fixtures. It adds no runtime, MO2, GECK, network,
external tool execution, release, or AI requirement.

Gate 211 adds focused unit coverage for generated JIP emission manifests,
checksums, and digest evidence using temp-copied synthetic fixtures. It
verifies package non-mutation flags, output digest content, checksum-file
exclusion from output digests, and validation-error no-manifest behavior
without adding real game assets, third-party mod files, local install
snapshots, GECK outputs, MO2 profiles, or external tool fixtures.

Gate 212 adds focused schema catalog and unit coverage for generated JIP
emission manifest schema validation. It verifies valid emitted manifests and
`WF-GEN-007` malformed-manifest diagnostics without adding real game assets,
third-party mod files, local install snapshots, GECK outputs, MO2 profiles, or
external tool fixtures.

Gate 213 adds focused unit coverage for generated JIP emission checksum
sidecar revalidation. It verifies clean generated checksum evidence,
`WF-GEN-008` digest drift diagnostics, missing-entry diagnostics, and
unexpected-entry diagnostics without adding real game assets, third-party mod
files, local install snapshots, GECK outputs, MO2 profiles, or external tool
fixtures.

Gate 214 adds focused CLI golden coverage for
`forge generate --target jip-scripts` and the unsupported
`forge build --target jip-scripts` boundary. It verifies generated-root script
emission, manifest/checksum evidence, CLI JSON output, output digest evidence,
and no live Data writes using synthetic redistributable fixtures only.

Gate 215 adds focused unit and CLI golden coverage for
`forge build --target jip-scripts`. It verifies dist script output,
build-manifest evidence, checksum evidence, dry-run no-write behavior,
`WF-BUILD-001` output containment, validation-error no-write behavior, and no
live Data writes using synthetic redistributable fixtures only.

Gate 216 adds focused unit and CLI golden coverage for
`forge package --target jip-scripts`. It verifies package staging under
`dist/jip-scripts/package/Data`, package-manifest evidence, install-plan
evidence, build-manifest evidence, checksum evidence, dry-run no-write
behavior, `WF-BUILD-001` output containment, validation-error no-write
behavior, and no live Data writes using synthetic redistributable fixtures
only.
Gate 218 adds fixture-backed xEdit audit validation without real xEdit,
plugin, MO2, GECK, or runtime fixtures. The public fixtures remain synthetic
and redistributable, and the new tests cover schema/catalog registration,
back-compat schema IDs, semantic `WF-SEM-044`, and non-emitting adapter
planning.

Gate 219 extends that fixture-backed coverage to generated scaffold files
using temp-copied synthetic fixtures. Tests assert generated-root containment,
content, output digests, no report output, no `Data` writes, and no-write
behavior when validation fails.

Gate 220 extends that coverage to generated manifest and checksum sidecars.
Tests assert `xedit-audit-script-manifest.json`, `checksums.sha256`, recorded
safety limits, expected report metadata, output digests, no report output, no
`Data` writes, and validation-error no-write behavior.

Gate 221 extends golden CLI coverage to `forge generate --target xedit-audit`.
Tests assert CLI JSON output, generated scaffold evidence, manifest/checksum
files, no report output, no `Data` writes, `--output` rejection, and
`forge build --target xedit-audit` rejection.

Gate 222 extends unit coverage to the synthetic xEdit audit report parser
contract. Tests assert successful synthetic report parsing, typed safety,
summary, record, and finding evidence, `WF-GEN-009` for missing reports,
`WF-GEN-009` for malformed JSON, no scaffold/sidecar emission, and no `Data`
writes.

Gate 223 extends unit coverage to xEdit audit report handoff projection.
Tests assert machine JSON and LF human text projection for successful
synthetic reports, `WF-GEN-009` diagnostic projection for missing reports, no
scaffold/manifest/checksum writes, no handoff file emission, and no `Data`
writes.

Gate 224 extends unit coverage to xEdit audit report handoff file emission.
Tests assert generated JSON/text handoff files, UTF-8 without BOM, LF line
endings, output digest records, diagnostic no-write behavior, no
manifest/checksum sidecars, and no `Data` writes.

Gate 225 extends unit coverage to xEdit audit report handoff manifest and
checksum sidecars. Tests assert manifest content, safety flags, generated file
metadata, output digest metadata, dedicated checksum content, no scaffold
checksum overwrite, diagnostic no-write behavior, and no `Data` writes.

Gate 226 extends unit coverage to xEdit audit report handoff sidecar
revalidation. Tests assert clean generated sidecars, `WF-GEN-010` diagnostics
for edited checksum digests, missing checksum entries, and unexpected checksum
entries, and no `Data` writes.

Gate 227 extends golden CLI coverage to
`forge generate --target xedit-audit-report-handoff`. Tests assert JSON output,
generated handoff JSON/text files, manifest/checksum evidence, missing-report
`WF-GEN-009` diagnostics, unsupported
`forge build --target xedit-audit-report-handoff`, no scaffold output, and no
`Data` writes.

Gate 228 is a planning/routing closeout gate. It adds no new runtime behavior
or test project coverage; validation remains normal build/test smoke plus
documentation and routing consistency checks.

Gate 229 extends fixture-backed golden CLI coverage to `forge docs`. Tests
verify reference index output, manifest/checksum evidence, dry-run no-write
behavior, `WF-GEN-001` output containment diagnostics, and no live `Data`
writes while keeping docs generation offline-first.

Gate 230 extends fixture-backed golden CLI coverage to `forge docs` schema
reference page skeletons. Tests verify planned and written schema reference
JSON/Markdown outputs, reference index links, manifest/checksum evidence,
dry-run no-write behavior, and no live `Data` writes while keeping docs
generation offline-first.

Gate 231 extends fixture-backed golden CLI coverage to `forge docs` project
registry reference page skeletons. Tests verify planned and written registry
reference JSON/Markdown outputs, reference index links, manifest/checksum
evidence, dry-run no-write behavior, JSON top-level property summaries, and no
live `Data` writes while keeping docs generation offline-first.

Gate 232 extends fixture-backed golden CLI coverage to `forge docs` validation
rule reference page skeletons. Tests verify planned and written rule reference
JSON/Markdown outputs, reference index links, manifest/checksum evidence,
dry-run no-write behavior, reserved rule family metadata, and no live `Data`
writes while keeping docs generation offline-first.

Gate 233 extends fixture-backed golden CLI coverage to `forge docs` built-in
capability reference page skeletons. Tests verify planned and written
capability reference JSON/Markdown outputs, reference index links,
manifest/checksum evidence, dry-run no-write behavior, catalogue metadata, and
no live `Data` writes while keeping docs generation offline-first.

Gate 234 extends fixture-backed golden CLI coverage to `forge docs` built-in
provider reference page skeletons. Tests verify planned and written provider
reference JSON/Markdown outputs, reference index links, manifest/checksum
evidence, dry-run no-write behavior, catalogue metadata, and no live `Data`
writes while keeping docs generation offline-first.

Gate 235 extends fixture-backed golden CLI coverage to `forge docs` canonical
command reference page skeletons. Tests verify planned and written command
reference JSON/Markdown outputs, reference index links, manifest/checksum
evidence, dry-run no-write behavior, command metadata, and no live `Data`
writes while keeping docs generation offline-first.

Gate 236 extends fixture-backed golden CLI coverage to `forge graph` project
source graph evidence. Tests verify planned and written graph JSON/Markdown
outputs, graph manifest/checksum evidence, dry-run no-write behavior,
generated-output containment diagnostics, graph metadata, and no live `Data`
writes while keeping graph generation offline-first.

Gate 237 extends fixture-backed golden CLI coverage to `forge graph`
capability requirement graph evidence. Tests verify requirement nodes,
catalogue capability/provider nodes, catalogue edges, graph manifest/checksum
evidence, dry-run no-write behavior, no-scan/no-provider-resolution execution
flags, and no live `Data` writes while keeping graph generation offline-first.

Gate 238 extends fixture-backed golden CLI coverage to `forge graph` generator
target graph evidence. Tests verify generator target nodes, source-to-target
edges, generated/dist output boundary edges, generated evidence handoff edges,
graph manifest/checksum evidence, dry-run no-write behavior, no-execution
flags, and no live `Data` writes while keeping graph generation offline-first.

Gate 239 extends fixture-backed golden CLI coverage to `forge graph` generated
artifact expectation graph evidence. Tests verify artifact expectation nodes,
target-to-expectation edges, expectation-to-boundary edges, graph
manifest/checksum evidence, dry-run no-write behavior, no-existence-check
flags, and no live `Data` writes while keeping graph generation offline-first.

Gate 240 extends fixture-backed golden CLI coverage to `forge graph` manifest
provenance reference graph evidence. Tests verify manifest reference nodes,
target-to-manifest edges, manifest-to-artifact edges, manifest-to-boundary
edges, graph manifest/checksum evidence, dry-run no-write behavior,
no-manifest-read flags, and no live `Data` writes while keeping graph
generation offline-first.

Gate 241 is a planning/routing closeout gate. It adds no new runtime behavior,
fixture corpus, or golden graph output; validation remains normal build/test
smoke plus documentation and routing consistency checks while preserving the
offline-first and AI-optional boundary.

Gate 242 extends fixture-backed golden CLI coverage for top-level
`forge explain` planning. Tests verify help lists the planned subject contract
and reserved JSON lists planned subjects with false execution flags while the
command remains reserved, offline-first, and AI-optional.

Gate 243 extends fixture-backed golden CLI coverage for
`forge explain diagnostic <rule-id>`. Tests verify plain family-level
explanation output, JSON output with false execution flags, invalid rule ID
usage JSON, and continued reserved behavior for non-diagnostic explain
subjects while preserving offline-first and AI-optional behavior.

Gate 244 extends fixture-backed golden CLI coverage for documented rule
metadata in `forge explain diagnostic <rule-id>`. Tests verify concrete rule
metadata in plain and JSON output, reserved-family fallback for valid IDs
without embedded metadata, and false execution flags while preserving
offline-first and AI-optional behavior.

Gate 245 extends fixture-backed golden CLI coverage for
`forge explain target <target-id>`. Tests verify documented target metadata in
plain and JSON output, usage JSON for unknown target IDs, false execution
flags, and continued reserved behavior for output/capability/provenance
subjects while preserving offline-first and AI-optional behavior.

Gate 246 extends fixture-backed golden CLI coverage for
`forge explain output <generated-or-dist-path>`. Tests verify documented
output path classification in plain and JSON output, usage JSON for unknown
output paths, false execution flags, and continued reserved behavior for
capability/provenance subjects while preserving offline-first and AI-optional
behavior.

Gate 247 extends fixture-backed golden CLI coverage for
`forge explain capability <capability-id>`. Tests verify built-in capability
catalogue metadata in plain and JSON output, usage JSON for unknown capability
IDs, false execution flags, and continued reserved behavior for provenance
while preserving offline-first and AI-optional behavior.

Gate 248 extends fixture-backed golden CLI coverage for
`forge explain provenance <manifest-or-output-path>`. Tests verify provenance
boundary planning in plain and JSON output, usage JSON for unknown provenance
paths, false execution flags, and unknown-subject reserved JSON while
preserving offline-first and AI-optional behavior.

Gate 249 adds no new fixture-backed test surface. It closes the documented
top-level `forge explain` command slice, preserves the Gate 243 through Gate
248 explain golden coverage as regression evidence, and routes the next lane
to `forge clean` planning while preserving offline-first and AI-optional
behavior.

Gate 250 extends fixture-backed golden CLI coverage for `forge clean` planning.
Tests verify clean help, reserved JSON scope metadata, false execution flags,
usage JSON for unsupported scopes, and no filesystem mutation of a synthetic
generated file while preserving offline-first and AI-optional behavior.

Gate 251 extends fixture-backed golden CLI coverage for `forge clean` dry-run
path planning. Tests verify generated, default-generated, and all-scope path
plans, usage JSON for unsupported scopes, false execution flags, and no
filesystem mutation of synthetic generated, dist, or cache files while
preserving offline-first and AI-optional behavior.

Gate 252 extends fixture-backed golden CLI coverage for `forge clean --all`
confirmation/refusal planning. Tests verify exit code 6 with `status: refused`
for unconfirmed all-scope plans, exit code 0 with both `--yes` and
`--confirm <project-id>`, false execution flags, and no filesystem mutation of
synthetic generated, dist, or cache files while preserving offline-first and
AI-optional behavior.

Gate 253 extends fixture-backed golden CLI coverage for explicit
`forge clean --generated` execution. Tests verify removed-path reporting,
missing-root reporting, generated dry-run preservation, default generated-scope
non-mutation, unchanged all-scope refusal behavior, and synthetic filesystem
fixtures only while preserving offline-first and AI-optional behavior.

Gate 254 extends fixture-backed golden CLI coverage for explicit
`forge clean --dist` execution. Tests verify removed-path reporting,
missing-root reporting, dist dry-run preservation, generated-scope regression
behavior, unchanged all-scope refusal behavior, and synthetic filesystem
fixtures only while preserving offline-first and AI-optional behavior.

Gate 255 extends fixture-backed golden CLI coverage for explicit
`forge clean --cache` execution. Tests verify removed-path reporting,
missing-root reporting, cache dry-run preservation, generated/dist regression
behavior, unchanged all-scope refusal behavior, and synthetic filesystem
fixtures only while preserving offline-first and AI-optional behavior.

Gate 256 extends fixture-backed golden CLI coverage for confirmed
`forge clean --all` execution. Tests verify all-root removed-path reporting,
all-root missing reporting, all-scope dry-run preservation, unchanged
all-scope refusal behavior, selected-root regression behavior, and synthetic
filesystem fixtures only while preserving offline-first and AI-optional
behavior.

Gate 257 extends fixture-backed golden CLI coverage for all-scope project-ID
confirmation validation. Tests verify matching JSON and YAML manifest identity
paths, mismatched project-ID refusal, missing-manifest refusal, unchanged
all-scope dry-run behavior, selected-root regression behavior, and synthetic
filesystem fixtures only while preserving offline-first and AI-optional
behavior.

Gate 258 extends fixture-backed golden CLI coverage for active build/cache
lock safety. Tests verify explicit cache clean refusal, manifest-confirmed all
clean refusal, filesystem preservation when `.wastelandforge/cache/build.lock`
exists, selected-root regression behavior, and synthetic filesystem fixtures
only while preserving offline-first and AI-optional behavior.

Gate 259 is a planning/routing closeout gate. It adds no new runtime behavior
or fixtures, preserves the Gate 250 through Gate 258 clean golden coverage as
regression evidence, records deferred clean backlog items, and routes the next
lane to `forge release prepare` planning while preserving offline-first,
deterministic, and AI-optional behavior.

Gate 260 extends fixture-backed golden CLI coverage for planning-only
`forge release prepare`. Tests verify JSON planning metadata, help boundary
text, unsafe output-root refusal, no filesystem writes, and false execution
flags while preserving offline-first, deterministic, and AI-optional behavior.

Gate 261 extends fixture-backed golden CLI coverage for local release-plan
emission. Tests verify `release-plan.json` creation, CLI `writtenOutputs`
metadata, `--dry-run` no-write behavior, unsafe output-root refusal, and no
release summary, build manifest, checksum, staging, archive, publish, remote,
external tool, runtime probe, or AI behavior.

Gate 262 extends fixture-backed golden CLI coverage for local release-summary
emission. Tests verify `release-summary.json` creation beside
`release-plan.json`, CLI `output.releaseSummary` and `writtenOutputs`
metadata, `--dry-run` no-write behavior, unsafe output-root refusal, and no
build manifest, checksum, staging, archive, publish, remote, external tool,
runtime probe, or AI behavior.

Gate 263 extends fixture-backed golden CLI coverage for local release-prepare
build-manifest emission. Tests verify `build-manifest.json` creation beside
`release-plan.json` and `release-summary.json`, CLI `output.buildManifest`
and `writtenOutputs` metadata, output digests for the plan and summary,
`--dry-run` no-write behavior, unsafe output-root refusal, and no checksum,
staging, archive, publish, remote, external tool, runtime probe, or AI
behavior.

Gate 264 extends fixture-backed golden CLI coverage for local release-prepare
checksum sidecar emission. Tests verify `checksums.sha256` creation beside
`release-plan.json`, `release-summary.json`, and `build-manifest.json`, CLI
`output.checksums` and `writtenOutputs` metadata, checksum entries for the
three evidence files, `--dry-run` no-write behavior, unsafe output-root
refusal, and no staging, archive, publish, remote, external tool, runtime
probe, or AI behavior.

Gate 265 extends fixture-backed golden CLI coverage for local release-prepare
staging payload skeleton emission. Tests verify
`staging/release-payload.json` creation beside the release evidence files, CLI
`output.stagingRoot`, `output.stagingPayload`, and `writtenOutputs` metadata,
build-manifest and checksum coverage for the staging payload, `--dry-run`
no-write behavior, unsafe output-root refusal, and no archive, publish,
remote, external tool, runtime probe, or AI behavior.

Gate 266 extends fixture-backed golden CLI coverage for local release-prepare
archive planning metadata. Tests verify `release-archive-plan.json` creation
beside the release evidence files, CLI `output.releaseArchivePlan`,
`output.plannedArchive`, planned archive no-write metadata, build-manifest and
checksum coverage for the archive plan, `--dry-run` no-write behavior, unsafe
output-root refusal, and no archive creation, publish, remote, external tool,
runtime probe, or AI behavior.

Gate 267 extends fixture-backed golden CLI coverage for deterministic local
release archive creation. Tests verify `archives/release.zip` creation beside
the release evidence files, sorted stored ZIP entries, deterministic
ZIP-compatible timestamps, CLI `output.releaseArchive`, build-manifest and
checksum coverage for the archive, `--dry-run` no-write behavior, unsafe
output-root refusal, repeated-run archive digest stability, and no FOMOD,
publish, remote, external tool, runtime probe, or AI behavior.

Gate 268 extends fixture-backed golden CLI coverage for local release archive
evidence revalidation. Tests verify `release-archive-evidence.json` creation
beside the release evidence files, archive SHA-256 and length evidence,
expected and actual ZIP entry names, stored-compression status, deterministic
timestamp metadata, CLI `output.releaseArchiveEvidence`, build-manifest and
checksum coverage for the archive evidence sidecar, `--dry-run` no-write
behavior, unsafe output-root refusal, and no FOMOD, publish, remote, external
tool, runtime probe, or AI behavior.

Gate 269 closes the current release-prepare lane as a planning/routing gate.
No new test fixture, diagnostic, or runtime behavior is added. Validation is
limited to document/routing consistency plus the normal local build/test smoke
checks; release publishing remains blocked until explicit governance preflight
and human-approval behavior exists.

Gate 270 adds the explicit governance preflight shell for
`forge release publish` while keeping release publication blocked. Golden CLI
tests verify normal refusal, dry-run planning, required evidence reporting,
governance check reporting, missing human approval reporting, and false
publish/remote/upload/signing/tool/runtime/AI execution flags. The gate still
does not read release artifacts, write publish outputs, publish releases, call
remote repositories, upload assets, sign or attest artifacts, execute external
tools, run runtime probes, or use AI.

Gate 271 adds fixture-backed golden CLI coverage for release-publish local
evidence discovery. Tests verify that an empty project root reports eight
missing release-prepare artifacts, that a project after `forge release
prepare` reports eight present artifacts as `present-not-validated`, and that
artifact reads, content validation, checksum revalidation, archive
revalidation, publish, remote, upload, signing, external-tool, runtime-probe,
and AI execution flags remain disabled.

Gate 272 adds fixture-backed golden CLI coverage for release-publish local
evidence content-shape classification. Tests verify well-formed JSON evidence,
well-formed checksum text shape, binary archive deferral, malformed JSON
reporting, and disabled checksum revalidation, archive revalidation, publish,
remote, upload, signing, external-tool, runtime-probe, and AI execution.

Gate 273 adds fixture-backed golden CLI coverage for release-publish checksum
sidecar entry classification. Tests verify parsed checksum entries,
expected-path coverage, missing expected path coverage, unexpected path
classification, deliberately changed digest text remaining
`expected-not-revalidated`, and disabled checksum digest revalidation, archive
revalidation, publish, remote, upload, signing, external-tool, runtime-probe,
and AI execution.

Gate 274 adds fixture-backed golden CLI coverage for release-publish
build-manifest output cross-reference. Tests verify parsed manifest outputs,
expected output coverage, local artifact and checksum sidecar path
cross-reference, rewritten output path mismatch reporting, and disabled
checksum digest revalidation, build-manifest digest revalidation, semantic
evidence validation, archive revalidation, publish, remote, upload, signing,
external-tool, runtime-probe, and AI execution.

Gate 275 adds fixture-backed golden CLI coverage for release-publish
release-archive-evidence metadata cross-reference. Tests verify parsed
archive-evidence output metadata paths, expected path coverage, local
artifact, checksum sidecar, and build-manifest output cross-reference,
rewritten metadata path mismatch reporting, and disabled checksum digest
revalidation, build-manifest digest revalidation, release-archive-evidence
digest revalidation, semantic evidence validation, archive revalidation,
publish, remote, upload, signing, external-tool, runtime-probe, and AI
execution.

Gate 295 adds fixture-backed unit and golden CLI coverage for
`forge package --target reports`. Tests verify package-plan, staging-layout,
build-manifest, checksum sidecar, dry-run no-write behavior, `dist/` output
containment, JSON CLI output, and that current reports packaging does not copy
`dist/build` inputs or create archives. It adds no public fixture payloads,
no real provider samples, no release publication, no remote calls, no signing
or attestation, no external-tool execution, no runtime probes, and no AI
requirement.

Gate 296 extends fixture-backed unit and golden CLI coverage for
`forge package --target reports` input discovery. Tests verify missing inputs
from a clean fixture, present inputs after `forge build --target reports`,
per-entry `sourceExists` and `inputStatus`, present/missing summary counts,
and that current reports packaging still does not copy inputs or create
archives. It adds no public fixture payloads, no real provider samples, no
release publication, no remote calls, no signing or attestation, no
external-tool execution, no runtime probes, and no AI requirement.

Gate 297 extends fixture-backed unit and golden CLI coverage for
`forge package --target reports` staging-copy behavior. Tests verify clean
fixtures copy zero missing inputs, package runs after
`forge build --target reports` copy expected present report evidence into
`dist/reports-package/staging/reports`, package entries report `staged` and
`stageStatus`, and copied staged payloads are covered by the package build
manifest and checksum sidecar. It adds no committed generated payloads, no
real provider samples, no archive creation, no release publication, no remote
calls, no signing or attestation, no external-tool execution, no runtime
probes, and no AI requirement.

Gate 298 extends fixture-backed unit and golden CLI coverage for
`forge package --target reports` deterministic archive creation. Tests verify
clean fixture package runs create an evidence-only `package.zip`, runs after
`forge build --target reports` include staged `reports/*` payload entries,
archive entries are sorted with deterministic timestamps, and package
build-manifest/checksum evidence covers `package.zip`. It adds no committed
generated payloads, no real provider samples, no archive revalidation, no
release publication, no remote calls, no signing or attestation, no
external-tool execution, no runtime probes, and no AI requirement.

Gate 299 extends fixture-backed unit and golden CLI coverage for
`forge package --target reports` archive evidence revalidation. Tests verify
package runs write `package-archive-evidence.json`, recompute archive digest
metadata, compare expected and actual entry names, verify ordinal ordering,
verify deterministic timestamps, verify stored compression, and cover the
sidecar in package build-manifest/checksum evidence. It adds no committed
generated payloads, no real provider samples, no `reports` verify-existing
behavior, no release publication, no remote calls, no signing or attestation,
no external-tool execution, no runtime probes, and no AI requirement.

Gate 300 is a planning/routing closeout for the reports package lane. It
requires document and routing consistency plus normal local build/test smoke
checks, but adds no new fixture corpus, generated payloads, provider samples,
runtime command behavior, schemas, diagnostics, `reports` verify-existing
behavior, release publication, remote calls, signing or attestation,
external-tool execution, runtime probes, or AI requirement. The next
validation route is Gate 301 `forge release verify` self-report evidence
under `dist/release-dry-run/release-verify.json`.

Gate 301 extends fixture-backed unit and golden CLI coverage for release
dry-run self-report evidence. Tests verify `release-verify.json` is written,
uses release verify JSON report identity, is reported through CLI JSON, and is
covered by release dry-run build-manifest and checksum evidence. It adds no
committed generated payloads, no real provider samples, no package verifier
execution, no release publication, no remote calls, no signing or
attestation, no external-tool execution, no runtime probes, and no AI
requirement.

Gate 302 extends fixture-backed unit and golden CLI coverage for release
dry-run evidence-index output. Tests verify `release-evidence-index.json` is
written, lists local release-publish preflight evidence paths and command
hints, reports disabled execution boundaries, is reported through CLI JSON,
and is covered by release dry-run build-manifest and checksum evidence. It
adds no committed generated payloads, no real provider samples, no command
fan-out, no package verifier execution, no release publication, no remote
calls, no signing or attestation, no external-tool execution, no runtime
probes, and no AI requirement.

Gate 303 extends fixture-backed unit and golden CLI coverage for release
dry-run evidence handoff output. Tests verify
`release-evidence-handoff.md` is written, renders evidence rows and command
hints from the evidence index, reports disabled execution boundaries, is
reported through CLI JSON, and is covered by release dry-run build-manifest
and checksum evidence. It adds no committed generated payloads, no real
provider samples, no command fan-out, no package verifier execution, no
release publication, no remote calls, no signing or attestation, no
external-tool execution, no runtime probes, and no AI requirement.

Gate 304 extends fixture-backed unit and golden CLI coverage for release
dry-run evidence status projection. Tests verify
`release-evidence-status.json` is written, marks indexed evidence paths as
present or missing, projects present/missing counts into the evidence index
and Markdown handoff, is reported through CLI JSON, and is covered by release
dry-run build-manifest and checksum evidence. It adds no committed generated
payloads, no real provider samples, no command fan-out, no package verifier
execution, no evidence content validation, no release publication, no remote
calls, no signing or attestation, no external-tool execution, no runtime
probes, and no AI requirement.

Gate 305 extends fixture-backed unit and golden CLI coverage for release
dry-run missing-evidence actions. Tests verify
`release-evidence-actions.json` is written, lists manual actions for missing
capability/environment and package-validation evidence, links from the
evidence index, status projection, and Markdown handoff, is reported through
CLI JSON, and is covered by release dry-run build-manifest and checksum
evidence. It adds no committed generated payloads, no real provider samples,
no command fan-out, no package verifier execution, no evidence content
validation, no release publication, no remote calls, no signing or
attestation, no external-tool execution, no runtime probes, and no AI
requirement.

Gate 306 extends fixture-backed unit and golden CLI coverage for release
dry-run evidence collection plans. Tests verify
`release-evidence-collection-plan.json` is written, orders the
schema-validation, capability/environment, package-validation, and
release-verification steps, links missing manual steps to action IDs, links
from the evidence index, status projection, action checklist, and Markdown
handoff, is reported through CLI JSON, and is covered by release dry-run
build-manifest and checksum evidence. It adds no committed generated
payloads, no real provider samples, no command fan-out, no package verifier
execution, no evidence content validation, no release publication, no remote
calls, no signing or attestation, no external-tool execution, no runtime
probes, and no AI requirement.

Gate 307 extends fixture-backed golden CLI coverage for release-publish
collection-plan evidence. Tests verify `forge release publish --dry-run`
reads `dist/release-dry-run/release-evidence-collection-plan.json`, validates
its identity, links, ordered steps, summary counters, and disabled execution
flags, adds `release-dry-run-collection-plan` to required evidence/readiness,
and still does not publish, upload, sign, call remote repositories, execute
external tools, run runtime probes, use real third-party plugin fixtures, or
require AI.

Gate 308 extends fixture-backed golden CLI coverage for release-publish
release dry-run evidence cross-links. Tests verify release publish dry-run
reads the local `release-evidence-index.json`,
`release-evidence-status.json`, `release-evidence-actions.json`,
`release-evidence-collection-plan.json`, and `release-evidence-handoff.md`
files, validates identity, links, required evidence rows, actions,
collection steps, summary counters, handoff references, and disabled
execution flags, adds `release-dry-run-cross-links` to required
evidence/readiness, updates Doctor release-readiness counts, and still does
not publish, upload, sign, call remote repositories, execute external tools,
run runtime probes, use real third-party plugin fixtures, or require AI.

Gate 309 extends fixture-backed golden CLI coverage for release-publish
release dry-run evidence remediation. Tests verify missing, malformed,
cross-link-mismatched, and clean dry-run evidence states produce the expected
manual remediation summary, command hints, JSON fields, plain output fields,
and false execution flags. The tests still use synthetic fixtures and do not
publish, upload, sign, call remote repositories, execute external tools, run
runtime probes, use real third-party plugin fixtures, or require AI.

Gate 310 extends fixture-backed golden CLI coverage for Doctor export release
dry-run evidence remediation handoff projection. Tests verify primary JSON,
plain output, Markdown summaries, release-readiness bundle indexes,
triage indexes, and handoff summaries expose the manual remediation item and
local release-verify command hint derived from release-publish dry-run
preflight data. The tests still use synthetic fixtures and do not publish,
upload, sign, call remote repositories, execute external tools, run runtime
probes, use real third-party plugin fixtures, or require AI.

Gate 311 is a planning/routing closeout for the release dry-run remediation
handoff lane. It adds no new fixture corpus, generated payloads, provider
samples, runtime command behavior, schemas, diagnostics, release execution,
Doctor execution, init execution, external-tool execution, runtime probes, or
AI requirement. Required validation is document and routing consistency plus
normal local build/test smoke checks. The next validation route was Gate 312
`forge init` project scaffold planning.

Gate 312 extends fixture-backed golden CLI coverage for the planning-only
`forge init` scaffold report. Tests verify help text, JSON planning output,
derived project identity, planned scaffold paths, false execution flags,
existing-path refusal with exit code `6`, diagnostic-only format rejection,
and the no-write boundary against temp directories. The gate adds no public
fixture corpus, proprietary assets, provider samples, generated payload
fixtures, release execution, external-tool execution, runtime probes, plugin
mutation, MO2/GECK automation, remote calls, signing, attestation, or AI
requirement.

Gate 316 extends golden CLI coverage for `forge init` minimal scaffold
emission. Tests verify dry-run remains no-write, normal init writes only the
manifest and dependency/capability registry files, generated/dist/editor/
workflow/README paths remain absent, written-path execution metadata is
reported, existing planned paths still return exit code `6`, diagnostic-only
formats remain rejected, and the created scaffold passes `forge validate`
with zero errors. The gate uses temp-only synthetic directories and adds no
public fixture corpus, proprietary assets, provider samples, generated
payload fixtures, release execution, external-tool execution, runtime probes,
plugin mutation, MO2/GECK automation, remote calls, signing, attestation, or
AI requirement.

Gate 317 extends golden CLI coverage for `forge init` config and README
scaffold emission. Tests verify normal init writes `.wastelandforge/config.jsonc`
and `README.md`, dry-run remains no-write, generated/dist/cache/editor/
workflow paths remain absent, config and README write flags are reported,
existing planned paths still return exit code `6`, and the created scaffold
passes `forge validate` with zero errors. The gate uses temp-only synthetic
directories and adds no public fixture corpus, proprietary assets, provider
samples, generated payload fixtures, release execution, external-tool
execution, runtime probes, plugin mutation, MO2/GECK automation, remote calls,
signing, attestation, or AI requirement.

Gate 318 extends golden CLI coverage for `forge init` VS Code task scaffold
emission. Tests verify normal init writes `.vscode/tasks.json`, parses the
task file, checks validate/capability-scan/build task labels and the validate
problem matcher, dry-run remains no-write, generated/dist/cache/workflow paths
remain absent, the VS Code write flag is reported, existing planned paths still
return exit code `6`, and the created scaffold passes `forge validate` with
zero errors. The gate uses temp-only synthetic directories and adds no public
fixture corpus, proprietary assets, provider samples, generated payload
fixtures, release execution, external-tool execution, runtime probes, plugin
mutation, MO2/GECK automation, remote calls, signing, attestation, or AI
requirement.

Gate 319 extends golden CLI coverage for `forge init` GitHub Actions workflow
scaffold emission. Tests verify normal init writes
`.github/workflows/wastelandforge.yml`, checks Windows and Ubuntu lane markers,
least-privilege permissions, pinned action SHAs, SARIF/artifact/release dry-run
markers, dry-run remains no-write, generated/dist/cache paths remain absent,
the workflow write flag is reported, existing planned paths still return exit
code `6`, and the created scaffold passes `forge validate` with zero errors.
The gate uses temp-only synthetic directories and adds no public fixture
corpus, proprietary assets, provider samples, generated payload fixtures,
workflow execution, release execution, external-tool execution, runtime probes,
plugin mutation, MO2/GECK automation, remote calls, signing, attestation, or
AI requirement.

Gate 320 extends golden CLI coverage for `forge init` editor schema
association scaffold emission. Tests verify normal init writes
`.vscode/settings.json`, parses the settings file, checks manifest,
dependency, capability, and dialogue JSON schema associations, checks YAML
schema mappings, confirms dry-run remains no-write, confirms generated,
distribution, and cache paths remain absent, confirms editor schema write
metadata is reported, keeps existing planned-path refusal at exit code `6`,
and validates the created temp scaffold locally. The gate uses temp-only
synthetic directories and adds no public fixture corpus, proprietary assets,
provider samples, generated payload fixtures, workflow execution, release
execution, external-tool execution, runtime probes, plugin mutation, MO2/GECK
automation, remote calls, signing, attestation, VS Code extension process,
language-server process, or AI requirement.

Gate 321 adds no new runtime test coverage because it is a docs and prompt
routing closeout. It records that the `forge init` onboarding lane is complete
for now and routes the next value slice to Forge CLI runner/bootstrap planning.
The gate does not add public fixtures, generated payload fixtures, workflow
execution, release execution, external-tool execution, runtime probes, plugin
mutation, MO2/GECK automation, remote calls, signing, attestation, VS Code
extension process, language-server process, or AI requirement.

Gate 322 adds no new runtime test coverage because it is a docs and prompt
routing planning gate. It defines the Forge CLI runner/bootstrap model and
routes Gate 323 to a source-built runner shim scaffold. The gate does not add
public fixtures, generated payload fixtures, workflow execution, release
execution, external-tool execution, runtime probes, plugin mutation, MO2/GECK
automation, remote calls, signing, attestation, VS Code extension process,
language-server process, local tool package publication, or AI requirement.

Gate 323 adds source-built runner smoke coverage rather than new product
fixtures. Validation invokes `eng/forge.ps1 --version`,
`eng/forge.cmd --version`, and `eng/forge.ps1 help` so the new wrappers
exercise the real CLI through `dotnet run` and preserve normal CLI output. It
adds no public fixtures, generated payload fixtures, workflow execution,
release execution, external-tool execution, runtime probes, plugin mutation,
MO2/GECK automation, remote calls, signing, attestation, VS Code extension
process, language-server process, local tool package publication, or AI
requirement.

Gate 324 adds no runtime test coverage because it is a planning gate for the
local-tool package metadata lane. It records the future validation shape:
metadata inspection, `dotnet pack`, local package-source install into a
temporary manifest, and local tool `--version`/`help` smoke checks. It adds no
public fixtures, generated payload fixtures, workflow execution, release
execution, package creation, external-tool execution, runtime probes, plugin
mutation, MO2/GECK automation, remote calls, signing, attestation, VS Code
extension process, language-server process, local tool package publication, or
AI requirement.

Gate 325 adds local package smoke validation without public fixtures. It runs
`dotnet pack`, installs `WastelandForge.Cli` version `0.1.0` from ignored
local package output into a temporary local-tool manifest, then runs
`forge --version` and `forge help` through `dotnet tool run`. It adds no
public fixtures, generated payload fixtures, workflow execution, release
execution, external-tool execution, runtime probes, plugin mutation,
MO2/GECK automation, remote calls, signing, attestation, VS Code extension
process, language-server process, local tool package publication, or AI
requirement.

Gate 326 adds no runtime test coverage because it is a planning gate for the
checked-in local tool manifest flow. It validates by confirming no committed
`.config/dotnet-tools.json` exists and by checking prompt routing and
protected-file boundaries. It adds no public fixtures, generated payload
fixtures, workflow execution, release execution, package restore execution,
external-tool execution, runtime probes, plugin mutation, MO2/GECK automation,
remote calls, signing, attestation, VS Code extension process,
language-server process, local tool package publication, or AI requirement.

Gate 327 adds local tool restore smoke coverage without public fixtures. It
runs `dotnet pack`, restores the checked-in local tool manifest from ignored
local package output using a temporary local-only NuGet config and isolated
`NUGET_PACKAGES`, then runs `forge --version` and `forge help` through
`dotnet tool run`. It adds no public fixtures, generated payload fixtures,
workflow execution, release execution, external-tool execution, runtime
probes, plugin mutation, MO2/GECK automation, remote calls, signing,
attestation, VS Code extension process, language-server process, local tool
package publication, or AI requirement.

Gate 328 adds no runtime test coverage because it is a planning gate for
generated workflow and task bootstrap integration. It records the validation
split between source-repository local tool restore and consumer-project
scaffold assumptions, and validates by checking routing, documentation, and
protected-file boundaries. It adds no public fixtures, generated payload
fixtures, workflow execution, release execution, external-tool execution,
runtime probes, plugin mutation, MO2/GECK automation, remote calls, signing,
attestation, VS Code extension process, language-server process, local tool
package publication, generated task/workflow mutation, or AI requirement.

Gate 329 adds repo-local restore helper smoke coverage without public
fixtures. Validation runs `eng/Restore-ForgeTool.ps1`, which packs
`WastelandForge.Cli`, restores the checked-in local tool manifest from ignored
local package output using a local-only NuGet config and isolated
`NUGET_PACKAGES`, then verifies `forge --version` through `dotnet tool run`.
It adds no public fixtures, generated payload fixtures, workflow execution,
release execution, external-tool execution, runtime probes, plugin mutation,
MO2/GECK automation, remote calls, signing, attestation, VS Code extension
process, language-server process, local tool package publication, generated
task/workflow mutation, or AI requirement.

Gate 330 adds repository-owned CI local-tool bootstrap coverage without public
fixtures. The Ubuntu validation lane restores the checked-in local tool before
exporting SARIF and GitHub/Markdown validation output through the restored
local tool; the mandatory Windows lane restores the local tool as a bootstrap
smoke; and the release dry-run lane restores the local tool before running
`dotnet tool run forge -- release verify`. It adds no public fixtures,
generated payload fixtures, release execution beyond existing dry-run
verification, external-tool execution, runtime probes, plugin mutation,
MO2/GECK automation, signing, attestation, VS Code extension process,
language-server process, local tool package publication, generated
task/workflow mutation, or AI requirement.

Gate 331 plans repository-owned developer task local-tool bootstrap coverage
without public fixtures. The planned tasks will restore the checked-in local
tool before read-only Forge task invocations, but Gate 331 itself adds no task
file, public fixtures, generated payload fixtures, workflow execution, release
execution, external-tool execution, runtime probes, plugin mutation,
MO2/GECK automation, signing, attestation, VS Code extension process,
language-server process, local tool package publication, generated
task/workflow mutation, or AI requirement.

Gate 332 adds repository-owned developer task local-tool bootstrap coverage
without public fixtures. Validation restores the checked-in local tool through
`eng/Restore-ForgeTool.ps1`, runs `forge help`, validates the synthetic
ExampleMod fixture, and lists capabilities through `dotnet tool run forge --`.
It adds no public fixtures, generated payload fixtures, workflow execution,
release execution, external-tool execution, runtime probes, plugin mutation,
MO2/GECK automation, signing, attestation, VS Code extension process,
language-server process, local tool package publication, generated
task/workflow mutation, or AI requirement.

Gate 333 adds no runtime test coverage because it is a routing closeout gate.
It records the accepted repository-owned bootstrap lane and routes the next
slice to consumer-project package-source policy planning. It adds no public
fixtures, generated payload fixtures, workflow execution, release execution,
external-tool execution, runtime probes, plugin mutation, MO2/GECK automation,
signing, attestation, VS Code extension process, language-server process,
local tool package publication, generated task/workflow mutation, or AI
requirement.

Gate 334 adds no runtime test coverage because it is a planning gate for
consumer-project package-source policy. It records that generated consumer
projects stay source-agnostic and use an existing `forge` command until
package/feed governance is gated. It adds no public fixtures, generated
payload fixtures, workflow execution, release execution, external-tool
execution, runtime probes, plugin mutation, MO2/GECK automation, signing,
attestation, VS Code extension process, language-server process, local tool
package publication, generated task/workflow mutation, or AI requirement.

Gate 335 adds targeted golden coverage for generated consumer-project command
availability guidance. The init scaffold test now proves the generated README
documents the `forge` on `PATH` prerequisite, the generated VS Code tasks include
and depend on `Forge: Check Command`, and the generated workflow message explains
`FORGE_COMMAND` without adding source-repository restore commands. It adds no
public fixtures, generated payload fixtures, workflow execution, release
execution, external-tool execution, runtime probes, plugin mutation,
MO2/GECK automation, signing, attestation, VS Code extension process,
language-server process, local tool package publication, package restore,
publication, or AI requirement.
