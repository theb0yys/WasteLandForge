# Capability and Provider Notes

This directory contains durable capability and provider model notes that are
not ADRs, schemas, or generated command output.

- `provider-version-parser.md` - current provider-version parser contract and
  future scan-evidence projection notes.
