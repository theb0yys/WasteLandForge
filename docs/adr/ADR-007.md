# ADR-007 - Contract, Schema, and Registry Model

Status: Accepted
Research classification: Documented
Source: `WasteLandForge/research/R004 WastelandForge Contract, Schema and Registry Design-deep-research-report.md`

## Decision

WastelandForge source truth is represented as versioned YAML/JSON registry documents normalized to canonical JSON, validated by JSON Schema Draft 2020-12 plus deterministic semantic validators.

## Consequences

- Canonical truth lives in version-controlled source contracts.
- Generated outputs, game saves, GECK sessions, plugin binaries, and AI responses are not canonical truth.
- Schemas are public API and must use stable IDs and explicit schema versions.
- Registry loading must support file and directory roots while producing one logical canonical graph.
- `forge validate` must not rewrite author source.

## Gate Impact

Gate 3 creates the schema skeleton. Gate 4 and Gate 5 implement source
locations, diagnostics, loading, schema validation, and semantic validation.
Gate 62 adds manifest `0.2.0`, dependency `0.2.0`, capability `0.2.0`, and
MCM registry `0.1.0` schemas without mutating earlier schema IDs.
Gate 63 adds `mcm-extender-output/0.1.0` as a generated-output schema used for
local output validation.
Gate 64 extends the MCM source and output schema surfaces for documented MCM
Extender runtime requirements and translation maps.
Gate 65 extends those unpublished schema surfaces for documented MCM Extender
checkbox and string-toggle option types.
Gate 66 extends those unpublished schema surfaces for documented MCM Extender
keybind option type support.
Gate 67 extends those unpublished schema surfaces for documented MCM Extender
header option type support.
Gate 68 extends those unpublished schema surfaces for documented MCM Extender
image option map support.
Gate 69 adds cross-registry semantic validation from MCM image filenames to
required texture asset targets without changing schema IDs.
Gate 70 stages validated referenced texture assets as generated output without
changing source schema IDs or making generated files canonical truth.
Gate 71 adds generated package-manifest metadata without changing source
schema IDs or making package metadata canonical truth.
Gate 72 adds generated ZIP archive output without changing source schema IDs
or making archives canonical truth.
Gate 73 exposes the generated MCM package archive through `forge package`
without changing source schema IDs or making package outputs canonical truth.
Gate 74 adds generated package manifest schema
`package-manifest/0.1.0/schema.json`. It is package evidence, not canonical
source truth.
Gate 76 adds generated install-preview report schema
`install-preview/0.1.0/schema.json`. It is install intent evidence, not
canonical source truth or an installer contract.
Gate 77 adds generated install-preview Markdown summary evidence without
changing source schema IDs or making generated summaries canonical truth.
Gate 78 adds generated package-verification report evidence without changing
source schema IDs or making generated verification reports canonical truth.
Gate 79 adds generated package-verification report schema
`package-verification/0.1.0/schema.json`. It is package evidence, not
canonical source truth or an installer contract.
Gate 80 adds generated package-verification Markdown summary evidence without
changing source schema IDs or making generated summaries canonical truth.
Gate 81 adds package-verification evidence cross-checks without changing
source schema IDs or making generated evidence canonical truth.
Gate 82 extracts those checks into reusable validator code without changing
source schema IDs, generated evidence schema IDs, or canonical source truth.
Gate 83 adds a file-based verifier over existing generated evidence files
without changing source schema IDs, generated evidence schema IDs, or
canonical source truth.
Gate 84 adds payload digest recomputation over generated package files without
changing source schema IDs, generated evidence schema IDs, or canonical source
truth.
Gate 85 adds archive digest recomputation over generated package archives
without changing source schema IDs, generated evidence schema IDs, or
canonical source truth.
Gate 86 adds archive entry-name revalidation over generated package archives
without changing source schema IDs, generated evidence schema IDs, or
canonical source truth.
Gate 87 records the future package-verification command-surface decision
without changing source schema IDs, generated evidence schema IDs, or
canonical source truth.
Gate 88 implements the verify-existing package command skeleton without
changing source schema IDs, generated evidence schema IDs, or canonical source
truth.
Gate 89 adds SARIF/GitHub diagnostic projections for verify-existing package
evidence without changing source schema IDs, generated evidence schema IDs, or
canonical source truth.
Gate 90 adds Markdown summary output for verify-existing package diagnostics
without changing source schema IDs, generated evidence schema IDs, or
canonical source truth.
Gate 91 adds checksum-file revalidation for verify-existing package evidence
without changing source schema IDs, generated evidence schema IDs, or
canonical source truth.
Gate 92 adds build-manifest content revalidation for verify-existing package
evidence without changing source schema IDs, generated evidence schema IDs, or
canonical source truth.
Gate 93 adds install-preview summary content revalidation for verify-existing
package evidence without changing source schema IDs, generated evidence schema
IDs, or canonical source truth.
Gate 94 adds install-preview/package-manifest entry content cross-checking for
verify-existing package evidence without changing source schema IDs, generated
evidence schema IDs, or canonical source truth.
Gate 95 adds package-verification summary content revalidation for
verify-existing package evidence without changing source schema IDs, generated
evidence schema IDs, or canonical source truth.
Gate 96 adds package-verification JSON check content revalidation for
verify-existing package evidence without changing source schema IDs, generated
evidence schema IDs, or canonical source truth.
Gate 97 adds package-verification JSON metadata content revalidation for
verify-existing package evidence without changing source schema IDs, generated
evidence schema IDs, or canonical source truth.
Gate 98 adds package-verification archive detail content revalidation for
verify-existing package evidence without changing source schema IDs, generated
evidence schema IDs, or canonical source truth.
Gate 99 adds install-preview archive detail content revalidation for
verify-existing package evidence without changing source schema IDs, generated
evidence schema IDs, or canonical source truth.
Gate 100 adds package-manifest archive detail content revalidation for
verify-existing package evidence without changing source schema IDs, generated
evidence schema IDs, or canonical source truth.
Gate 101 adds archive detail cross-report consistency revalidation for
verify-existing package evidence without changing source schema IDs, generated
evidence schema IDs, or canonical source truth.
Gate 102 adds package archive presence revalidation for verify-existing
package evidence without changing source schema IDs, generated evidence schema
IDs, or canonical source truth.
Gate 103 adds checksum unexpected-entry revalidation for verify-existing
package evidence without changing source schema IDs, generated evidence schema
IDs, or canonical source truth.
Gate 104 adds checksum duplicate-entry revalidation for verify-existing package
evidence without changing source schema IDs, generated evidence schema IDs, or
canonical source truth.
Gate 105 adds checksum canonical-order revalidation for verify-existing package
evidence without changing source schema IDs, generated evidence schema IDs, or
canonical source truth.
Gate 106 adds checksum digest canonical-casing revalidation for verify-existing
package evidence without changing source schema IDs, generated evidence schema
IDs, or canonical source truth.
Gate 107 adds checksum line-ending and trailing-newline revalidation for
verify-existing package evidence without changing source schema IDs, generated
evidence schema IDs, or canonical source truth.
Gate 108 adds checksum path separator canonicalization revalidation for
verify-existing package evidence without changing source schema IDs, generated
evidence schema IDs, or canonical source truth.
Gate 109 adds checksum blank-line revalidation for verify-existing package
evidence without changing source schema IDs, generated evidence schema IDs, or
canonical source truth.
Gate 110 adds checksum entry spacing canonicalization revalidation for
verify-existing package evidence without changing source schema IDs, generated
evidence schema IDs, or canonical source truth.
Gate 111 adds checksum path casing canonicalization revalidation for
verify-existing package evidence without changing source schema IDs, generated
evidence schema IDs, or canonical source truth.
Gate 112 adds checksum case-insensitive duplicate revalidation for
verify-existing package evidence without changing source schema IDs, generated
evidence schema IDs, or canonical source truth.
Gate 113 adds checksum malformed-entry format revalidation for verify-existing
package evidence without changing source schema IDs, generated evidence schema
IDs, or canonical source truth.
Gate 114 adds checksum path containment revalidation for verify-existing
package evidence without changing source schema IDs, generated evidence schema
IDs, or canonical source truth.
Gate 115 adds checksum comment-line rejection revalidation for verify-existing
package evidence without changing source schema IDs, generated evidence schema
IDs, or canonical source truth.
Gate 116 adds `install-plan/0.1.0` as a generated evidence schema and emits
install-plan JSON/Markdown outputs without changing source schema IDs or
canonical source truth.
Gate 117 revalidates existing install-plan content in verify-existing mode
without introducing a new schema ID or changing canonical source truth.
Gate 118 revalidates existing `install-plan.json` against the existing
`install-plan/0.1.0` generated evidence schema in verify-existing mode without
introducing a new schema ID or changing canonical source truth.
Gate 119 revalidates existing `package-manifest.json` against the existing
`package-manifest/0.1.0` generated evidence schema in verify-existing mode
without introducing a new schema ID or changing canonical source truth.
Gate 120 revalidates existing `install-preview.json` against the existing
`install-preview/0.1.0` generated evidence schema in verify-existing mode
without introducing a new schema ID or changing canonical source truth.
Gate 121 revalidates existing `package-verification.json` against the existing
`package-verification/0.1.0` generated evidence schema in verify-existing mode
without introducing a new schema ID or changing canonical source truth.
Gate 166 adds a redacted Doctor export ZIP sidecar without changing source
schema IDs or making the archive canonical truth. The archive is generated
handoff evidence derived from existing redacted Doctor export output.
Gate 168 extends that generated handoff archive with per-requirement
explanation Markdown entries derived from existing capability explanation
data. It does not change source schema IDs or make the archive canonical
truth.
Gate 169 adds matching per-requirement explanation JSON entries to that
generated handoff archive. The JSON is derived from existing capability
explanation data, redacted before archiving, and does not change source
schema IDs or make the archive canonical truth.
Gate 170 adds generated requirement explanation index JSON and Markdown files
to that same handoff archive. The index is derived from existing requirement
resolution and diagnostic handoff data, and does not change source schema IDs
or make the archive canonical truth.
Gate 171 adds a generated `README.md` entry to that same handoff archive. The
README is derived from the already redacted Doctor export report and archive
supplement paths, and does not change source schema IDs or make the archive
canonical truth.
Gate 172 adds generated diagnostic index JSON and Markdown entries to that
same handoff archive. The diagnostic index is derived from already redacted
Doctor export diagnostic metadata, and does not change source schema IDs or
make the archive canonical truth.
Gate 173 adds generated action index JSON and Markdown entries to that same
handoff archive. The action index is derived from already redacted Doctor
export action metadata, and does not change source schema IDs or make the
archive canonical truth.
Gate 174 adds generated requirement index JSON and Markdown entries to that
same handoff archive. The requirement index is derived from already redacted
Doctor export requirement metadata, and does not change source schema IDs or
make the archive canonical truth.
Gate 175 adds generated provider index JSON and Markdown entries to that same
handoff archive. The provider index is derived from already redacted Doctor
export provider metadata, and does not change source schema IDs or make the
archive canonical truth.
Gate 176 adds generated capability index JSON and Markdown entries to that
same handoff archive. The capability index is derived from already redacted
Doctor export capability metadata, and does not change source schema IDs or
make the archive canonical truth.
Gate 177 adds generated Doctor area index JSON and Markdown entries to that
same handoff archive. The Doctor area index is derived from already redacted
Doctor export readiness metadata, and does not change source schema IDs or
make the archive canonical truth.
Gate 178 adds generated catalogue-policy index JSON and Markdown entries to
that same handoff archive. The catalogue-policy index is derived from already
redacted Doctor export catalogue-policy metadata, and does not change source
schema IDs or make the archive canonical truth.
Gate 179 adds generated summary index JSON and Markdown entries to that same
handoff archive. The summary index is derived from already redacted Doctor
export summary metadata, and does not change source schema IDs or make the
archive canonical truth.
Gate 180 adds generated evidence index JSON and Markdown entries to that same
handoff archive. The evidence index is derived from already redacted Doctor
export provider evidence metadata, and does not change source schema IDs or
make the archive canonical truth.
Gate 181 adds generated redaction index JSON and Markdown entries to that same
handoff archive. The redaction index is derived from already redacted Doctor
export redaction metadata, and does not change source schema IDs or make the
archive canonical truth.
Gate 182 adds generated open-question index JSON and Markdown entries to that
same handoff archive. The open-question index is derived from already
redacted Doctor export open-question metadata, and does not change source
schema IDs or make the archive canonical truth.
Gate 183 adds generated scan-input index JSON and Markdown entries to that
same handoff archive. The scan-input index is derived from already redacted
Doctor export capability scan input metadata, and does not change source
schema IDs or make the archive canonical truth.
Gate 184 adds generated bundle navigation index JSON and Markdown entries to
that same handoff archive. The bundle index is derived from existing archive
supplement paths and redacted bundle metadata, and does not change source
schema IDs or make the archive canonical truth.
Gate 185 adds generated triage index JSON and Markdown entries to that same
handoff archive. The triage index is derived from existing redacted Doctor
summary, diagnostic, requirement, action, wrong-scope, and open-question
metadata, and does not change source schema IDs or make the archive canonical
truth.
Gate 186 adds primary Doctor export triage JSON, plain text, and Markdown
summary projection from the same redacted metadata. It does not change source
schema IDs, make generated output canonical truth, or add archive paths to the
primary triage contract.
Gate 187 adds Doctor triage command hints to primary and bundle Doctor export
projections. The hints derive from existing redacted metadata, use canonical
command strings with placeholders, and do not change source schema IDs or make
generated output canonical truth.
Gate 188 adds Doctor triage worklist projection to primary and bundle Doctor
export outputs. The worklist derives from existing redacted metadata and
command-hint IDs, and does not change source schema IDs or make generated
output canonical truth.
Gate 189 adds Doctor worklist summary metadata to primary and bundle Doctor
export outputs. The priority and source summaries derive from existing
worklist items, and do not change source schema IDs or make generated output
canonical truth.
Gate 190 adds a compact Doctor remediation status header to primary and bundle
Doctor export outputs. The header derives from existing worklist and
command-hint data, and does not change source schema IDs or make generated
output canonical truth.
Gate 191 adds human operator handoff sections to plain text, Markdown
summaries, and bundle triage Markdown. The handoff derives from existing
remediation, worklist-summary, and command-hint data, and does not change
source schema IDs, JSON contracts, or canonical source truth.
Gate 192 adds a Doctor bundle `handoff-summary.md` sidecar derived from the
same redacted triage metadata. It points at remediation, worklist, command
hints, and archive paths without changing source schema IDs, JSON contracts,
or canonical source truth.
Gate 193 adds a scan-side operator handoff checklist to plain and Markdown
summary output derived from existing scan metadata. It does not change source
schema IDs, JSON contracts, or canonical source truth.
Gate 194 adds an explain-side operator handoff checklist to plain and
Markdown summary output derived from existing explanation metadata. It does
not change source schema IDs, JSON contracts, or canonical source truth.
Gate 195 extends Doctor bundle requirement explanation Markdown and generated
archive notes to document that same explain-side handoff projection. It does
not change source schema IDs, JSON contracts, or canonical source truth.
Gate 196 adds provider-version declaration metadata to built-in provider
catalogue definitions and renders it in list/explain output. It does not
change source schema IDs, project registry schemas, or canonical project
source truth.
Gate 197 renders the same declaration-only provider-version metadata in scan
and Doctor provider index views. It does not change source schema IDs, project
registry schemas, generated source truth, or canonical project source truth.
Gate 202 records the JIP LN text-script generator evidence checkpoint. It does
not add source schema IDs, project registry schemas, generated source truth,
or canonical project source truth; Gate 203 is the first planned source
contract skeleton step.

Gate 203 adds the `jip-scripts/0.1.0` source registry schema and optional
manifest `registries.jipScripts` root. The schema records source-controlled
JIP LN text-script intent only and does not make generated script files
canonical truth.

Gate 204 adds semantic validation over those schema-valid source contracts.
It does not add a new source schema ID or make generated script files
canonical truth.

Gate 205 extends the unpublished JIP source schema with opaque body/source-line
records. The lines are canonical source records only; generated script files
remain non-canonical and future work.

Gate 206 adds semantic validation over those canonical source-line records
without adding a new schema ID or making generated script files canonical
truth.
