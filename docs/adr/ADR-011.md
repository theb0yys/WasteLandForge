# ADR-011 - Validation and Release Governance Model

Status: Accepted
Research classification: Documented
Source: `WasteLandForge/research/WastelandForge Validation Testing CI Release and Governance-deep-research-report.md`

## Decision

WastelandForge uses layered validation, deterministic fixture-backed testing, GitHub Actions CI centered on Windows and .NET, immutable published schemas, SemVer-governed version streams, mandatory local build manifests, optional SLSA-style release provenance, least-privilege repository governance, and contribution rules that do not require AI.

## Consequences

- Validation is staged from load/source checks through release validation.
- Canonical issue data is JSON.
- SARIF is the diagnostics exchange format.
- TRX is the native .NET test output.
- Public fixtures must be synthetic and redistributable.
- GitHub Actions must include a mandatory Windows lane once CI exists.
- Release publish flows require governance checks and explicit approval.

## Gate Impact

Gate 7 creates fixture/test structure. Gate 8 creates CI and governance
baseline. Gate 9 creates release dry-run and build manifest evidence. Gate 61
extends local build-manifest evidence to `forge build --target reports` and
keeps release publishing gated separately. Gate 62 extends deterministic
fixture-backed tests and local manifest/checksum evidence to the `mcm-json`
generate/build target while keeping release publishing gated separately.
Gate 63 adds local generated-output schema validation for MCM Extender JSON
and extends fixture-backed tests around that validation path.
Gate 64 extends fixture-backed tests for generated translation files,
runtime requirement pass-through, and duplicate translation output refusal.
Gate 65 extends fixture-backed tests for generated checkbox and string-toggle
MCM Extender options.
Gate 66 extends fixture-backed tests for generated keybind MCM Extender
options.
Gate 67 extends fixture-backed tests for generated header MCM Extender
options.
Gate 68 extends fixture-backed tests for generated image MCM Extender options.
Gate 69 extends fixture-backed tests for MCM image asset-reference
diagnostics.
Gate 70 extends fixture-backed tests for staged MCM texture asset outputs,
manifest evidence, output digests, and build checksums.
Gate 71 extends fixture-backed tests for package-manifest entries, payload
digests, and checksum evidence.
